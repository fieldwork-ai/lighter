{
  "name": "lighter-benchmark-fixture",
  "version": "1.0.0",
  "private": true,
  "description": "A dependency tree chosen for its shape, not its function: deep, wide, and full of small files, which is what makes an install a filesystem benchmark rather than a network one.",
  "license": "MIT",
  "dependencies": {
    "typescript": "5.7.2",
    "eslint": "9.17.0",
    "webpack": "5.97.1",
    "jest": "29.7.0",
    "express": "4.21.2"
  }
}
