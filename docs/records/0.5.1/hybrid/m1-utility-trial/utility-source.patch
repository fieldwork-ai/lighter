diff --git a/crates/lighter-vmm/src/machine.rs b/crates/lighter-vmm/src/machine.rs
index 57c7cd68..3ccae518 100644
--- a/crates/lighter-vmm/src/machine.rs
+++ b/crates/lighter-vmm/src/machine.rs
@@ -752,6 +752,7 @@ impl Machine {
                         let prepare = || {
                             let _phase = crate::boot_timing::Phase::new("memory_background");
                             if demand_ram {
+                                crate::qos::prepare_in_background();
                                 return memory.prepare_remaining(&ctx.shutdown);
                             }
                             let control = control.expect("deferred memory control");
diff --git a/crates/lighter-vmm/src/qos.rs b/crates/lighter-vmm/src/qos.rs
index 6c3d86c5..5f7b8819 100644
--- a/crates/lighter-vmm/src/qos.rs
+++ b/crates/lighter-vmm/src/qos.rs
@@ -18,6 +18,8 @@ pub const CONNECTION_STACK: usize = 256 << 10;
 
 /// `QOS_CLASS_USER_INTERACTIVE`.
 const USER_INTERACTIVE: u32 = 0x21;
+/// `QOS_CLASS_UTILITY`: work that should yield to foreground latency.
+const UTILITY: u32 = 0x11;
 
 unsafe extern "C" {
     fn pthread_set_qos_class_self_np(qos_class: u32, relative_priority: i32) -> i32;
@@ -25,9 +27,19 @@ unsafe extern "C" {
 
 /// Raises the calling thread to the interactive class. Best effort.
 pub fn raise_interactive() {
+    set_class(USER_INTERACTIVE);
+}
+
+/// Background preparation has a first-access fallback, so it need not compete
+/// at foreground priority with the vCPUs needed to make Docker ready.
+pub fn prepare_in_background() {
+    set_class(UTILITY);
+}
+
+fn set_class(class: u32) {
     // SAFETY: a plain call on the current thread with constant arguments.
-    let rc = unsafe { pthread_set_qos_class_self_np(USER_INTERACTIVE, 0) };
+    let rc = unsafe { pthread_set_qos_class_self_np(class, 0) };
     if rc != 0 {
-        tracing::debug!(rc, "could not raise the thread's QoS");
+        tracing::debug!(rc, class, "could not set the thread's QoS");
     }
 }
