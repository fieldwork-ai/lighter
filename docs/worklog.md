# Worklog: what was tried, what it did

One row per experiment, appended as they happen, so nobody reruns one by accident. Keep rows to one line. Numbers are the ones that decided it, on the machine named; "M5" is the M5 Pro, "M1" the 8 GB M1. Verdicts: **kept**, **dropped**, **ruled out** (a hypothesis, not a change), **open**.

| date | area | tried | result | verdict |
|---|---|---|---|---|
| 2026-09-01 | own disk | mmap the disk image instead of preadv/pwritev (all variants) | slower on every case | dropped |
| 2026-09-01 | own disk | rq_affinity changes, dsb barrier, level interrupt line alone | no change | ruled out |
| 2026-09-02 | own disk | host poller on the block queue alone | wake latency eats it; M1 suite flat | dropped alone, kept with guest poll |
| 2026-09-02 | own disk | guest virtio-blk poll-after-submit (patch 0008), `virtio_blk.poll_usecs` | fio qd1 reads 40k → 128k, writes 35k → 208k; 2 interrupts per run | kept |
| 2026-09-02 | own disk | both sides polling: guest poll + host watcher on block queues | fio reads 124k/415k, writes 316k/290k (qd1/qd8) | kept |
| 2026-09-02 | own disk | bounce buffer for reads instead of preadv into guest pages | 130k vs 124k, noise | dropped |
| 2026-09-02 | own disk | fio working-set size 16M..2G | 16M still 6.4 µs/read; not a cache miss story | ruled out |
| 2026-09-02 | own disk | one virtio-blk queue per vCPU (`VIRTIO_BLK_F_MQ`) | read completions no longer via ksoftirqd; fio reads 124k → 438k qd1 | kept |
| 2026-09-02 | guest kernel | lean config: no PAC/BTI/MTE, no stack zero-init, stack-protector regular, no audit, no IRQ accounting, voluntary preempt | stat/open ~8% faster on M1, nothing on installs | dropped |
| 2026-09-02 | guest kernel | `rodata=on` | no change | ruled out |
| 2026-09-02 | memory | free-page reporting withheld | fresh-memory touch 3x faster (60 → 15 ms per 512 MiB) but OrbStack pays the same; installs unchanged | open (lever past parity) |
| 2026-09-02 | memory | guest 6 GB instead of 4 GB on the M1 | worse (host is 8 GB) | ruled out |
| 2026-09-02 | scheduling | `idle=poll` in the guest | no change: vCPU wake latency is not the gap | ruled out |
| 2026-09-02 | scheduling | QoS classes on vCPU threads, static (all interactive) | flat on M5 and M1 | dropped |
| 2026-09-02 | scheduling | QoS override on busy vCPUs only, sampled every 25 ms | flat on M5; npm slightly worse on M1 | dropped |
| 2026-09-02 | scheduling | 4 vCPUs instead of 8 on the M1 | yarn −4%, npm −7%, sys −1 s | open (cause unknown; not QoS) |
| 2026-09-02 | filesystem | ext4 without metadata checksums | mkdir+rmdir −40%, unlink −35% on M1; installs unchanged | dropped (btrfs is the default; ext4 keeps the standard format) |
| 2026-09-02 | filesystem | ext4 without a journal | no change | ruled out |
| 2026-09-02 | filesystem | ext4 without dir_index | readdir −40% but file creation 10x slower | ruled out |
| 2026-09-02 | filesystem | XFS data disk (reflinks) | yarn 10.4 → 8.5 s on M1 (system time halved); rm-rf 350 → 620 ms | superseded by btrfs |
| 2026-09-02 | filesystem | XFS log/agcount tuning (`logbsize=256k,logbufs=8`, `agcount=8`) | no change | ruled out |
| 2026-09-02 | filesystem | btrfs data disk, nodatacow, `-m single` | yarn 8.3 s on M1 (OrbStack 7.8); copy-tree at parity on M1 | kept (decision) |
| 2026-09-02 | filesystem | VFS clone-size threshold (`fs.clone_min_bytes`) | a clone beats a copy at every size on btrfs once files are clean (1 KiB: 24 vs 50 ms/2000); the "small clones cost 2x" reading was dirty pages being flushed | dropped |
| 2026-09-02 | filesystem | btrfs `ssd` mount option | no change | ruled out |
| 2026-09-02 | filesystem | btrfs inline completion of checksum-free reads (patch 0009) | fio reads on btrfs 39k → 415k qd1 | kept |
| 2026-09-02 | filesystem | same for writes on 6.18 (where btrfs punts writes to the workqueue too) | fio writes 32k → 119k qd1 | kept |
| 2026-09-02 | memory | THP `madvise` instead of `always` | no change | ruled out |
| 2026-09-02 | writeback | `vm.dirty_writeback_centisecs=1500` (OrbStack's) | no change | ruled out |
| 2026-09-02 | own disk | hole-filling cost of a fresh sparse image | 1 GB fresh vs overwrite equal; copy-tree ×5 stable at 2.8 s on M1 | ruled out |
| 2026-09-02 | guest kernel | 6.12.51 → 6.18.49 (nine patches re-derived; one now upstream) | install suite identical on M1; fio identical on the quiet M1 | kept (LTS) |
| 2026-09-02 | guest kernel | 7.2.3 measured | suite identical to 6.18; rm-rf 2.5x slower (7.x btrfs defaults `discard=async`); completion patch hangs it | ruled out |
| 2026-09-02 | virtio-blk | poll only for synchronous requests (writeback's async writes get one look) | wall time unchanged; a third of a CPU no longer burned under a tree copy | kept |
| 2026-09-02 | VMM | exit counting (`LIGHTER_EXIT_STATS=1`) | yarn is 3k–10k exits/s, all virtio kicks; not the gap | kept (diagnostic) |
| 2026-09-02 | share | pnpm on the share, guest profiles | ours 68% idle with a kick per request; OrbStack spins in `virtio_fs_request_complete` and is answered in time — fs-server latency | open (share milestone) |
| 2026-09-02 | own disk | copy-tree in a persistent VM (`.logs/loop.sh`), counters per copy | our copy writes the full 1 GB to disk while `cp` runs (66k writes, 2.9 s system); OrbStack's writes 80 MB and 1 s system — writeback is forced mid-copy on our side | open (cause being found) |
| 2026-09-02 | memory | guest 16 GB instead of 8 GB on the M5 (OrbStack's guest is 16 GB) | copy unchanged: still 1 GB written during the copy | ruled out (not the dirty threshold) |
| 2026-09-02 | scheduling | vCPU count 4 vs 8 on the M1 | yarn −4%, npm −7% (see above); QoS is not why | open |
| 2026-09-02 | filesystem | btrfs default mounted an existing ext4 disk with btrfs options (`nodatacow`) | mount failed, dockerd started on the 2 GB root and died — the daily driver lost Docker; fixed by mounting by the type blkid reports, formatting by the default | fixed (trap) |
| 2026-09-02 | own disk | per-device counters around a copy on both guests | OrbStack writes the full 1 GB during the copy too (55k writes, 20 KB each) — the gap is per-write cost: 3.0 s system vs 1.6 for the same writes | open (completion path) |
| 2026-09-02 | virtio-blk | poll for asynchronous (writeback) requests too (`virtio_blk.poll_async`, runtime switch) | copy 2.0–2.1 s vs 2.2–2.7 without; system time 2.9 s either way | kept on by default (small wall gain), not the cost |
| 2026-09-02 | own disk | copy at 1, 2 and 8 vCPUs, and 8 with `idle=poll` | wall 1.7–2.2 s at every count; system time 1.4–1.7 s at 1–2 vCPUs vs 2.8–3.0 at 8 — btrfs workers waking each other across idle vCPUs cost ~1.3 s of CPU off the critical path. (`idle=poll` is x86-only and measured nothing on arm64; the generic switch is `nohlt`) | open (worker pool / wakeups) |
| 2026-09-02 | filesystem | btrfs `thread_pool=2` / `=1` mount option | copy system time 2.9 → 2.5 / 2.2 s, wall unchanged | open (decide after the reclaim fix) |
| 2026-09-02 | filesystem | why the copy is written back mid-copy: call chain is `btrfs_preempt_reclaim_metadata_space → flush_space → btrfs_start_delalloc_roots` | the fs has one 256 MB metadata chunk (btrfs's size for disks under ~50 GiB; the bench disk is 32 GiB) and every small file's reservation trips preemptive reclaim; OrbStack's fs has a 1 GB chunk with 700 MB free | open (fix in flight: bigger metadata chunk) |
| 2026-09-03 | filesystem | btrfs preemptive metadata reclaim off (`btrfs.preemptive_reclaim=0`, patch 0010) | copy system time 2.9–3.1 → 2.2–2.4 s, wall unchanged; the copy is still written back in full by a second path | kept as a switch; not the whole story |
| 2026-09-03 | filesystem | who writes the copy back with preemptive reclaim off | 93% `btrfs_async_reclaim_metadata_space` — reservations ticketing; a 64 GiB disk's 1 GB chunk does not change it (47k writes mid-copy, 2.4 s system); the writer blocks on its ticket while the flush runs | open (overcommit fraction under test) |
| 2026-09-03 | filesystem | overcommit fraction for flushing reservations (`btrfs.overcommit_shift`): 3 (upstream eighth) vs 1 (half) vs 0 | copy 1.75–1.92 s → 1.51–1.69 s at 1; writes 47k → 8.6k (per-file flushes gone, dirty flusher writes in bulk); 0 no better; system time 2.3 s unchanged | kept (default to decide) |
| 2026-09-03 | filesystem | reclaim off + half-device overcommit, guest at 16 GB (OrbStack's guest size on the M5) | copy 1.09–1.24 s = OrbStack's 1.05–1.3; with `thread_pool=1` system time 1.55–1.68 s = theirs. The bench had been giving our guest 8 GB against their 16 | kept; bench memory to match |
| 2026-09-03 | filesystem | btrfs `thread_pool=1` / `=2` with the switches on, guest 8 GB | copy 1.28–1.57 s / 1.38–1.54 vs 1.48–2.17 default; system 1.8 / 2.1 / 2.2–2.5 s | open (M1 install legs queued; pick after) |
| 2026-09-03 | bench | guest memory matched to OrbStack's guest (`bench_memory_mib` in run.sh) | the M5 bench had given us 8 GB against their 16 | kept |
| 2026-09-03 | filesystem | M1 install suite with the reservation switches (reclaim off, half-device overcommit) vs upstream defaults, two legs each | yarn 8.1–8.4 s, npm 10.7–11.4, pnpm 3.1–3.5 either way; copy-tree 2.9–4.0 vs 3.2–4.2 — the installs never ticket, the switches are a copy-tree fix | kept (no install effect) |
| 2026-09-03 | filesystem | btrfs `thread_pool=1` vs default (8) on the M1: install suite ×2, fio, 8-job sequential write, 8 parallel buffered writers | installs: npm 10.3–10.4 s vs 10.7–11.4, yarn/pnpm/copy same; fio randwrite 154–159k vs 140–142k, reads same; seqwrite 8 jobs 850–912 MB/s vs 710–810; parallel writers same. One worker per btrfs queue is never worse | kept (default) |
| 2026-09-03 | own disk | fresh M5 table, patch 0010 defaults, 16 GB guest, medians ours/OrbStack | npm 8821/7160, pnpm 2442/2101, yarn 5722/5044, ripgrep 131/107, find-walk 128/130, copy-tree 1926/1078, rm-rf 918/524. The copy is 1.1 s in the loop (sync before it) and 1.9 in the suite (earlier trees still dirty); rm-rf removes a copy made seconds ago, dirty now that nothing flushes it mid-copy — OrbStack's stock reclaim writes its copy back as it goes and its rm finds clean inodes | open (both) |
| 2026-09-03 | wakeups | `perf bench sched pipe` in both guests, M5 | ours 19.7 µs a round trip unpinned and 18.9 pinned to two vCPUs, 1.4 pinned to one; OrbStack 1.2 on one, two, any two — and its host idle. Cross-vCPU wakeup is the gap: an installer's thread pool pays it on every handoff, which is why its CPU time matches OrbStack's while its wall time does not | root cause (installs) |
| 2026-09-03 | wakeups | `nohlt` (every idle vCPU spins) | pipe 14.2 µs (the IPI trap itself is the rest); pnpm 1.97 s (from 2.4, OrbStack 2.1) but npm 9.2 (from 8.3) and yarn 7.0 (from 5.8) with eight host threads spinning; idle host CPU 8 cores | discarded (unbounded) |
| 2026-09-03 | wakeups | patch 0011: bounded adaptive poll before WFI with TIF_POLLING_NRFLAG defined for arm64, so a waker writes the flag and sends no IPI (`idle.poll_ns`, 200 µs cap) | pipe two vCPUs 18.9 → 2.9 µs (unpinned 7.6), idle host CPU still ~0; M5 own disk npm 8.3 → 5.1 s, pnpm 2.4 → 1.45, yarn 5.8 → 4.5 — all past OrbStack (7.2 / 2.1 / 5.1); system time halves | kept |
| 2026-09-03 | wakeups | patch 0011 on the M1 (4 GB guest) | pipe two vCPUs 21 → 1.9 µs; own disk yarn 7.3 s (OrbStack 7.8), npm 8.5–8.6 (10.9), pnpm 2.0–2.3 (2.4); copy-tree 3.3–4.1 (3.1) and rm-rf 0.72 (0.68) unchanged — the wakeup patch is not a writeback fix | kept; copy/rm on the M1 open |
| 2026-09-03 | suite | M5 on patch 0011, 16 GB, medians ours/OrbStack (same session) | share: npm 7903/8638, pnpm 4395/5304, yarn 5673/8045, ripgrep 101/1011, find-walk 133/469, copy-tree 4391/9326, rm-rf 3126/3333, watch 6/12 ms. Own disk: npm 5613/7160, pnpm 1499/2101, yarn 4477/5092, ripgrep 94/107, find-walk 120/130, copy-tree 1568/1078, rm-rf 595/524 | ahead on 13 of 15; copy-tree and rm-rf on own disk open |
| 2026-09-03 | suite | M1 on patch 0011, 4 GB guest (OrbStack's), medians ours/OrbStack | own disk: npm 8069/10418, pnpm 1953/2393, yarn 7606/7790, ripgrep 128/157, find-walk 127/130, copy-tree 2696/3141, rm-rf 745/672. Share: npm 12860/11027, pnpm 10095/5889, yarn 13962/10012, ripgrep 216/1108, find-walk 179/514, copy-tree 6665/13726, rm-rf 4634/3887, watch 2/3 | own disk ahead but rm-rf; share installs and rm-rf behind (fd ceiling / 4 GB) |
| 2026-09-03 | own disk | why the suite's copy-tree is 1.6 s when the loop's is 1.1: the suite VM's 32 GiB disk | btrfs reserves a worst-case 256 KB of metadata per dirty file (75k files: 19 GB) against half the unallocated device less a 10 GB data chunk: 9 GB on 32 GiB (tickets, 65k requests, 1.4–1.6 s), 24 GB on 64 GiB (no tickets, 8k requests, 1.1 s); shift 0 on 32 GiB still tickets (18 GB). OrbStack's guest disk is 320 GB virtual; ours is 64 by default and the bench gave 32 | root cause; fix: plug the flush (patch 0012) and size the disk like OrbStack |
| 2026-09-03 | own disk | patch 0012: `blk_start_plug` around `start_delalloc_inodes` | 32 GiB disk: the ticketed copy 65k → 6.2k requests, 1.42–1.64 → 1.37 s; the writer still waits on its ticket, so not the whole gap | kept |
| 2026-09-03 | own disk | data disk sized to the Mac's free space (product default and the bench; 64 GiB floor), as OrbStack's | suite copy-tree 1098–1160 ms (OrbStack 1050–1109), rm-rf 498–514 (481–537). Trap on the way: fstrim of a 325 GiB device sends a 2 GiB−512 discard, APFS refuses an unaligned punch (EINVAL), and init's `set -e` died on the failed fstrim — punch the aligned interior, and fstrim is `|| true` | kept; M5 own disk at parity or ahead on all 7 |
| 2026-09-03 | share | M1 share pnpm at 4 GB vs 8 GB guest | 9.2–9.9 s either way (OrbStack 5.9); rm-rf 4.5–5.5 (3.9). Not memory: 22–25 s of guest system time per install is the FUSE round trips and the reply spin | open (share) |
| 2026-09-03 | suite | M5 final (patches 0011+0012, free-space disk, 16 GB), medians ours/OrbStack | share: npm 8724/8616, pnpm 4352/5349, yarn 6411/7945, ripgrep 100/1036, find-walk 121/488, copy-tree 4188/9135, rm-rf 2844/3289, watch 2/3. Own disk: npm 5007/7022, pnpm 1377/2002, yarn 4475/5009, ripgrep 96/106, find-walk 121/131, copy-tree 1196/1060, rm-rf 495/484 | ahead on 13 of 15; share npm within 1%, own-disk copy-tree 13% behind |
| 2026-09-03 | share | `sample` of the VMM on the M1 during pnpm on the share | vCPU threads spend 30–45% of their time in a pthread mutex wait inside the run loop, the fs pool's waker and the poller wait on it too: the fs transport lock. VMMSTATS: ~40% of requests still trap (notifies 35k per 3 s against 23k polled), inflight ≈1.0 — pnpm is serial on the share, so every trap is on the critical path. Per 3 s: 27k "other" (FICLONE) at 50 µs, 27k create at 12 µs, renames at 250 µs | root cause candidate (M1 share); poll window / worker legs running |
| 2026-09-03 | share | host poll window 200 / 1000 / 5000 µs, and 4 fs workers, pnpm on the share | M1 9.6 / 9.7 / 10.6 / 10.0 s; M5 pnpm 4.4 / 4.3 / 4.3 — and the doorbell exits do not move: ~40% of requests still trap at every window | discarded (not the window) |
| 2026-09-03 | share | kicks counted per device kind | every trap is the fs device; FORGET (27k per 3 s on pnpm) rides the hiprio queue, which the poller did not watch, so each batch of forgets was a doorbell exit while the request queues were quiet | root cause |
| 2026-09-03 | share | poller watches hiprio too | M5 pnpm share: traps 32k → 44 per 3 s window in the steady state, system time 13.7–14.1 → 10.7–11.3 s, wall 4.3 either way (18 cores absorb the exits); M1 pending | kept |
| 2026-09-03 | own disk | M1 copy-tree / rm-rf on the plug kernel and thread_pool=1 rootfs, still a 32 GiB disk | copy-tree 1.72–1.90 s (was 2.7; OrbStack 3.1), rm-rf 0.69–0.77 (0.67) | kept; free-space disk to come with the synced tree |
| 2026-09-03 | suite | M1 final (patches 0011+0012, free-space disk, 4 GB), medians ours/OrbStack | own disk: npm 8008/10798, pnpm 1808/2427, yarn 7569/8089, ripgrep 130/154, find-walk 127/130, copy-tree 2800/3145, rm-rf 630/690 — ahead on all seven. Share: npm 14156/10915, pnpm 9433/5941, yarn 13357/9885, ripgrep 277/1151, find-walk 138/516, copy-tree 6666/13523, rm-rf 4740/3859, watch 2/3 | own disk done on both machines; M1 share installs and rm-rf are the remaining gap |
| 2026-09-03 | share | M5 share with the stock-Mac descriptor budget (6144) and a 4 GB guest | pnpm 4.9 s vs 4.3 — the budget costs ~13%, nowhere near the M1's 9.4 vs OrbStack's 5.9 | not the M1 gap |
| 2026-09-03 | share | M1 resampled with hiprio watched | mutex waits fall from 3.5–5k to 0.1–2.7k sample-ms per 4 s window; what remains is the fs pool's waker and the vCPUs waiting on the fs transport lock while the poller serves a request INLINE under it (`Inline::WhenIdle`: a lone request is served on the polling thread, lock held for the syscall, 50 µs for a clone) | inline-policy legs running on both machines |
| 2026-09-03 | share | inline policy on pnpm/npm share: `whenidle` (default) / `never` / `always` | M5: 4.3 & 7.9–8.1 / 4.4 & 8.7–9.0 / 4.4 & 8.1–8.5 s; M1 pnpm: 8.0–11.5 / 9.3–9.6 / 8.9–10.5. The default stands | kept |
| 2026-09-03 | share | poller + fs workers at user-interactive QoS (`LIGHTER_SERVER_QOS=1`) | M1: the poller then catches every request (polled = notifies) and guest system time falls 16–18 → 6–7 s, but wall is unchanged (8.5–10.3 s); M5: pnpm 5.3 vs 4.3 — worse. Traps were not the M1 wall cost | discarded (env switch kept) |
| 2026-09-03 | share | 4 vCPUs on the M1 (the product default there is 4) | pnpm share 7.4–7.9 vs 8.5–10.0 at 8; own-disk installs lose parallelism | open (bench uses 8 for both) |
| 2026-09-03 | share | `op-latency` instrument on the M5, share µs ours / OrbStack | create+close 25 / 48, stat-cached 0.8 / 0.8, stat-missing 12 / 19, write-4k 44 / 58, write-chunked 68 / 106, unlink 39 / 20, link 160 / 203, rename 34 / 43, create-parallel 33 / 47. Unlink is the one op we lose, 2× | unlink open |
| 2026-09-03 | share | QoS raise extended to the apply workers; M1 pnpm share, reps 1/2/3 | 8 vCPUs: 11.8 / 9.5 / 8.2 s (no QoS 10.2 / 9.5 / 8.7); 4 vCPUs 10.5 / 9.2 / 8.0; 6 apply threads 10.2 / 9.0 / 8.2. M5 neutral (pnpm 4.2, npm 7.7–8.1). Reps get faster every time on our share and barely on OrbStack's (6.2 / 5.9 / 5.8) | small; switch kept |
| 2026-09-03 | share | what pnpm's import costs us: LIGHTER_CLONE (opcode 62000, our whole-file clone) acknowledged in 27 µs on the M5 and 37–50 on the M1, 62k per install — 1.7 s of a 4.3 s install, 2.3 of 8 | target: the clone acknowledgement path |
| 2026-09-03 | share | clone acknowledgement phased (stats keyed `clone-us-*`), M5 pnpm | of 3.6 s across 124k clones: `Apply::push` 2.4 s (20 µs each — it BLOCKS while the queue holds more than its 1024-job window, and three workers copying on APFS cannot keep up with pnpm), resolve/settle/path 0.7 s, the rest under 0.1 s. The guest is waiting on host apply throughput, not on us | root cause; window legs running |
| 2026-09-03 | share | apply window 4096 / 16384 | M5 pnpm 4.3 either way, npm worse (8.5–10.2 vs 7.9–8.1: the APFS contention the window exists to cap); push still waits 2.1–3.5 s — the byte cap binds next. On the M1 both deep-window legs FAILED: pnpm exited non-zero, and the boot log carries "an acknowledged create failed to apply errno=17" — a promised create that lands on EEXIST when the queue runs far ahead. A correctness item, not a tuning one | discarded; bug noted |
| 2026-09-03 | share | rm-rf profiled on the share, M1 / M5 mean µs | readdir 115 / 42, rmdir 89 / 70, unlink 5.8 / 10, setattr 18 / 15. The tree was copied a moment before, so the first readdir at each level settles the apply backlog ("reads never lie"); the timed rm pays for the untimed copy. M1 rm-rf this run 3.9–4.3 s (OrbStack 3.9) | close; readdir cost open |
| 2026-09-03 | share | apply byte cap 64 → 256 MB | M5 pnpm 4.3 → 5.5 s, push waits 4.3 → 5.9 s: a deeper backlog only adds APFS contention. M1 8.1–10.3 either way, and its push waits are only 0.5 s — the M1 guest is not waiting on the caps at all. Both M1 legs logged "acknowledged create failed to apply" (1, 1, 7 with QoS) at the DEFAULT cap too | discarded; the EEXIST-on-apply bug reproduces on the M1 |
| 2026-09-03 | share | guest reply spin `virtiofs.poll_usecs` 100 (default) / 20 / 0 | M5 pnpm 4.3 / 5.0 / 4.3–5.1, npm 7.9–8.1 / 8.1–8.2 / 8.2–8.3, guest system time 11 → 6.5 → 3 s; M1 pnpm best reps 8.0 / 8.7 / 9.5. The spin is where the guest's system time goes and it buys wall time; the default stands | kept |
| 2026-09-03 | share | clone reply carries the entry; guest keeps its dentry and inode (patch 0004 reworked, server forwards the caller's nodeid) | M1 yarn 13.4 → 10.7–11.0 s (OrbStack 9.9); pnpm 8.3 unchanged — its per-file FORGET/LOOKUP pair was not the dentry drop after all: the run's forgets are the setup's rm -rf, and its lookups are the ENOENT probe before each create | kept (yarn win) |
| 2026-09-03 | share | pnpm's requests per imported file, from strace and the histogram | LOOKUP (ENOENT), CREATE, LIGHTER_CLONE, SETATTR (utimes), GETATTR — the GETATTR is a path stat of the store file, one per import (47k statx in strace, 63k GETATTR per rep) | target |
| 2026-09-03 | share | cache lifetime 30 s → 300 s under push (`Timings::PUSHED`) | no change in GETATTRs (218k a run either way); a debug kernel says why each syncs: `cache=0` — the guest has no writeback cache, so the write's MODSIZE invalidation and the rename's ctime invalidation force the next stat to the server | kept (harmless); root cause found |
| 2026-09-03 | share | patch 0013: a write moves size/blocks/times in place and a rename moves ctime, for the create dialect, instead of invalidating | building | in flight |
| 2026-09-03 | share | patch 0013 (writes and renames move times in place) | M5 GETATTRs 218k → 126k a pnpm run, pnpm 4.27–4.31 s; the rest is atime — pnpm hashes every store file it imports, the read invalidates atime, the next stat syncs. Fix is the guest's mount: `noatime` on every share (init), no kernel change | kept; noatime rootfs built |
| 2026-09-03 | share | why the store-file GETATTRs survived patch 0013's first hunks and `noatime`: `inval=ffffffff` — upstream's evict-race guard in `fuse_change_attributes_common` leaves a brand-new inode's mask set when any eviction overlapped its lookup, so its first stat is a round trip; under an install's churn that is every new inode. A mount's `noatime` never reaches `fuse_invalidate_atime` (mount flag, not inode flag) | patch 0013 now: write/rename/read move times in place, and the guard is skipped for our dialect (the server never reuses a nodeid). M5 pnpm run: GETATTR 218k → 0 | kept |
| 2026-09-03 | share | M1 pnpm/yarn share on the final 0013 kernel (three reps) | pnpm 8.3 / 7.2 / 8.4 s (OrbStack 5.9), yarn 11.8 / 11.1 / 11.4 (9.9) — the M1's run-to-run spread is ±1 s, so single reps decide nothing there | open |
| 2026-09-03 | share | npm's syscalls per install on the share (strace) | write 271k (4.4 per file), statx 233k (72k ENOENT), mkdirat 167k (158k EEXIST, answered by the dentry cache), openat 71k. Requests: write 145k a rep, lookup 70k, create 62k — every write syscall is a round trip without the guest's writeback cache | target: write count |
| 2026-09-03 | share | guest writeback cache (`LIGHTER_FS_WRITEBACK=1`) on the M5 share | npm 9.1–9.9 s (8.1–8.8 without), pnpm 5.0 (4.3): the kernel flushes times with a SETATTR per file (805k a run) and stats come back (190k). The server's refusal was right for speed too | discarded |
| 2026-09-03 | correctness | "an acknowledged create failed to apply errno=17", 1–8 per pnpm run on the M1 | pnpm's import threads race to make the same store file (content-hash names). The first create is promised; the second took the synchronous path, which settled the parent and any queued removal of the name but not its queued creation, reached the host first, and the promise landed on EEXIST with its bytes lost. The synchronous path now settles a promised name too, so the exclusive second creator sees EEXIST as it should. Regression test added | fixed |
| 2026-09-03 | share | which names the ENOENT lookups ask for (sampled) | store hashes before pnpm writes them and package directories before it makes them: pnpm's own existence checks, one per file, not the kernel's | stays (application-driven) |
| 2026-09-03 | share | clone acknowledgement phased on the M1 | of 6.5 s across a 7.6 s run: `stat_fd` of the source 4.0 s — the store is parked under the stock descriptor budget, so an fstat reopened the file by path on the serial track; building the source's path 1.2 s | root cause (M1) |
| 2026-09-03 | share | clone source located by its parent and name (`Located::At`): one `fstatat` on the serial track, the job opens the source itself on a worker; the path only for the event-only fallback | M1 stat term 4.0 → 2.6 s over three phases (10.8 µs a clone), pnpm best rep 7.1 → 6.7 s (OrbStack 5.9); push waits are now the top term there too — the M1 has become host-apply-bound like the M5 | kept |
| 2026-09-03 | share | rename phased | 48.5k of 50.4k renames promised; the 1.9k that go synchronous are DIRECTORY renames (declined by kind), and each settles the backlog: 1.7 ms apiece, 3.3 s of a three-phase M5 run — about a second a rep | target |
| 2026-09-03 | correctness | the EEXIST race, second mechanism | pnpm writes the full-hash name and renames it to the short one; two threads doing that for identical content leave two HELD creates for the short name — the promised rename re-names a held create without queuing anything — and the second's create was queued first. The holder of the new name is now materialized before a replacing rename queues over it. Regression test added; 4 → 1 failure per run on the M1, one mechanism left | mostly fixed |
| 2026-09-03 | share | a setattr acknowledged while its clone is queued lands in the clone job's pass (chmod/utimes by name at bind); its own job keeps only the bookkeeping | M1 pnpm 6.6 / 7.8 / 6.8 s, no failed creates; M5 4.3 either way (host-bound: push waits 5.8 s of a two-rep run) | kept |
| 2026-09-03 | share | directory renames promised like files (only renaming OVER a directory stays synchronous) | fs tests green; m4 gate running | in flight |
| 2026-09-03 | share | directory renames promised, M1 pnpm ×3 | 6.9 / 6.6 / 7.2 s (OrbStack 5.9), no failed creates; M5 rename term 4.75 → 1.22 s but push waits 5.8 → 9.1 s and wall unchanged — the guest only reaches the caps sooner | kept (correct and simpler); the host apply is the bound on both machines |
| 2026-09-03 | share | apply workers 3 / 5 / 8 on the M5 (pnpm) | 4.3 / 4.0–4.1 / 4.4–5.2 s; per-clone job time 154 → 240 µs at five (APFS contention), wall still better; M1 legs running | 5 likely |
| 2026-09-03 | share | copy job keeps its descriptor: fchmod, futimens and fstat on it instead of three path lookups | M5 pnpm 4.06–4.3 at five workers; gates running | in flight |
| 2026-09-03 | share | apply workers on the M1 (pnpm ×3): 5 → 5.7 / 6.9 / 6.5 s, push waits 2.3 s; 4 → 6.5 / 6.1 / 7.0, waits 3.8 s (3 earlier: 6.9 / 6.6 / 7.2, waits 6.5 s) | best rep within 0.2 s of OrbStack's 5.8; median still 0.6 behind | default 3 → 5 |
| 2026-09-03 | share | M1 share installs on the committed tree (five workers), three reps, medians ours/OrbStack | pnpm 6.2/5.9, yarn 10.8/9.9, npm 11.9/10.9 — 5–9% behind each, from 60/35/30 at the start of the night; no failed creates | open |
| 2026-09-03 | share | clone request carries the source's size and mode (patch 0004 reworked again) so the acknowledgement stats nothing | building | in flight |
| 2026-09-03 | share | npm profiled on the M1: 122k create jobs at 101 µs, all on the serial lane (6 s of one worker per install) while the clone lane runs five wide | creates, writes, mkdirs and symlinks are keyed by directory and file, so they only overlap across directories — the same parallelism unlinks and rmdirs already get. Switch `LIGHTER_FS_APPLY_WIDE=1` | root cause (npm/yarn) |
| 2026-09-03 | share | wide lane, M5 npm share ×2 | 9.0 / 8.3 → 6.2 / 6.9 s (OrbStack 8.6), no failed creates; M1 legs running | kept if the M1 agrees |
| 2026-09-03 | share | wide lane on the M1 (npm ×3): 11.7–13.2 s vs 12.4–13.4 serial; yarn 11.5–12.9 | neither gain nor loss where the vCPUs already fill the cores; the M5 gains a quarter | default on |
| 2026-09-03 | share | clone request carries the source's size and mode; M1 pnpm ×3 | 7.0 / 6.7 / 7.0 with push waits 9.1 s — the acknowledgement stat was never the M1's bound once the queue was; the M1 host is CPU-starved by its own eight vCPUs. Four-vCPU legs running (the product default there) | kept (exact, cheaper); M1 open |
| 2026-09-03 | bench | M1 at four vCPUs (the product default there) with the wide lane: share pnpm 5.9–7.0, yarn 9.9–11.2, npm 12.2–13.1; own disk npm 7.5–7.6, yarn 7.3–7.4, pnpm 1.6–2.0 | a wash on the share, a touch faster on own disk; the bench keeps eight, OrbStack's count | no change |
| 2026-09-03 | bench | every case's setup ends in `sync` | our `rm -rf` of the previous tree returned at once and its 66k host unlinks (7 s of a worker) drained inside the timed install; OrbStack removes synchronously in the untimed setup. Both runtimes now start each measurement with their queues drained | kept; final suites running |
| 2026-09-03 | suite | finals with the drained setups, medians ours/OrbStack. M5 share: npm 7321/8673, pnpm 4000/5403, yarn 5440/8102, ripgrep 93/1040, find-walk 121/470, copy-tree 3333/9004, rm-rf 2689/3307, watch 2/99. M5 own disk: npm 5150/7038, pnpm 1355/2056, yarn 4501/5083, ripgrep 95/101, find-walk 121/132, copy-tree 1217/1122, rm-rf 514/477. M1 share: npm 12675/11042, pnpm 6585/5917, yarn 12827/10080, ripgrep 334/1129, find-walk 124/518, copy-tree 5583/16475, rm-rf 4941/3903, watch 3/2. M1 own disk: npm 7787/9640, pnpm 1796/2408, yarn 7617/7838, ripgrep 143/147, find-walk 127/134, copy-tree 2944/2583, rm-rf 645/717 | M5 ahead on 13 of 15 (copy-tree, rm-rf 8% behind on own disk); M1 own disk ahead on 6 of 7 (copy-tree, noisy), share behind on the three installs and rm-rf | standing |
| 2026-09-03 | own disk | the M5's 8% on copy-tree/rm-rf: in the loop on a fresh filesystem both match OrbStack (1.10–1.15 s, 480–490 ms); after the suite's installs they are 1.16–1.24 and 490–500. `ssd`, `discard=async`, thread_pool 1/4/8 change nothing | filesystem history; open |
| 2026-09-03 | wakeups | guest idle poll cap on the M1 (`idle.poll_ns`): 0 / 50 µs / 200 µs (default) | share pnpm 5.5–5.7 / 5.3–6.4 / 6.4–7.1 s (OrbStack 5.9), share npm 12.8–13.5 / 10.8–12.3 / 12.7 (11.0); own disk pnpm 2.9–3.0 / 1.7–2.2 / 1.8, npm 10–11.2 / 8.0–8.5 / 7.8. On eight cores the idle vCPUs' spinning starves the server that the share is waiting on; 50 µs keeps the own-disk win and hands the share back its cores | 50 µs where the vCPUs fill the cores, 200 otherwise |
| 2026-09-03 | wakeups | `idle.poll_ns` set from the vCPU-to-core ratio: 50 µs where the vCPUs fill the Mac's cores, 200 otherwise (`config::idle_poll_ns`, mirrored in run.sh) | M5 stays at 200 (8 of 18 cores), M1 goes to 50 (8 of 8) | kept |
| 2026-09-03 | wakeups | 50 µs on the M1, the rest of the cases: share yarn 10.0–11.0 (OrbStack 10.1), rm-rf 3.8–4.6 (3.9); own disk yarn 7.8–8.3 (7.8), rm-rf 660–700 (717). 100 µs: share pnpm 5.2–6.2, npm 11.2–12.0 | share within noise of OrbStack on every install now; own-disk yarn gives back a few percent | 50 kept; final M1 suite running |
| 2026-09-03 | suite | M1 final with the 50 µs idle poll, medians ours/OrbStack. Share: npm 12628/11021, pnpm 6422/5914, yarn 12414/10057, ripgrep 183/1147, find-walk 129/521, copy-tree 5638/17201, rm-rf 4879/3927, watch 3/66. Own disk: npm 7662/10445, pnpm 1803/2414, yarn 8009/7720, ripgrep 130/160, find-walk 129/129, copy-tree 3110/2363, rm-rf 673/765 | the share installs are 9–23% behind IN THE SUITE while within noise of OrbStack in isolation (pnpm 5.2–6.4, npm 10.8–12.3, yarn 10.0–11.0): the suite runs them back to back in one VM, where the stock descriptor budget churns harder as the registry fills | open: descriptor churn across a long-lived VM |
| 2026-09-03 | share | parked-inode revivals counted (`REOPENS`, printed with the stats) | M5, three installs: 202k; M1: 936k, with the descriptor count swinging 7–5,656. Every one an open, an fstat and a close the workload never asked for — ~30 µs on the M1, 28 s across 35 s of installs. Backtraces: readdirplus → `relookup` → `still_is` → `reference` — the identity check of every listed entry revived it, and every clone is bound parked | root cause (the suite-vs-loop gap) |
| 2026-09-03 | share | `still_is` checks a parked inode by its parent and name (one fstatat, birth time as the tiebreak) | M5: reopens 202k → 46, readdir mean 86 → 44 µs, pnpm 3.9–4.3, rm-rf share 2.7–3.0 (OrbStack 3.3); M1 running | kept |
| 2026-09-03 | share | M1 with the by-name identity check: revivals 936k → 401k across the installs, descriptors pinned at the 6.2k budget; sampled backtraces of the rest: three of five from the sweep's own `promote` — parking a batch of cold files and reviving a batch of warm ones into their places, every pass | promotion now only into real headroom (budget − open), never into the room the sweep is making | in flight (M1 running) |
| 2026-09-03 | share | headroom-only promotion on the M1: pnpm 5.9 / 6.5, npm 13.0 / 11.4; the sampled revivals that remain come through `reference` from `entry` and from the create job's `parent.reference()` — parked DIRECTORIES, which past half the budget the sweep parks and every operation inside revives | `locate`/`reopen` now reach a parked inode through its nearest resident ancestor's descriptor and a relative path (`nearest_resident`), so a parked directory is never opened to reach what is in it | in flight (M1 running) |
| 2026-09-03 | suite | M5 final on the by-name identity build, medians ours/OrbStack. Share: npm 6601/8717, pnpm 4476/5431, yarn 5726/7951, ripgrep 96/973, find-walk 117/478, copy-tree 3395/9014, rm-rf 2664/3293, watch 3/11. Own disk: npm 5065/7063, pnpm 1384/2033, yarn 4502/5098, ripgrep 99/102, find-walk 124/124, copy-tree 1190/1072, rm-rf 501/475 | share ahead on all eight; own disk ahead on four, level on one, copy-tree 11% and rm-rf 5% behind | standing |
| 2026-09-03 | share | every operation on a name under a parked directory goes through the nearest resident ancestor and a relative path (`Inode::at` / `at_dir`, 20 call sites), `still_is` included | M1 install run: revivals 260k → 86k — and pnpm FAILED, with thousands of queued clones and mkdirs reporting ENOENT: the path is the guest's view of where a directory is, and a rename the guest was told is done can still be queued while a create keyed on the directory alone runs in its own lane. Reproduced on the M5 at `LIGHTER_FS_FD_BUDGET=6144` | fixed below |
| 2026-09-03 | share | `at_dir` checks its path resolves to this inode (one fstatat) and opens by identity when it does not; `still_is` falls through to identity on a by-name miss rather than retiring the identity | M5 at the stock budget: pnpm 3948 (unlimited budget: 3900–4100), no failures. A second bug found by the unit test: the create job recorded a file's birth time BEFORE applying the promised mtime, and APFS moves birth time back to an earlier mtime, so every by-name identity check on a created file missed and revived it | kept |
| 2026-09-03 | share | the sweep promotes only directories into headroom | M5 stock budget, traced: revivals 10.4k → 3.6k for a pnpm install, 4145 of the remaining 4160 directories (2.3k the sweep's own promotions, 1.9k the path check failing on a directory whose rename is queued); pnpm 4290 → 4348 (noise) | kept |
| 2026-09-03 | share | an `ENOENT` through a relative path is believed only if the prefix still resolves to the directory afterwards, else the directory is opened by identity and the operation retried (`Inode::under`) | M1: no failed jobs; npm 11.6–12.3, pnpm 6.3–6.7, yarn 11.8–12.7, rm-rf 2.6–3.5 (OrbStack 11.0 / 5.9 / 10.1 / 3.9), revivals 16k across the three installs, from 936k at the start of the night. M5 stock budget: pnpm 4237, 2.9k revivals, all directories. Gates m2/m3/m4 pass | kept |
| 2026-09-03 | own disk | M5 copy-tree and rm-rf alone on a fresh disk, both runtimes | copy-tree 1284–1722 / 1075–1217, rm-rf 511–545 / 460–513: the gap is real, not state left by the installs. Guest counters: a `cp -a` of 928 MB writes 2.1M sectors on ours during the copy and 100–195k on OrbStack's — the flusher runs inside our window. Same dirty ratios; the loop with the suite's 16 GiB, 64 GiB disk and 200 µs poll was at parity (1100–1310 / 1100–1270) | open |
| 2026-09-03 | guest kernel | syscall and small-file microbenchmark inside both guests, same container | `perf bench syscall basic` 0.108 / 0.097 µs, tmpfs create 50k 166 / 150 ms, tmpfs rm 80 / 68, disk create 384 / 365, disk rm 296 / 276: a consistent 7–10% per operation in the kernel itself. OrbStack's `/proc/config.gz` is a 34-line stub, so no config diff; ours carries PREEMPT, PTR_AUTH_KERNEL, INIT_STACK_ALL_ZERO, IRQ_TIME_ACCOUNTING, AUDIT, SVE/SME/MTE from the arm64 defconfig | building without them |
| 2026-09-03 | share | M1 yarn share, guest perf inside the install: ours 75% idle, OrbStack's 44% in `virtio_fs_request_complete` (its guest spins on completion; ours sleeps and is woken through `vm_notify`); wall 11.06 / 10.68 this run | the host server's latency is the M1 share bound |
| 2026-09-03 | own disk | guest meminfo sampled at 4 Hz through a 928 MB `cp -a` on the M5 | Dirty climbs to 950 MB with Writeback at 0 throughout, and drains only after the copy in the `sync` that follows — no flusher inside the window after all; the sectors counted earlier were the sync's. The copy is page-cache reads, page-cache writes and btrfs metadata: guest CPU, where the kernel's per-operation cost is | points at the kernel config |
| 2026-09-03 | guest kernel | rebuilt without PREEMPT (voluntary), PTR_AUTH_KERNEL, INIT_STACK_ALL_ZERO, IRQ_TIME_ACCOUNTING, AUDIT, SVE/SME/MTE; BPF_JIT_ALWAYS_ON | M5, same container, before → after (OrbStack): syscall 0.107 → 0.093 µs (0.097), tmpfs create 50k 158 → 115 ms (150), tmpfs rm 69 → 53 (68), disk create 368 → 273 (365), disk rm 294 → 210 (276), own-disk `cp -a` 928 MB 1110–1240 → 800–940 ms (1100–1270) | kept; gates and the M1 suite running |
| 2026-09-03 | suite | M5 final on the new kernel, medians ours/OrbStack. Share: npm 6315/9093, pnpm 4725/5538, yarn 5383/8384, ripgrep 84/1047, find-walk 96/519, copy-tree 3312/9404, rm-rf 2585/3382, watch 3/11. Own disk: npm 4777/7068, pnpm 1210/2040, yarn 4267/5266, ripgrep 91/112, find-walk 97/128, copy-tree 955/1150, rm-rf 386/479 | ahead on every case, both sides | done |
| 2026-09-03 | suite | M1 final on the new kernel, medians ours/OrbStack. Share: npm 12475/11175, pnpm 7768/5950, yarn 12155/10029, ripgrep 159/1131, find-walk 122/522, copy-tree 5551/15129, rm-rf 2922/3914, watch 3/2. Own disk: npm 7599/9756, pnpm 1745/2442, yarn 7928/7850, ripgrep 123/149, find-walk 127/146, copy-tree 3034/2441 (1696–4634), rm-rf 651/700 | the three share installs slipped 12–30% behind where the isolated runs an hour earlier were at parity (pnpm 6.3–6.7); voluntary preemption on an oversubscribed 8-core box is the suspect | A/B running |
| 2026-09-03 | share | M1 kernel A/B on the share installs, previous image vs new, same VM settings as the suite | previous: pnpm 6302 5752, yarn 10567 10900; new: pnpm 6279 5812, yarn 11488 10444 — identical, and both at parity with OrbStack (5950 / 10029). The suite's 7768 / 12155 are not the kernel: they come from running behind npm's three reps in one VM | chasing what accumulates |
| 2026-09-03 | share | pnpm ×3, npm ×3, pnpm ×3 in one VM, both machines | M5 (16 GiB guest): pnpm 3663–4840 before, 3741–4685 after — nothing accumulates. M1 (4 GiB guest): 5501–6363 before, 6751–6976 after, +13%; revivals only 7.6k for the run, registry 100–180k live inodes. Either the guest's 4 GiB is under page-cache pressure after three npm trees or the server's registry at that size is what costs | 6 GiB run to split them |
| 2026-09-03 | share | M1 host memory sampled through pnpm ×3, npm ×3, pnpm ×3 (4 GiB guest on the 8 GB Mac) | VMM resident 4.2–5.3 GB, host free ~60 MB throughout, compressor 600 MB → 1.7 GB during npm, ~80 MB paged out; no "host memory pressure changed" line in the boot log, so macOS never reported Warn and the balloon never inflated. pnpm 6.6–7.7 before and 6.7–7.7 after this time: the M1 numbers swing with the host's memory state more than with anything in the server | comparing OrbStack's footprint |
| 2026-09-03 | share | M1 host memory through the same sequence, ours then OrbStack, with the kernel's pressure level sampled | ours: resident 4.2–6.0 GB, compressor 1.1–1.9 GB, pnpm 6.5–6.6 before npm and 7.4–8.0 after (+15%); OrbStack: resident falls to ~2 GB within and between installs, compressor flat at 580 MB, pnpm 5.9–6.3 both times. The level read 1 (normal) throughout on both — macOS compresses first and reports later, so a balloon driven by the level alone never moves on an 8 GB Mac | steer by host headroom |
| 2026-09-03 | memory | balloon steered by host headroom (free + inactive + speculative + purgeable below an eighth of RAM) | never fired on the M1: macOS keeps the guest's own anonymous pages on its inactive list, so that sum read 2–3 GB while the compressor grew 630 MB → 2 GB through an npm install. `vm_stat` sampled: free 57–360 MB, inactive 1.5–2.5 GB, file-backed 1.1–1.4 GB, anonymous 3.1–3.9 GB | replaced by the compressor signal |
| 2026-09-03 | memory | balloon steered by the host's compression rate (a 256 MiB step per second while compressing) | M1: it fires — compressor 900 MB instead of 2 GB — but the host's resident size did not move and pnpm after npm took 12.4 / 8.1 / 6.2 s. The driver hands back scattered 4 KiB pages; only aligned runs of four are a host page, so the guest lost its cache and the host got nothing back | replaced: the guest reclaims from its containers' cgroup on request |
| 2026-09-03 | memory | while the host compresses, the guest reclaims from its containers' cgroup on request (`memory.reclaim`, twice the compression, 256 MiB–1 GiB, once a second) | M1: works mechanically — resident 4.7 → 3.2–3.7 GB, compressor 1.0–1.2 GB — but pnpm after npm took 12.4 / 12.2 / 10.9 s: the requests kept landing during the install and evicted the cache it was using. OrbStack's steady 2 GB is a standing bound, not a kick | replaced by `memory.high` = half of guest RAM on the containers' cgroup, set by the agent at boot; the reclaim stays as a backstop |
| 2026-09-03 | memory | the agent bounds the containers' cgroup to half of guest RAM (`memory.high`) at boot; the compression-driven reclaim kept as a backstop at 256 MiB/s | M1, pnpm ×3 / npm ×3 / pnpm ×3: 5617 5582 5799 / 11098 11830 10701 / 6490 5833 5884 (OrbStack 5.9–6.4 / 10.8–11.2 / 5.9–6.4); compressor 640 MB → 1.0 GB at most, from 2 GB. M5 (8 GiB cap on a 16 GiB guest): npm 6117 6245, pnpm 4535 3797, unchanged | kept |
| 2026-09-03 | daily driver | the private rootfs refresh compared the master's mtime with the copy's | the guest writes to its root disk, so a running machine's copy is always newer and was never refreshed: the daily driver had run a rootfs three builds old while every bench VM booted the current one (its `reclaim` answered "error unknown", `memory.high` read `max`). Now stamped with the master it was cloned from. The agent also keeps watching for the containers' cgroup rather than giving up after two minutes, since dockerd makes it at the first container | fixed |
| 2026-09-03 | memory | M1 final with the half-of-RAM bound | share: npm 10934 11406 11944, pnpm 12652 6801 6621, yarn 14071 13009 13867, copy-tree 4142–6095, rm-rf 2430–3415; own disk: yarn 7784–8067, copy-tree 5465–6823 (was 1696–4634), rm-rf 584–614. The bound throttles the big working sets: yarn copying its cache into a tree and a whole-tree copy want more than 2 GiB of cache at once. OrbStack's trace: 4.3 GB during npm, 2 GB fifteen seconds after | bound raised to 3/4, plus an idle trim to 1/4 |
| 2026-09-03 | memory | agent: `memory.high` = 3/4 RAM, and after 3 s of guest idle reclaim the containers' cache down to 1/4 RAM (10 s cooldown) | M5 4 GiB loop VM: cache 2.34 GB after a copy, 0.97 GB eight seconds later, bound 3.08 GB | measuring on the M1 |
| 2026-09-03 | memory | M1 with the 3/4 bound and a trim keyed on guest CPU idle > 90% for 3 s | share: pnpm 11841 10885 6314, npm 11656 13918 10692, pnpm 12257 7405 6534, yarn 14798 11802 11351 — worse than anything before it. A guest installing against the share is three quarters idle waiting on the host, so the trim fired inside every install and evicted the cache it was using | idle is now the containers' own CPU from their cgroup (< 50 ms/s for 3 s); M5 4 GiB loop: 1.27 GB held under a busy reader, 1.01 GB nine seconds after it stopped |
| 2026-09-03 | memory | M1 with the 3/4 bound and a trim after 3 s of container idle | share: pnpm 12503 9378 6515, npm 11722 11338 11767, pnpm 14910 10372 7084, yarn 16450 11731 11285; own disk yarn 7818–7938, copy-tree 5132 6232 1867. The first of three repetitions is the slow one every time: the pause between cases is enough for a three-second trim, so each case starts cold and reads its gigabytes back through the share | trim after 30 s of container idle instead |
| 2026-09-03 | memory | M1 with the 3/4 `memory.high` and a 30 s trim | share: pnpm 12182 7457 7084, npm 12721 12151 10915, pnpm 7422 7211 7308, yarn 15403 10123 9551; own disk copy-tree 7872 3725 2311. The first repetition is still the slow one, and the half-of-RAM suite had the same shape while the uncapped runs never did: `memory.high` throttles the allocating tasks with sleeps once the cgroup sits at its line, so whichever install starts with the cache full from the one before pays it | bound kept by the agent reclaiming above 3/4 every second instead; no `memory.high` |
| 2026-09-03 | memory | M1 with the bound kept by reclaim (no `memory.high`), then the same sequence again with the host sampled | reclaim build: pnpm 11894 6380 6414, npm 11281 12591 12287, pnpm 10766 6505 6269, yarn 14185 11904 9708; own disk yarn 7758–8076, copy-tree 3700 2842 1857. Again, nothing new written: pnpm 12133 7858 6434, yarn 12648 9382 10319, and no host process above 20% CPU. The first repetition is slow whatever the agent does; every slow-first run followed a 2 GB rootfs landing on the M1, which evicts the Mac's file cache, and the one good run had it warm | rerunning with the host cache left alone |
| 2026-09-03 | memory | M1 again with nothing written to the host since the last run | pnpm 9368 6087 6279, yarn 14427 12989 14264: not the host cache. The first repetition is slow under every form of the bound and yarn under all of them, and the one good sequence (10:50: 5617 5582 5799 / npm / 6490 5833 5884) ran with an agent whose two-minute window had closed before the suite's first container — no bound at all, only the compression backstop | the bound is dropped; the 30 s idle trim stays |
| 2026-09-03 | share | M1 without any cache bound (the 30 s idle trim only) | pnpm 13099 11413 7302, npm 11738 11333 11205, pnpm 7830 6915 6651, yarn 15695 16040 10580: as bad as with the bound, so the bound was never the cause of the slow first repetitions and the slow yarn. Host: 78 GB free, no thermal warning, OrbStack's VM not running; 35 leftover run directories from interrupted runs (66 GB) removed. The last run's boot log shows the host-side backstop asking the guest for a gigabyte mid-run after the host compressed 2 GB in a second | A/B of the backstop on and off |
| 2026-09-03 | memory | A/B of the host-side backstop on the M1 share, pnpm then yarn ×3, `LIGHTER_MEMORY_BACKSTOP=0` vs 1 | off: pnpm 7391 8433 7775, yarn 10594 11377 9539, no requests; on: pnpm 12653 10579 7687, yarn 15160 10891 10015, eight requests. The backstop is the slow first repetition | removed; the host-side policy is the pressure-level balloon only, the agent keeps the 30 s idle trim |
| 2026-09-03 | suite | M5 final on the shipped build (f57a117), run BESIDE the daily driver and Nick's work (`--allow-noisy`), medians ours/OrbStack. Share: npm 7086/9416, pnpm 4840/5703, yarn 6463/8615, ripgrep 97/1066, find-walk 96/483, copy-tree 4233/9794, rm-rf 2685/3549, watch 3/2. Own disk: npm 4952/7388, pnpm 1227/2197, yarn 4184/5333, ripgrep 82/105, find-walk 99/138, copy-tree 1089/1585, rm-rf 415/556 | ahead on every case even noisy; the clean run of the morning stays as the record in `results/` | done |
| 2026-09-03 | suite | M1 final on the shipped build (f57a117), medians ours/OrbStack. Share: npm 12895/11240, pnpm 6592/6034, yarn 11109/10237, ripgrep 140/1147, find-walk 126/520, copy-tree 5042/14034, rm-rf 2683/3886, watch 2/3. Own disk: npm 7600/10102, pnpm 1744/2390, yarn 7881/7843, ripgrep 125/151, find-walk 128/130, copy-tree 2799/2378 (1691–3217), rm-rf 596/745 | ahead on eleven of fifteen; the three share installs 8–15% behind and own-disk copy-tree within its noise — the 8 GB host compressing a 4 GiB guest, which every cure measured worse than | the record |
| 2026-09-03 | memory | the VMM's own footprint at the peak of an npm install, `vmmap` on both machines | malloc heap 117–140 MB; guest RAM is the only shared memory (130 regions of 128 MB, the mapping split by the pages handed back); M1 footprint 4.6 GB against a 4 GiB guest. Nothing on the host side of the boundary to win. What is left for the M1 share installs is that share data is cached twice, in the guest's page cache and in the Mac's, and a 4 GiB guest on an 8 GB Mac pays for both; not caching it in the guest would cost ripgrep its 84 ms (1935 cold) | closed; the residual is understood |
| 2026-09-03 | memory | patch 0014: virtio-balloon inflates in order-2 compound pages (16 KiB, host-page units) with balloon compaction off; the host policy steers the balloon target off the compressor, +64 MiB/s while it compresses ≥ 32 MiB/s, −32 MiB/s when it has stopped and free+speculative+purgeable is above a sixteenth of RAM, capped at half of guest RAM, `LIGHTER_MEMORY_STEER=0` to A/B | the design that lets the guest's own LRU choose what to give up, gradually, which every host-driven reclaim did not | building |
| 2026-09-03 | memory | patch 0014 in a 4 GiB loop VM on the M5 with a fixed 512 MiB balloon target (`LIGHTER_BALLOON_TEST_MIB`) | every 1 MiB batch the guest inflates releases 1024 KiB on the host — the whole of it — where the unpatched driver's scattered 4 KiB pages released next to nothing. Gates m2/m3/m4 and test-hv pass on the kernel | kept |
| 2026-09-03 | memory | M1 A/B of the balloon steering on the 0014 kernel (`LIGHTER_MEMORY_STEER`), +64 MiB/s at ≥ 32 MiB/s compressed, cap half of RAM | off: pnpm 7416 7530 7084, npm 11768 14032 11814, pnpm 6893 7265 6198, yarn 12161 11114 11056, compressor to 2.1 GB. On: pnpm 6705 6168 6348, npm 13450 11260 11265, pnpm 9939 6397 6722, yarn 12933 13231 9673, target to 800 MiB, compressor 0.9–1.8 GB. The first pnpm gains 12%, yarn loses about as much with the balloon at 800 MiB through its phase | gentler profile: 32 MiB steps at ≥ 64 MiB/s, cap an eighth of RAM |
| 2026-09-03 | memory | the gentler steering on the M1, same kernel, same session as the off run above | pnpm 7174 6455 6163, npm 12449 11385 10934, pnpm 8304 6102 6110, yarn 13860 10641 10296; the balloon never past 256 MiB. Medians against off: pnpm 6455/7416, npm 11385/11768, pnpm 6110/6893, yarn 10641/11114 — a small win on all four, the first repetitions still paying a little | shipped on by default, `LIGHTER_MEMORY_STEER=0` to compare |
| 2026-09-03 | suite | M5 on the balloon build, beside the daily driver and a 75 GB Docker cache prune that ran in the middle of it | share npm 7622–8207, pnpm 4637–4984, yarn 6409–8550, copy-tree 4373–5500; own disk copy-tree 1790–6021, rm-rf 603–1299 — the outliers land on the prune. Not a record; the steering is inert on a 48 GB host (the compressor never moves), so the clean run of the morning stands | CSVs restored |
| 2026-09-03 | suite | M1 final on the balloon build (e3167d1), medians ours/OrbStack. Share: npm 11333/11072, pnpm 7126/6149, yarn 10877/10379, ripgrep 152/1146, find-walk 124/519, copy-tree 5271/20210, rm-rf 2523/3869, watch 2/2. Own disk: npm 8015/9800, pnpm 1727/2428, yarn 7918/7908, ripgrep 132/155, find-walk 124/130, copy-tree 3235/3870, rm-rf 584/722 | npm now level (was 15% behind), yarn 5% behind (was 8%), pnpm 16% behind on a 12.8 s first repetition with the other two at 6.4 and 7.1; own disk ahead or level on all seven | the record |
| 2026-09-03 | memory | deflate keyed on a quiet compressor (five quiet seconds, 32 MiB/s down) instead of free host memory above a floor, which an 8 GB Mac rarely shows | M1: pnpm 6710 6521 7299, npm 12630 12064 11692, pnpm 6889 6736 6585, yarn 11407 10674 10842; balloon to 256 MiB, nine moves. The second pnpm's first repetition no longer pays (8304 → 6889); the rest within the M1's noise of the free-floor run. Gates pass | kept |
| 2026-09-03 | suite | M1 final on the quiet-deflate build (648a64e), medians ours/OrbStack. Share: npm 11661/11093, pnpm 6025/6061, yarn 12193/10233, ripgrep 151/1121, find-walk 122/518, copy-tree 4957/16567, rm-rf 3527/4025, watch 2/2. Own disk: npm 7869/10210, pnpm 1763/2375, yarn 8018/7816, ripgrep 129/184, find-walk 125/130, copy-tree 2477/3622, rm-rf 590/695 | pnpm's slow first repetition is gone and its median is level; npm is 5% behind, yarn 19% behind with a 9.4 s third repetition; the share's four non-install filesystem cases are ahead and own disk is ahead or level on all seven | the record |
| 2026-09-03 | correctness | CI flake: `a_forgotten_directory_with_promises_is_found_again_as_itself`, once in ~40 rounds on a fast runner | APFS gives a removed-and-remade directory its old inode number back while the registry still holds the old inode for its queued promises; the new inode's identity claim was refused on the corpse's account, the map kept pointing at it, and the next lookup retired it and minted a third inode. A stale occupant now gives up the slot at claim time. Reproduced in 2 runs locally; 15 clean after | fixed |
| 2026-09-03 | correctness | the same test failed again in CI at round 5 with the identity fix in | the other way it sees a fresh inode is right: once the last queued promise lands the forgotten directory has none left and the registry releases it, and a lookup after that mints a new inode. The test asserted a timing it did not control. The apply queue now takes a hold (`Apply::hold`/`release`, tests only) and the test pins twenty queued subdirectory creates across the forget and the lookup — subdirectories, since an unlink settles the create it undoes, which a held queue never lets land | fixed; 20 clean runs, suite clean |
| 2026-09-03 | release | 0.1.0 built, signed and notarized from main (Apple submission 7e87a8c9), released on the private repo, tap formula pushed and audit-clean; the installer run on the M1 against the real asset | a fresh install could not start: `current_exe` answers with the symlink the installer (and Homebrew) reach the binary through, so "beside the executable" looked beside the link and found no gvproxy. Canonicalized; the installed release then boots, runs a container and stops on the M1. The installer also takes the API asset route when a token is present, since a private repository answers the download URL with 404 | fix on PR #2; the release re-cut from main once it merges |
| 2026-09-03 | release | the installed release could not start on the M1, and put up "lighter is damaged" on a Mac with a screen | `lighter start` ran the machine from a bundle it built on the user's Mac around a Developer ID copy whose Info.plist no signature sealed. Signing the bundle ad hoc cured the "damaged" verdict but Gatekeeper still assesses an ad hoc bundle at first launch and puts its verdict to the user: the M1's screen was locked, nobody saw it, and the machine process sat in `_dyld_start` for the whole 120 s timeout with an empty log. The release now ships the bundle beside the kernel and rootfs, signed with the same Developer ID and notarized with the binaries (`spctl` says accepted), and `lighter start` uses it as it is; a checkout still builds one ad hoc | fresh installs start in 3 s on the M1 and, under an isolated home, on the M5 |
| 2026-09-03 | memory | a `memory` case in the suite: the runtime's processes' physical footprint (Activity Monitor's number) settled, at the peak of an npm install, and 15 s and 60 s after | first reading, M5 beside the daily driver, 4 GiB guest: settled 1261, peak 4097, after 15 s 3798, after 60 s 3794 MiB. The footprint over-counts a Hypervisor.framework guest: a 2 GiB guest that had touched all its RAM read 3.5 GB, a 4 GiB one 6.8 GB — the same accounting for every runtime here, and the number a user sees, so it is what the suite reports | kept |
| 2026-09-03 | memory | the trim at 10 s of container idle, watched on a 4 GiB loop VM after filling its cache | inside the guest it works: Cached 3.1 → 1.3 GB and MemFree 0.17 → 1.9 GB within 15 s. The host got the pages back at 32 MB/s — 644 MB by 15 s, 1.45 GB by 60 s — which is free page reporting's own pace: 32 blocks of 2 MB per pass, two seconds between passes | patch 0015: 256 blocks a pass, a quarter second apart |
| 2026-09-03 | memory | the `memory` case on the M1, 4 GiB guests, lighter and OrbStack on the same metric | lighter (30 s trim): settled 1264, peak 4191, after 15 s 4120, after 60 s 3783; lighter (10 s trim): 1257 / 4042 / 3751 / 3751; OrbStack: 1088 / 4311 / 2150 / 1021 MiB. Not competitive on giving memory back, by 2.7 GB. The 10 s trim shows no first-repetition cliff in the share sequence (pnpm 6451–6913, npm 11803–12726, pnpm 6276–7454, yarn 11251–13122) | kept; the floor chased below |
| 2026-09-03 | memory | free page reporting at 128 KiB blocks (`page_reporting.page_reporting_order=5` on the command line) instead of 2 MiB, M5 loop VM, host-side sampling only | after the same fill and trim: footprint 5221 → 3727 (15 s) → 3113 (30 s) → 3164 (60 s), against 6674 → 5167 → 4359 → 4357 at 2 MiB; the guest held 2.7 GB in use, so the host's number is within 15% of the guest's — the "over-count" was free memory in pieces too small to report | kept; on every command line |
| 2026-09-03 | memory | the guest rests at a sixteenth of RAM after 10 s of container idle, with 128 KiB reporting and patch 0015, M5 loop VM, host-side sampling | after the same fill: footprint 6172 → 4002 (15 s) → 1837 (30 s, and stable), the guest left with 3.2 GB free and 550 MB cached. Against OrbStack on the M1: 2150 at 15 s, 1021 at 60 s. The 15 s reading carries the ten-second wait; three seconds fired between benchmark cases and ten did not | kept |
| 2026-09-03 | memory | the `memory` case on the M1 with patch 0015, 128 KiB reporting and the sixteenth resting cache, 4 GiB guests | lighter: settled 1141, peak 3904, after 15 s 1680, after 60 s 1244 MiB; OrbStack: 1088 / 4311 / 2150 / 1021. From 2.7 GB behind at 60 s this afternoon to 220 MiB, and ahead at 15 s. Colima on the M1 share: npm 23008, yarn 28132, ripgrep 15019, find-walk 3813, copy-tree 60404, rm-rf 12405; pnpm failed in the container; own disk npm 10737, pnpm 1582, yarn 11233, ripgrep 188, copy-tree 2560, rm-rf 726 | competitive |
| 2026-09-03 | memory | the share sequence on the M1 with patch 0015, 128 KiB reporting and the sixteenth trim together | pnpm 27382 23946 23649, npm 19438 19161 18851, pnpm 26308 23522 23629, yarn 24212 23772 19498 — three to four times slower than the sequence an hour earlier. The container's CPU is the same seven seconds spread over 27 s: it waits. Reporting size cleared by A/B (order 9: 26512 23553; order 5: 26406 23651) | two-by-two of kernel and rootfs running |
| 2026-09-03 | memory | compaction after the trim (`/proc/sys/vm/compact_memory`) with reporting left at 2 MiB, M5 loop VM | the same fill settles at 2764 MiB by 30 s (was 4357 without it; 1837 with 128 KiB reporting); at 512 KiB reporting 1823 | kept: what the trim frees coalesces into runs the host can take |
| 2026-09-03 | memory | two-by-two on the M1 share pnpm: kernel with and without patch 0015, rootfs at a quarter and a sixteenth | without 0015: 5513 5556 (quarter), 5985 6571 (sixteenth); with 0015: 26548 23633, 26437 23635. The patch is the regression, whichever rootfs: at eight times the rate the guest reports its free memory as fast as an install churns it, and every reported page it reuses is a fault on the host. Upstream's throttle was the right rate for a busy guest; what the idle guest frees now coalesces by compaction and goes back at that rate | 0015 dropped; reporting size back to the default |
| 2026-09-03 | memory | the final combination on the M1: the 0014 kernel, upstream reporting, the sixteenth trim with compaction | share sequence back: pnpm 6192 7152 6612, npm 13657 11930 12914, pnpm 6419 6178 5800, yarn 12773 10679 10124. `memory`: settled 1254, peak 4047, after 15 s 2126, after 60 s 1456 MiB; OrbStack 1088 / 4311 / 2150 / 1021. Level at 15 s, 435 MiB behind at 60 s, at no install cost. Gates pass | the record |
| 2026-09-04 | network | baseline on `ef81365`: iperf3 on four paths through a throwaway machine (`scripts/net-iperf.sh`) | egress 1.69 / reverse 2.96 / published port 1.47 / reverse 2.31 Gbit/s on the M5 the night before; tonight's run under a 180 s cap produced no number at all — the first transfer wedged the guest (RCU stall on CPU 0, never answers its NMI) and the Docker socket with it | the red baseline; the cause is `transmit` doing a blocking `write_all` to gvproxy's socket on the vCPU thread under the transport lock |
| 2026-09-04 | network | transmit off the vCPU: frames copied into a bounded outbox (8 MiB) on the kick, a `net-tx` thread drains it to the socket with one `writev` per batch; a full outbox leaves chains on the ring and the drain thread has the queue looked at again. Receive batched: one wake per burst (FIONREAD) instead of one per frame | egress 3.90 / reverse 3.10 / port 3.40 / reverse 3.83 Gbit/s; no stall; `docker ps` 129 ms after load | kept: the hang is gone and every path doubled; the number is now the frame rate (1500-byte frames, a trap per kick) |
| 2026-09-04 | network | link MTU 65520 (`LIGHTER_NET_MTU`, gvproxy `--mtu` and the device's `F_MTU` config together) | guest eth0 reads 65520 but every path unchanged (3.0–4.0 Gbit/s), and the script's new frame-size readout says why: `tx_avg=1513`, `rx_avg=1431` — Docker's bridge stays at 1500, so every container segments at 1460 whatever the link carries, and gvproxy's segments toward a published port follow the container's MSS | kept as a switch; dockerd now takes `--mtu` from eth0 in the rootfs init so the bridge follows the link (rebuild pending). Lesson: measure frame size, not MTU |
| 2026-09-04 | network | the transmit ring watched (no trap per frame) and the receiver reading a megabyte per `read` | at 1500: egress 3.61 / reverse 3.02 / port 2.97 / reverse 3.41 — no change from the outbox alone; the frame count was not the bound | kept (they cost nothing and the watcher is the block device's pattern); the bound is per byte, not per frame |
| 2026-09-04 | network | 64 KiB frames end to end (`--net=host` in the guest, link MTU 65520): `tx_avg=65495` | egress 6.15 Gbit/s at twelve thousand frames a second, reverse 4.20 at 32 KiB frames; the bridged paths unchanged (containers still at 1460) | forty times fewer frames bought 1.6×, so the cost is per byte |
| 2026-09-04 | network | who burns the CPU at 6.3 Gbit/s: `ps` during a 15 s big-frame egress | gvproxy 149% of a core; the VMM's two busiest threads 96% and 81%, nearly all system time; the iperf3 server 14% | the sidecar is the ceiling of this path, as the plan allowed: phase 1 stops here with the stall fixed, big frames on by default (`DEFAULT_MTU` 65520, dockerd `--mtu` from eth0), and the rest is phase 3's |
| 2026-09-04 | network | the default configuration on the rebuilt rootfs (link 65520, bridge following it), four paths then a 300 s soak alternating all four | egress 6.72 / reverse 3.98 / port 4.09 / reverse 6.44 Gbit/s, frames 65 KiB out and 32 KiB in; through the soak 6.4–6.6 and 3.9 steady, no stall, `docker ps` 130 ms | phase 1 closed: the hang is fixed and the sidecar path is 1.7–4× where it started; the exit target (Docker Desktop's 15–20) is gvproxy's to give and it does not, so the rest is the stream backend |
| 2026-09-04 | suite | network and power cases in `benchmarks/run.sh`: four iperf3 paths, UDP, connect rate, kept-alive GET median and p99, DNS from a container; `power-idle` = a quiet minute then a minute of powermetrics (CPU ms/s, wakeups/s, package-idle wakeups/s; `top` where `sudo -n powermetrics` is not allowed). `runtime_pids` is shared by the memory and power cases and now counts gvproxy for lighter | — | the M1 memory record excludes gvproxy's ~37 MiB; it is re-run in phase 4 with the shared list |
| 2026-09-04 | suite | first run of the network and power cases, OrbStack on the M5, one repetition (`--label` next time: an env `LABEL` is overridden by the script's own default and the trial overwrote `orbstack.csv`, restored from git) | TCP egress 94072 / reverse 53384 Mbit/s, published port 49812 / 92372, UDP 2880, 14114 connects/s, GET median 88 µs, DNS 345 µs; idle: 3 ms/s of CPU, 114 wakeups/s, 1 package-idle wakeup/s | the harness works end to end; these are the numbers phase 3 is measured against |
| 2026-09-04 | suite | the native column on the M5: the Mac over loopback | TCP 112613 / 128697 Mbit/s, UDP 21018, 26356 connects/s, GET median 40 µs; DNS 7710 µs — node's resolver asks the network every time where a runtime's forwarder answers from its cache, so that row compares caches, and the report should say so | the ceiling every published port is read against |
| 2026-09-04 | vsock | the raw ceiling of a stream over the vsock device as it stands (`scripts/vsock-ceiling.sh`: the agent's `blast`/`sink` verbs on the control channel, 256 MiB each way, host unix socket → proxy → device → guest) | guest→host 7.39 Gbit/s, host→guest 6.45 | under the 40 the plan set as the line, so phase 3 begins with the device: it was written for the Docker socket — 4 KiB guest receive buffers, a header and a copy per 4 KiB, a byte-at-a-time deque on the guest→host side, a 50 ms sleep when credit runs out |
| 2026-09-04 | rootfs | `guest/rootfs/build.sh` reused a stale `guest/out/lighter-agent` (built only when missing): the agent's new verbs answered "error unknown" from a freshly built image, and the M1's agent binary has no compaction call, so the M1 memory record (1254/4047/2126/1456) was taken without it | — | fixed: the agent is rebuilt when any of its source is newer; the M1 memory record is re-run in phase 4 |
| 2026-09-04 | vsock | where the ceiling's time goes: `ps -M` on the VMM through an 8 GiB blast and sink | guest→host: one thread at 100%, 4.5 s system to 0.15 user — the vCPU pushing each packet's payload into a byte deque one `push_back` at a time under both locks; host→guest: the vCPU thread at 99% with 13 s system, the guest driver taking sixteen 4 KiB packets per 64 KiB, and the host `send` loop at 77% copying each into a packet and again into the ring | two fixes: payloads moved whole through a deque of chunks and written vectored; a guest patch (0015) posting 64 KiB receive buffers |
| 2026-09-04 | vsock | payloads moved whole through the outbound queue and written vectored; the receive fill writes header and payload straight into the guest's buffers | on the 0014 kernel: guest→host 7.39 → 12.67 Gbit/s, host→guest 6.45 → 7.02 (a run taken while the kernel was building read 1.47 / 0.66 and is not a number: no measurements during builds) | kept |
| 2026-09-04 | vsock | guest patch 0015: 64 KiB receive buffers (`VIRTIO_VSOCK_DEFAULT_RX_BUF_SIZE`) instead of 4 KiB | host→guest 7.02 → 8.16 Gbit/s, guest→host unchanged at 12.69 | kept, but the bound on that side is elsewhere: per packet the host wakes the transport and the guest takes an interrupt, and the guest allocates an order-4 buffer for each |
| 2026-09-04 | vsock | the host side batched (packets for a whole read under one wake, a megabyte per socket read), the transmit ring watched, the payload copied once out of the guest, and the advertised credit window raised from 256 KiB to 4 MiB — each measured on its own | guest→host 11.5–13.1 Gbit/s across all four, host→guest 5.7–8.2; none moved the number beyond the noise (about ±15% run to run) | the host is not the bound on either side, and neither is credit: it is the guest driver's cost per 64 KiB packet, with the vCPU reading as busy while it waits because it polls before idling. Next: bigger packets (256 KiB) to see whether the cost is per packet or per byte |
| 2026-09-04 | vsock | why every host-side change left the ceiling where it was: the guest is 99% idle through a blast (19 system ticks in 2745), one packet per drain batch, an interrupt per two packets, and 256 KiB packets (patch 0015 amended) cost four times what 64 KiB ones did — per byte, not per packet. The kernel says why: `virtio_transport_tx_buf_size` caps a socket's transmit window at the *minimum* of the peer's advertised buffer and its own, and its own defaults to 256 KiB, so one packet is in flight per round trip whatever we advertise | 256 KiB per ~180 µs round trip = 11 Gbit/s, which is the number | the fix is the guest socket's own window: `SO_VM_SOCKETS_BUFFER_SIZE` set to 4 MiB by the agent on every stream and control socket (`vsock::set_buffer`), rootfs rebuild pending |
| 2026-09-04 | vsock | the guest window raised to 4 MiB (agent `set_buffer`): interrupts fell fivefold (6465 for 16384 packets), the guest still idle, throughput unchanged at ~10 Gbit/s — so the serial hop was the host's. A python socketpair on this Mac: 13.2 Gbit/s at the default buffers, 227 at 1 MiB, 267 at 4 MiB | macOS gives a unix stream socket 8 KiB each way (`net.local.stream.sendspace`), and every byte between the device and its reader crossed the kernel eight kilobytes at a time; 13 Gbit/s was the number we had been measuring all along | `sockbuf::widen` on every unix socket the VMM owns an end of: the proxy's accepted clients, the stream pairs, the gvproxy link; the ceiling script's own socket too |
| 2026-09-04 | vsock | the ceiling with every unix socket widened to 4 MiB | guest→host 167.7 Gbit/s, host→guest 81.3 (from 11 / 6); and the gvproxy path, whose link is a unix socket too: egress 24.7 / published-port reverse 27.4 (from 6.6), the two paths into the guest unchanged at 4 | the device is not the bound of anything now; phase 3's step 1 is answered and the backend is measured next |
| 2026-09-04 | streams | first boot with `LIGHTER_STREAMS=1`: the agent's proxy is up, the host listener answers, and `INIT streams=failed` — the guest kernel has no `NFT_FIB`, so `fib daddr oif "eth0"` cannot be evaluated | — | `CONFIG_NFT_FIB{,_INET,_IPV4,_IPV6}=y` in `lighter.config`, kernel rebuilding |
| 2026-09-04 | streams | first traffic through the stream path, with an interim address rule installed at runtime (the fib kernel was still building, on the same Mac): curl to example.com HTTP 200 in 48 ms; iperf3 container→Mac 18.3 Gbit/s, Mac→container 13.9; eth0 moved 112 bytes | against 24.7 / 4.1 on the gvproxy path in a quiet run: the direction into the guest is 3.4× already, the other is noise-bound until the machine is quiet | works; the published-port direction is next, then the fib rule, then the numbers on a quiet machine |
| 2026-09-04 | streams | published ports through a stream (the Mac listens per port, each connection a vsock stream naming the port, the agent connects to Docker's published address), on a quiet machine | Mac → published port 57.9 Gbit/s, container → Mac on it 51.7; gvproxy's forward on the same paths 4.1 / 26.8; OrbStack 47.1 / 88.5 | ahead of OrbStack into the container already; the ceiling behind it reads 172.8 / 101.1 |
| 2026-09-04 | streams | the outbound redirect never engaged at boot (`INIT streams=failed`) with the fib kernel up and the rule working by hand | busybox's shell does not take a heredoc on an `if` line as bash does | the ruleset goes through a file; nft's error goes to the console |
| 2026-09-04 | streams | the output hook matches `oifname "eth0"` (fib with an output interface is refused there, and the file applies whole or not at all); all four paths through streams on a quiet M5, the network device carrying nothing | container→Mac 49.0 Gbit/s, Mac→container 66.8, Mac→published port 62.1, container out of it 51.5; the night began at 1.7 / 3.0 / 1.5 / 2.3, gvproxy widened reads 27.0 / 4.0 / 4.1 / 26.8, OrbStack 90.5 / 44.5 / 47.1 / 88.5 | ahead of OrbStack into the container on both paths, behind out of it on both; gates and semantics next, then the default |
| 2026-09-04 | streams | `scripts/gates/m3-streams.sh`: TLS, a closed port refused at once, half-close, host.docker.internal to a server on the Mac, a published port answering and closing with its container, a thousand concurrent connections from one container, DNS and ICMP still on the network device, IPv6 when the Mac has it | all pass on the first run but half-close, whose client was busybox nc — which cannot half-close; node does, rerun pending. A refused connect reads as curl 52 (empty reply) rather than 7 (refused): the guest accepts locally and learns the refusal a moment later, as through gvproxy and OrbStack | streams on by default in the CLI and the bench VMM, `LIGHTER_STREAMS=0` keeps the frame path measurable |
| 2026-09-04 | streams | gates with streams on: m3-docker, m4-fs, m3-netload (20 s per path, stall and socket checks), m8-daily (six-service compose stack healthy in 23 s, three services on localhost, exec with stdin held open, a host edit reaching the container, the clock skewed an hour and put right, TLS from a container, a clean stop) | all green | the streams carry a day's stack |
| 2026-09-04 | streams | the half-close check: the path was right all along — a server on the Mac saw EOF after the request, replied, and the container got the reply — and both test clients were wrong: busybox nc cannot half-close, and example.com drops a connection that does | — | the gate half-closes against a server on the Mac |
| 2026-09-04 | suite | OrbStack's network and power baseline on the M5, three repetitions (`--label net-orbstack`) | TCP out 94034 / 95654 / 97698 Mbit/s, in 55039 / 53375 / 55082, into a published port 53705 / 51710 / 55160, out of one 92014 / 88685 / 95188, UDP ~3000, 18428 / 17631 / 18836 connects/s, GET median 75 / 75 / 72 µs, DNS 267 / 269 / 261 µs; idle 3 ms/s of CPU, 112 wakeups/s, 0 package-idle | the numbers to beat |
| 2026-09-04 | suite | the lighter target's network run died at its first case: bash 3.2 reads an empty array as unbound under `set -u`, and the lighter target's Docker arguments are empty | — | a `dk` helper with the `${arr[@]+"${arr[@]}"}` idiom |
| 2026-09-04 | suite | lighter's network and power baseline on the M5 (streams by default, the pump on the TCP socket directly, no splice yet), three repetitions | TCP out 45582 / 44485 / 35340 Mbit/s, in 69007 / 71251 / 69138, into a published port 71233 / 72258 / 71560, out of one 32600 / 47519 / 44821, UDP 0 / 0 / 0, 6202 / 2831 / 6148 connects/s, GET median 123 / 131 / 149 µs, DNS 378 / 380 / 379 µs; idle 5 ms/s of CPU, 113 wakeups/s, 1 package-idle | ahead of OrbStack into the container on both paths (69 v 54, 72 v 53); behind out of it (45 v 96, 45 v 92), on connection setup (6k v 18k), on request latency (130 v 75 µs), on DNS (378 v 265); UDP reads zero, which is a fault to find |
| 2026-09-04 | network | why the suite's UDP case read zero: with the link at 65520 bytes iperf3 sizes its datagrams to the path and sends 65 KB ones, which reach gvproxy and go no further — macOS caps a UDP datagram at `net.inet.udp.maxdgram` (9216); at 1400 bytes 9.2 Gbit/s go out and 1.8 arrive (80% loss unthrottled, as OrbStack's ~3 Gbit/s received shows too) | — | TCP no longer touches the frame path, so the big link MTU serves only UDP, and badly; A/B of 1500 on the streams pending |
| 2026-09-04 | streams | the agent splices the container→host leg through the guest kernel (TCP socket → pipe → vsock `sendmsg`); the link MTU back at 1500 | four paths 48.1 / 65.2 / 62.4 / 48.5 at 65520 and 51.9 / 58.8 / 58.6 / 50.7 at 1500: the splice moved nothing, the MTU moves nothing | splice kept (it costs nothing and removes a copy); 1500 is the default again for UDP's sake |
| 2026-09-04 | streams | profile of the outbound path at 42–47 Gbit/s: the guest under 1.4 cores busy, the VMM at 2.8–5 cores of which the vCPUs' share is the idle poll; the hottest working leaf `writev` on the pump's writer, near a core | the host's writer is the bound, not the guest: a fresh 256 KiB Vec per packet is a faulted, zeroed mapping and an unmap every packet | payload buffers recycled from the writer to the ring reader |
| 2026-09-04 | streams | payload buffers recycled from the writer to the ring reader (a bounded spare list on the shared state) | 62.9 / 88.1 / 87.4 / 52.8 Gbit/s from 48.1 / 65.2 / 62.4 / 48.5 — three paths up by a third or more, the two into the container now twice OrbStack's 44.5 / 47.1 | kept; the host→guest send path still allocates a payload per packet and gets the same treatment |
| 2026-09-04 | streams | the three-second readings were the noise: at six seconds per path two runs read 62.0 / 88.3 / 83.0 / 68.7 and 62.1 / 83.0 / 85.1 / 68.7 Gbit/s | against OrbStack's 96 / 54 / 53 / 92: into the container 1.6× theirs on both paths, out of it 65% and 75% of theirs | the loop script measures at six seconds from here; the outbound writer is the remaining bound |
| 2026-09-04 | streams | the threads that carry a stream's bytes raised to the interactive QoS class (`qos::raise_interactive`, unconditional, unlike the filesystem server's measured-off raise) | 64.1 / 86.2 / 82.9 / 67.1 and 62.8 / 79.4 / 82.6 / 67.9: no change | kept, it costs nothing and rules out an efficiency core; not the lever |
| 2026-09-04 | vsock | credit updates coalesced to a quarter of the window (a packet, a lock and a guest interrupt per batch before) | 65.2 / 80.5 / 85.7 / 57.6 on the second run: within noise | kept; fewer interrupts, not the lever. The arithmetic now: two host copies of every outbound byte, the ring reader's and the writer's, each near a core at 8 GB/s = 62 Gbit/s |
| 2026-09-04 | vsock | the writer sends guest payloads from where they are (`Chunk::Guest`: the chain held, `writev` from its host addresses, the descriptors returned through a completion queue the transport drains on any notify) | 59.2 / 85.4 / 87.3 / 52.6 and 53.9 / 84.1 / 88.5 / 71.6: correct — every gate path still works — and no faster. Two bugs on the way: the header parser refusing a packet handed no payload, and completions drained only on a transmit notify the waiting guest would never send | kept (a copy fewer is a copy fewer); the outbound bound is not the copy count, so it is the per-batch synchronization: a completion wake, an acknowledge and a recycle per batch, each a lock and the wake a guest interrupt |
| 2026-09-04 | vsock | completion wakes deferred to a pile of 32 (to save an interrupt per batch) | outbound 0.14 / 0.12 Gbit/s, the other paths untouched | reverted, and the collapse is the finding: the guest waits on our completions to free its socket's send memory (`sk_sndbuf`, 208 KiB by default, under one packet), so the outbound pipeline has been one packet deep whatever the credit window said — which is the writer "near a core" and 60 Gbit/s. The agent asks for an 8 MiB send buffer (init raises `net.core.wmem_max`) |
| 2026-09-04 | streams | the guest's stream sockets with an 8 MiB send buffer (init raises `wmem_max` to 16 MiB) | 71.7 / 93.1 / 90.5 / 56.1 and 60.9 / 89.5 / 89.9 / 74.4: the two paths into the container at 90, OrbStack's outbound figure and 1.7× its inbound; outbound up and still spread 56–74 | kept; the outbound bound and its spread are next |
| 2026-09-04 | streams | the Mac's own single-stream TCP send, a plain process to a sink on the LAN address | 123.8 Gbit/s with 256 KiB writes, 149.8 with 1 MiB, 145.1 with 4 MiB | the host socket is not the outbound bound at 65; nor is the copy count; what remains is the guest's own TCP, which terminates the container's stream before the agent carries it — measuring container→container inside the guest to say so |
| 2026-09-04 | streams | every stage of the outbound path measured alone: container→container inside the guest 166 Gbit/s, container→the guest's own address 187, the raw device 157, the Mac's own TCP send 124–150; the agent's splice thread 62% busy (`wait_woken`) and the guest at 1.3 of 9 cores through a 60 Gbit/s transfer | no stage is the bound; the chain waits on itself, each hop handing off in large pieces with a wakeup between | the outbound leg in the agent split across two threads with a 4 MiB pipe between (init raises `fs.pipe-max-size`) |
| 2026-09-04 | streams | the two-thread splice: every path reads zero and iperf3's server refuses the client's parameters as invalid — bytes corrupted or reordered on the split leg — while a page fetch through it is fine | — | bisecting: single-thread splice with the 4 MiB pipe first. Also owed: an integrity check in the streams gate, a checksummed gigabyte each way, since iperf3 checks nothing |
| 2026-09-04 | streams | single-thread splice with the 4 MiB pipe | 73.5 / 88.4 / 88.6 / 64.9 Gbit/s, every path clean | kept; the two-thread split was the corruption (two threads splicing one pipe whose pages the vsock send takes by reference is not a thing to do), and it is gone |
| 2026-09-04 | streams | the streams gate on the zero-copy, 8 MiB-buffer, single-thread-splice build, now with a checksummed 256 MiB each way | every check passes: the bytes arrive intact out of a container and into a published port, plus TLS, refusal, half-close, host.docker.internal, port lifecycle, a thousand connections, DNS, ICMP | — |
| 2026-09-04 | streams | connection setup measured on the control socket alone (connect, ping, close, sequential): 8147/s, median 117 µs, p90 149 | that is the vsock handshake, two host thread spawns (the proxy's and the pump's writer), the agent's accept and its spawn, and a ping round trip, before any TCP; the published-port path adds the Mac's accept, another spawn, and the agent's connect into the container: 6k/s. OrbStack: 18k/s | the establishment wait is a condvar, not a poll; what is left to cut is the spawns (an event loop) and the handshake's hops |
| 2026-09-04 | streams | quarter-megabyte stacks on per-connection threads; the guest's TCP windows raised to 16 MB | 63.2 / 80.8 / 75.4 / 66.0 Gbit/s, within the run-to-run spread; connect+ping+close on the control socket 9103/s, median 103 µs (from 8147 / 117) | kept, small; the next step on connection setup is an event loop in place of two threads per stream, and it is not tonight's |
| 2026-09-04 | record | the M5 record chain starts at 03:50: lighter, OrbStack, Colima and Docker Desktop through every case on the share and their own disks, native, then the report | — | phase 4 |
| 2026-09-04 | streams | a cache of idle threads (`workers::run`): a finished connection thread parks thirty seconds for the next job instead of exiting, so churn reuses threads | first build aborted every machine at boot with a vCPU stack overflow — a helper that notifies waiters had been made to call itself by the same substitution that made everything else call it | fixed; measuring |
| 2026-09-04 | streams | a kqueue reactor (`reactor.rs`): one thread for every stream, non-blocking sockets, the vsock side poking a pipe when credit, bytes, or a connection's state change; backpressure both ways; `LIGHTER_STREAM_THREADS=1` keeps the two-threads-a-stream path measurable | written, not yet built | the lever for connection setup (two spawns and two stacks a connection) and per-request latency (a scheduler hop on every hop of every byte) |
| 2026-09-04 | streams | the reactor against two threads a stream, on a published port: 300 fresh-connection GETs, 2000 kept-alive GETs, 1000 connects | reactor: 0 failures, GET median 89 µs p99 228, 20979 connects/s; threads: 0 failures, 106 µs p99 348, 7262 connects/s; OrbStack 72–75 µs, 18428 connects/s | the reactor is the path: ahead of OrbStack on connection setup, 15 µs behind on a request; the two-thread path stays behind `LIGHTER_STREAM_THREADS=1` |
| 2026-09-04 | streams | the reactor's first throughput run: outbound 0.16 / 0.02 Gbit/s, inbound 46.9 / 62.8; the streams gate (integrity included) passes on it; a profiled transfer sat idle for seconds then ran at 80 | not a missed wake: after a write that could not complete finally did, the reactor took no further chunks until the guest's next packet, and the guest was waiting on that write's credit. Inbound halved on the one thread copying every byte twice | pull again after every completed write; inbound reads go straight into the buffer the packet keeps |
| 2026-09-04 | streams | the reactor after the pickup fix: 53.6 / 70.3 / 92.3 / 76.8 and 66.0 / 71.6 / 91.4 / 56.0 Gbit/s; GET 85 µs, 19439 connects/s | outbound runs; inbound to a container 70 against the two-thread path's 88, because the one reactor thread also does the copy into guest memory when it wakes the transport | delivery moved to a spinning thread |
| 2026-09-04 | streams | delivery on its own thread for everything | inbound 79–81, published port 83–84; GET 117 µs (from 85), 19130 connects/s | a parked thread costs a wakeup on every small request; split by size: a full read is bulk and goes to the thread, a small one is delivered inline |
| 2026-09-04 | streams | delivery split by size: a full 256 KiB read goes to the delivery thread, a smaller one is delivered inline | 77.1 / 78.9 / 83.4 / 67.0 and 74.7 / 80.0 / 83.3 / 77.6 Gbit/s; GET median 86 µs p99 205; 21312 connects/s | the reactor is the path, in every direction better than two threads a stream or level, and ahead of OrbStack on connection setup and both directions into the container; behind on the two out (78% of theirs) and 11 µs on a request |
| 2026-09-04 | suite | the M1's Docker Desktop memory reading: settled 314 MiB, peak 317 — the app's processes only; its VM is Virtualization.framework's XPC service (2.3 GB resident on the M1) and the pid pattern missed it | — | the pattern includes the XPC service; the M1's Docker Desktop memory case is re-run at the end with it |
| 2026-09-04 | dns | DNS carried to the host: first as raw forwarding to the nameserver in resolv.conf — 2842 µs median (Tailscale's MagicDNS resolver, every time, and no `host.docker.internal`); then through `getaddrinfo` with the Docker names answered locally — 483 µs, the gate passing | the Mac's resolver answers in 154 µs; the other 330 was the DNS stream riding the Docker-socket pump, six thread hops each way | DNS on the reactor: cache hits (10 s) and the Docker names answered inline, misses and non-address types off-thread |
| 2026-09-04 | record | the M1's Docker Desktop record (share, own disk) is in; its memory case is re-run at the end with the VM's process counted | — | — |
| 2026-09-04 | dns | DNS on the reactor: a cache hit or a Docker name answered on the reactor thread itself, a miss through the system resolver off-thread | median 52 µs, p90 64 (from 483 on the pump path, 386 through gvproxy; OrbStack 265); the gate passes | five times OrbStack's speed; the Mac's resolver, VPN included, still answers every miss |
| 2026-09-04 | streams | the kernel join (BPF sockmap: an `sk_skb` verdict program on each socket's ingress redirecting to its peer, hand-assembled, nineteen instructions; the guest kernel carries vsock's sockmap support) — first load refused with EINVAL and an empty log; a probe mode in the agent (`--bpf-probe`) loaded a trivial program of every type fine and showed the verifier's words for ours: "unreachable insn 17" | the jump to the pass label was one short | fixed; the sockmap path is measured next |
| 2026-09-04 | streams | the join loads (the jump was one short) and carries nothing: every request hangs, both directions | — | two facts in the kernel: joining hooks the socket's data-ready callback but never runs it, so a request that arrived in full before the join sits until the next packet; and a socket runs verdicts from the moment it enters a map with a program, so a verdict whose peer is not yet in the target map drops. The agent now enters both sockets into a program-less target map before either enters the attached one, and pokes each socket's receive low-water mark after (TCP's `tcp_set_rcvlowat` runs `tcp_data_ready`) |
| 2026-09-04 | streams | the corrected join, with the vsock side's pre-join bytes moved by hand as a stand-in for a kernel kick | kept-alive GET median 72 µs p99 302 (from 86; OrbStack 72–75), 18158 connects/s (from 21312), Mac→published port 87.5 Gbit/s; the container→host direction loses its tail: the integrity check fails, and the half-close and egress iperf3 with it | the failure is the one predicted: a redirected skb is sent by the target's backlog worker, asynchronously, so the agent shutting the target down on the source's FIN races the worker. The stream's end has to ride the redirect — kernel patch 0016: an EOF bit on the last skb (TCP's FIN; a marker vsock queues behind the peer's last data on its shutdown or reset) that the backlog acts on after sending it; vsock's `read_skb` draining its queue like TCP's; vsock's `set_rcvlowat` signalling readiness like TCP's. The agent then only waits for the hangup on both sockets |
| 2026-09-04 | streams | the join on the patched kernel (0016) — every request through it, both directions; the M5's kept-alive GET median 62 µs p99 169 (OrbStack 72–75), 22159 connects/s (OrbStack 18428), Mac→published port 89.2 Gbit/s | the other three iperf3 paths read 0, and a run by hand ends with the guest's OOM killer taking dockerd | a sockmap redirect has no backpressure: the source's data is consumed the moment the verdict runs (TCP's window stays open, vsock's credit is returned) while the bytes wait in the target's backlog, unbounded; a bulk sender fills the guest in a second. Kernel patch 0017: the backlog is counted, sources past a high mark are paused (their data-ready returns without reading, so their own flow control reaches the sender) and resumed from their backlog workers below a low mark |
| 2026-09-04 | process | a chain whose port latency and paths read zero while a single curl passed: three machines from killed chains were still up, one holding the test port; and a `cargo build --release` leaves the binary unsigned (`make sign PROFILE=release`), which reads as an empty machine.log | — | kill by LIGHTER_HOME after a chain is stopped; sign after every release build; an empty machine.log means read start.log |
| 2026-09-04 | streams | the join with backpressure (0017): 4.35 / 89.8 / 92.0 / 4.29 Gbit/s, no stall, the streams gate passing end to end (checksummed 256 MiB each way, half-close, TLS, refusal, port lifecycle); GET median 61 µs p99 144, 21150 connects/s | into the container 1.7× OrbStack on both paths; out of it a tenth of the copy path | the out direction moves one credit window per jiffy: on EAGAIN (vsock out of credit) the backlog parks a jiffy, and the credit update's write-space wake was a `schedule_delayed_work` that defers to the pending timer — 2 MiB per 4 ms is 4.3 Gbit/s. The callback now `mod_delayed_work`s the timer to now |
| 2026-09-04 | streams | the write-space wake pulling the parked timer forward | 78.1 / 86.7 / 89.6 / 88.9 Gbit/s (OrbStack 96 / 54 / 53 / 92); GET median 61 µs p99 152, 21132 connects/s; the streams gate passes | the join is the path: the two into the container 1.6–1.7× OrbStack, the published port's reverse at 97% of theirs, egress at 81%. Next: where egress's cost is on the host |
| 2026-09-04 | streams | egress profiled: on the host the reactor thread 89% busy, 80% of it in `writev` (the Mac kernel's copy), a vsock TX poller spinning, and vCPU 0 at 96% while the guest reads 12% system of eight — the container's send, the receive softirq, the verdict and the backlog's copy into vsock all on the sender's CPU | — | tried the backlog on the unbound workqueue (0018): 54.1 / 67.3 / 65.6 / 44.4 and 48.2 / 68.0 / 66.6 / 56.1 — every path down by a quarter to a half, inbound included. The cross-vCPU wakeups and the lost cache cost more than the shared CPU. Reverted |
| 2026-09-04 | streams | receive packet steering in the guest (`rps_default_mask=ff`, so the container's receive softirq — and the verdict and backlog with it — lands on a different vCPU from the sender), set live through the control socket | egress 44.6 / 50.5 Gbit/s against 86.6 / 84.9 quiet baseline | rejected: as with the unbound backlog, moving the chain off the sender's CPU costs more in cross-vCPU wakeups than it frees |
| 2026-09-04 | streams | why out is slower than in: `skb_send_sock` offers an skb one page fragment per `sendmsg`, and vsock makes a packet of each call and copies it — the container's stream reaches the host as 4–32 KiB packets against the 256 KiB linear ones going in | — | kernel patch 0018: every remaining fragment in one bvec per call, and vsock honoring MSG_SPLICE_PAGES from a kernel caller — pages by reference into the packet, no copy, charged as its zero-copy path charges |
| 2026-09-04 | streams | patch 0018's first cut: inbound up to 92.1 / 96.0 Gbit/s (port), egress 0, the integrity check failing, and a `sock_wfree` underflow in the vsock transmit completion | — | the generic splice helper cuts a 32 KiB compound-page fragment into eight, hits the fragment limit and comes up short; the short path returned an error without charging the bytes already attached, and the freed skb subtracted them. Now vsock walks the bvec segments itself, one caller fragment to one packet fragment, charges exactly what it took, and a short packet is a short packet (the header says) |
| 2026-09-04 | streams | spliced pages, fragment for fragment (0018): 91.3 / 89.4 / 97.8 / 90.6 and 83.5 / 89.1 / 96.8 / 82.7 Gbit/s at ten seconds a path on a quiet host; the streams gate passes | OrbStack's earlier 96 / 54 / 53 / 92: the two outbound paths level within the run-to-run spread, the two inbound 1.7×; no copy of a container's outbound byte anywhere in the guest now (TCP pages by reference into vsock packets, the packet's pages straight to the host ring) | the join is the design: kernel TCP at both ends, sockmap in the middle, one host copy. Next: OrbStack's numbers taken the same hour with the suite, then egress's spread |
| 2026-09-04 | record | the suite's network cases, OrbStack and lighter in the same hour on the M5 (medians, Mbit/s): egress 94894 / 89878, egress-r 51994 / 89795, port 52479 / 94404, port-r 92630 / 90167, UDP 2969 / 3017; connects/s 21362 / 17333; GET median 75 / 61 µs, p99 150 / 157; DNS 253 / 54 µs | ahead on both inbound paths (1.7–1.8×), GET and DNS; level on port-r and UDP; behind on egress (95%, and rep 1 read 37.6) and on connections (81%) | the suite's connect probe sends no byte, so the join's ten syscalls buy nothing there: join on the first readable event instead, and let closed sockets leave the maps on their own |
| 2026-09-04 | streams | the join on the first byte, teardown without syscalls (closed sockets leave the maps; peer entries in an LRU hash) | 22186 connects/s (from 21150 joined eagerly, 21312 before the join); kept-alive GET median 63 µs p99 173; 88.9 / 88.8 / 93.5 / 90.3 Gbit/s; the gate passes | kept |
| 2026-09-04 | streams | the outbound spread traced: a published port's reverse at 0.1 s intervals runs at 100–105 Gbit/s and stops dead for a tenth of a second to a second at a time; with the join off it stops more (10–15 intervals of 80 against 0–9), so the fault is the host's. A trace of every stream every 10 ms (`LIGHTER_STREAM_TRACE=1`): in the hole the reactor's counters freeze with nothing to do, the device's outbound queue is empty, and the guest holds its whole credit window unused | — | the completion path: a transmit chain the writer has finished with goes back to the guest "on the next look at either ring", forced only once 32 have piled up. With spliced pages a chain is 18 descriptors, the guest's ring holds about 14, less than the pile and less than the credit-update threshold: the guest fills the ring, the host writes all of it, and nothing wakes anyone until unrelated traffic drains a ring. `complete` now wakes when every held chain is done |
| 2026-09-04 | streams | with completions returned: the published port's reverse 0 stalled intervals of 80 on the join (86.4 / 90.8 avg; the copying fallback still stalls, 13 of 80 on one run — its own hole, to find); four paths 83.2 / 90.3 / 97.1 / 88.9 and 82.6 / 89.1 / 95.9 / 81.6; egress alone at 0.1 s intervals four times: 93 / 94 / 94 / 92 average, minimum 59–72, the one dip under 60 the first tenth of a second (the Mac's receiver warming: `writev` EAGAIN while it starts reading) | outbound level with OrbStack's 93–99 within the spread; the gate passes | the trace stays behind `LIGHTER_STREAM_TRACE=1` |
| 2026-09-04 | record | the suite's network and power cases for lighter after the day's work, same hour as OrbStack's (medians, lighter / OrbStack): egress 88.7 / 94.9 Gbit/s, egress-r 87.9 / 52.0, port 94.1 / 52.5, port-r 89.0 / 92.6, UDP 2.7 / 3.0; connects/s 17.4k / 21.4k; GET 60 / 75 µs, p99 157 / 150; DNS 54 / 253 µs; idle 4 / 3 ms of CPU a second, 100 / 115 wakeups a second | ahead on both inbound paths, GET, DNS and wakeups; level on port-r, UDP, p99 and idle CPU; egress at 94% (alone, at 0.1 s intervals, 92–94 against their 93–99) | connections: a fresh machine reads 20.1k then 17–18k as the Mac accumulates TIME_WAIT (6,000 after six runs; a four-second churn exhausts its ephemeral ports outright), and OrbStack against the same loop reads 17.3k / 21.6k / 19.3k with the same 3,001 TIME_WAIT behind it — the case measures the Mac kernel's port recycling for both, and the spread covers both. The network is level or ahead on every case |
| 2026-09-04 | memory | the suite's memory case on the M5 (lighter / OrbStack, MiB): settled 3371 / 1730, peak 8815 / 5266, after 15 s 7687 / 1874, after 60 s 7309 / 1867 — while the VMM's own footprint reading, traced every second (`LIGHTER_MEM_TRACE=1`), settled at 3646, and the guest reported 14.3 GB free with every byte released on the host | — | the suite counted the VMM twice: its pattern for the gvproxy sidecar (`gvproxy.*$RUN_DIR`) also matched the VMM, whose command line names the gvproxy binary and the run directory. Every lighter memory figure recorded before today was double, on both machines: the M1's 1254 / 4047 / 2126 / 1456 against OrbStack's 1088 / 4311 / 2150 / 1021 was really about 627 / 2024 / 1063 / 728. Fixed in the suite; both machines' memory records are re-taken in the record phase |
| 2026-09-04 | memory | the M5's memory case with the count fixed (lighter / OrbStack, MiB): settled 1686 / 1730, peak 4596 / 5266, after 15 s 3803 / 1874, after 60 s 3713 / 1867; the footprint per second: the install ends at 26 s, the containers' trim fires at 36 s and returns 0.8 GB, nothing after | ahead settled and at the peak; 1.9 GB behind once idle | the end state, by hand on a 12 GB guest after an install: the containers' cgroup at 50 MB but 1.2 GB of cache in the root cgroup (the image layers dockerd extracted, charged where no trim reaches), 340 MB of free memory below the reporting order, and the host's double charge on the cache the share wrote. `drop_caches=1` returned 420 MB, a compaction 250 more, the slab drop 30. The VMM's own heap is a few megabytes. So: dockerd in a cgroup of its own, trimmed with the containers'; and a second stage at forty seconds idle — the containers down to a sixty-fourth, compaction again |
| 2026-09-04 | memory | dockerd in its own cgroup, two stages at ten and forty seconds idle | settled 1624, peak 4420, after 15 s 3846, after 60 s 1819 (OrbStack 1730 / 5266 / 1874 / 1867) | ahead at a minute; the second stage (containers to a sixty-fourth, compaction) returns 1.6 GB within two seconds of firing, the first 0.7 GB. Stages moved to five and ten seconds (the suite pauses three between installs): after 15 s 2784, after 60 s 1820 — and the trace shows the rest of the gap: after the second stage the footprint trickles down at 100–200 MB every two seconds for twenty seconds, one compaction pass leaving most of what the trim freed below the reporting order for the kernel's background compaction to finish. Compaction now repeats after a trim until under 64 MB is left below order nine |
| 2026-09-04 | memory | compaction repeated after a trim | after 15 s 2678, after 60 s 1827; the second stage lands 1.1 GB at once and the rest trickles for twenty seconds — with the guest's free memory already in reportable orders (12 GB in 4 MiB blocks, 350 MB below order nine) | the trickle is reporting's own pace: a cycle every two seconds, its budget a fraction of each free list, one 64 MB report a cycle for the trim's 2 MiB blocks. Kernel patch 0019 makes the delay a runtime parameter; the agent sets it to 100 ms after a trim and back to two seconds fifteen seconds later or the moment the containers work again, since faster reporting under churn was what cost a pnpm install fourfold |
| 2026-09-04 | memory | the first pass deep (a sixty-fourth at five seconds), reporting's budget the whole list when hurried | settled 1532, peak 4564, after 15 s 2587, after 60 s 1499 (OrbStack 1730 / 5266 / 1874 / 1867) | ahead by 370 MB at a minute and on the peak; the first pass lands 1.6 GB seven seconds after the install and a trickle of a hundred megabytes a second follows for fifteen, with the free lists below the reporting order not moving — so not compaction; dirty pages next |
| 2026-09-04 | memory | dirty and writeback sampled through the drain: zero at the trim; the free lists show the trim putting a gigabyte into 4 MiB blocks at once and another 470 MB coalescing over the next thirty seconds while the small orders shrink slowly, 350 MB never | the second run of the same build read 2054 / 1669 against the first's 2587 / 1499, a spread of a few hundred megabytes | the trickle is compaction working around unmovable pages — the slab an install needs, spilled into movable blocks while the page cache filled memory. `vm.defrag_mode=1` (the 6.15 allocator's switch that keeps unmovable allocations out of movable blocks) set in init |
| 2026-09-04 | memory | `vm.defrag_mode=1` | settled 1582, peak 4528, after 15 s 2159, after 60 s 1641; the free lists still coalesce upward for twenty seconds after the trim | within the spread of the run before; kept for now (it costs nothing measured yet, and the mechanism is right), not the lever. The freed pages sit at 512 KiB–1 MiB meanwhile — a third of what was freed — so the fast window now reports order 7, back to 9 with the delay |
| 2026-09-04 | memory | 512 KiB runs reported in the fast window | settled 1499, peak 4366, after 15 s 2071, after 60 s 1658 (OrbStack 1730 / 5266 / 1874 / 1867); the first pass lands 2.0 GB at once, 700 MB trickle over the next twenty seconds | 200 MB behind at fifteen seconds, within the spread; ahead everywhere else. Last round: 128 KiB runs and full-strength proactive compaction in the window |
| 2026-09-04 | memory | 128 KiB runs and full-strength proactive compaction in the fast window | settled 1473, peak 4514, after 15 s 1639, after 60 s 1404 (OrbStack 1730 / 5266 / 1874 / 1867) | ahead on all four readings on the M5. The memory story now: containers and the engine trimmed at five and ten seconds idle, compaction repeated until the freed memory is reportable, and reporting hurried for a while after — every hundred milliseconds, the whole free list a cycle, runs down to 128 KiB — then back to the kernel's churn settings |
| 2026-09-04 | record | the M5 record's lighter share stage: storage and network rows as measured today; the memory row broken — settled 15, peak 15 (the gvproxy sidecar alone: `footprint` returned nothing for the VMM through the install window and worked before and after), after 15 s 4212, after 60 s 3581 against the standalone case's 1639 / 1404 | — | two questions for after the records leave the machine: why `footprint` fails on the VMM in that window, and what holds two gigabytes more after eight storage cases than on a fresh machine (the standalone memory case boots fresh; the suite's runs it after the storage cases on one machine). The rows are re-taken under the suite's own conditions with the trace on, and spliced in by hand since a run truncates the file |
| 2026-09-04 | record | the M5 record on the final build (medians, lighter / OrbStack): share npm 6885 / 8492, pnpm 3844 / 4724, yarn 5308 / 7795, ripgrep 83 / 1022, find-walk 94 / 595, copy-tree 3259 / 8707, rm-rf 2303 / 2966; own disk npm 4596 / 7009, pnpm 1190 / 2033, yarn 4094 / 5084, ripgrep 88 / 102, find-walk 93 / 127, copy-tree 904 / 1115, rm-rf 382 / 496; network egress 90.0 / 97.2 Gbit/s, egress-r 88.7 / 52.9, port 93.6 / 54.2, port-r 89.8 / 93.1, UDP 2.65 / 3.12, connects 18.2k / 16.2k, GET 59 / 73 µs (p99 143 / 119), DNS 51 / 251 µs; idle 4 / 2 ms/s, 95 / 99 wakeups/s. Colima and Docker Desktop far behind on everything (Colima's own-disk stage failed: its VM disk full — pruned and re-run) | ahead on every storage case both ways, ahead or level on the network except UDP at 85% (gvproxy's path), level on idle power | the lighter memory row (15 / 15 / 4212 / 3581) is broken and re-taken under the suite's conditions once the traced run says why |
| 2026-09-04 | record | why the memory row read 15: `footprint` prints gigabytes from ten up, and the suite parsed megabytes only — after the storage cases the guest holds its whole 16 GiB as cache, so the VMM read "16 GB" and the row read the sidecar alone. Fixed. The traced sequence run says what the suite's conditions are: settled 16 GB, 17 GB through the install, 4.9 GB fifteen seconds after, 3.4 GB at a minute — with the balloon at 4 GB under the host's pressure (three runtimes' VMs on the Mac at once); OrbStack under the same sequence 2475 / 5509 / 2357 / 2180 | the sequence is a user's day: builds, then idle. OrbStack's guest never holds more than 5.5 GB because its memory is sized dynamically; ours fills to RAM and gives it back five seconds later | a bound on the containers' cache while they work — a quarter of RAM from 8 GiB up, none below (the M1's 4 GiB guests measured a bound at half or three quarters costing an install a third), `lighter.cachebound=<MiB>` to override — under A/B in the suite's own sequence for both installs and the memory row |
| 2026-09-04 | record | the M1's lighter share stage hit its seventy-minute cap in the warm-up install; the guest had booted with streams and the join on and the container started. The same install through the CLI's machine (four vCPUs) takes ten seconds there, curl and a published port answer | — | bisecting the suite's launch on the M1 by vCPU count with the stream trace on: eight polling vCPUs on an eight-core Mac beside today's spinning host threads is the suspect |
| 2026-09-04 | record | the M1's lighter stage stalled twice in its warm-up while the same launch, bisected by hand, ran in a minute at eight and four vCPUs. Live on the second stall: the guest's console full of "stream to host refused: Operation timed out" — the agent's vsock connects to the host timing out, so no container connection ever established — with the Mac at 20 MB free and its compressor full: Docker Desktop had been up for nine hours, its app named "Docker Desktop" there where the chain's quit asked for "Docker" | — | on an 8 GB Mac a 4 GiB guest under an install beside Docker Desktop's processes leaves the VMM's threads starved to the point of a two-second vsock handshake timing out. The chain now quits Docker Desktop by both names and kills what remains, prints free pages before each stage, and was relaunched with 2.7 GB free |
| 2026-09-04 | memory | a bound on the containers' cache while they work (`memory.high` on their cgroup, a quarter of RAM), under the suite's sequence | installs npm 6.8–7.2 / pnpm 3.9–4.2 / yarn 5.6–6.9 s against 6.9–7.4 / 3.8–4.3 / 5.8–6.5 without: no cost; memory settled 10255, peak 10541, after 15 s 3495, after 60 s 3299 from 16399 / 18447 / 5719 / 4761 (OrbStack 2475 / 5509 / 2357 / 2180) | the first pass of it measured nothing: the agent wrote the limit at boot, before dockerd had made the cgroup. Four gigabytes of cache read as ten: the host's double charge on host-written pages. An eighth of RAM for the containers and an eighth of that for the engine next |
| 2026-09-04 | network | UDP carried as datagrams over one vsock stream: netfilter redirects a container's UDP to the agent, which reads each datagram's original destination and multiplexes every flow on one stream in batches (`recvmmsg`); the host keeps one connected socket per flow on the reactor's kqueue and frames replies back, dropped rather than queued when the guest has no credit; the agent sends replies from the redirected socket so conntrack restores the source. DNS and DHCP stay where they were | — | written, building |
| 2026-09-04 | memory | the bound at an eighth of RAM, the engine at an eighth of that | memory settled 6388, peak 6427, after 15 s 3144, after 60 s 2859 — but pnpm 4.2–7.9 s and yarn 7.5–9.0 against 3.8–4.3 / 5.6–6.9: a working set larger than two gigabytes, paid on every page | rejected; the bound stays at a quarter. The two runs put a number on the double charge: a bound of 4 GB reads as 10.5 through the sequence, of 2 GB as 6.4 — the Mac counts host-written guest cache twice. A shared mapping of guest RAM (`LIGHTER_MEM_SHARED=1`) under measurement, since a private anonymous mapping is the suspect |
| 2026-09-04 | network | UDP's first cut: the agent's flow opened toward 172.17.0.1:15202 — the redirected address, not the container's destination. `IP_RECVORIGDSTADDR` reports the header as delivered, after REDIRECT's rewrite, and conntrack's original-destination lookup answers only for a connected socket | — | TPROXY instead: the datagram arrives with its destination intact at a transparent socket, a policy route makes a foreign destination deliverable locally, and replies leave a transparent socket bound to that destination. Kernel: NFT_TPROXY, NF_TPROXY_IPV4/6, NFT_SOCKET, policy routing; rootfs: iproute2 |
| 2026-09-04 | memory | guest RAM in a shared anonymous mapping instead of a private one | settled 1471, peak 4514, after 15 s 1596, after 60 s 1484: the peak to the megabyte of the private mapping's | there is no double charge in the mapping. What the peak holds beyond the guest's use is reporting's lag: pages an install frees and reuses are resident until re-reported, two seconds and a sixteenth of a list at a time, and a 16 GiB guest has gigabytes of them in flight while OrbStack's dynamically sized guest has little free memory to lag. `lighter.reporting=fast` on the command line keeps the hurried settings throughout, under A/B for the peak and the installs |
| 2026-09-04 | memory | reporting hurried throughout (`lighter.reporting=fast`), the bound at a quarter | memory settled 5426, peak 5681, after 15 s 3224, after 60 s 2494 — the peak halved, level with OrbStack's 5509; pnpm 4.9–9.2 s and yarn 6.8–8.2 against 3.9–4.2 / 5.6–6.9 | the lag hypothesis holds: half the peak was free memory waiting to be re-reported. Rejected as a setting, because every reported page an install reuses is a host fault — upstream's two seconds are what keeps the churn cheap. The rung above both is what OrbStack has: a guest sized to its working set through the balloon, with no free memory to lag or fault. Next |
| 2026-09-04 | record | the M5 record's lighter memory row spliced from the suite's-sequence run with the quarter bound (10255 / 10541 / 3495 / 3299), the honest number under those conditions against OrbStack's 2475 / 5509 / 2357 / 2180 | behind under the sequence, ahead on the standalone case (1473 / 4514 / 1639 / 1404 against 1730 / 5266 / 1874 / 1867) | the record says so |
| 2026-09-04 | network | UDP over the stream with TPROXY, the bridge's netfilter call off, a 16 MiB receive buffer on the agent's socket | container → Mac: 4.73 Gbit/s delivered at 1400-byte datagrams (the sender at 12.5, the rest dropped on the way — the Mac's receiver is the bound), 14.8 at 8000 bytes, 15.2 at 60000; TCP untouched; the streams gate passes | ahead of OrbStack's 3.1 at the suite's size by half again; the Mac's own loopback reads 21.8. The first cut's silence was the bridge: with `bridge-nf-call-iptables` on, the prerouting hooks run in the bridge's context for traffic off docker0 and the socket TPROXY assigns there does not survive into routing — every datagram counted as "no port" |
| 2026-09-04 | power | the final build's record read 11 ms/s and 331 wakeups/s idle where the morning read 4 and 95; a fresh boot reads 5–7 and 97–110 with the UDP proxy running or stopped and the reporting knobs hurried or at defaults, after traffic through an idle container the same, the suite's idle case alone 103, and the full network sequence at one rep followed by the idle case 98 | not reproduced by any single change; the three-rep sequence is being replayed exactly, with and without the connect-rate case, since its 6,000 connections leave TIME_WAIT and conntrack timers expiring inside the sampled minute |
| 2026-09-04 | stall | the M1's lighter share warm-up stalled a third time, 20 min quiet: on the host every thread parked (all eight vCPUs in wait-for-interrupt, the vsock pumps on their condvars, the port watcher blocked reading the daemon, the filesystem settler asleep), the control socket and the Docker socket both silent — a guest asleep on something, not a host thread wedged | added `kill -USR1` → a dump of every virtqueue's indices, event flags and interrupt status plus the vsock counters (`crates/lighter-vmm/src/dump.rs`); the M1 reruns the stage under a watchdog that dumps and samples at four quiet minutes |
| 2026-09-04 | stall | the M1's stall read out: the vsock receive ring empty (`next_avail == next_used`, no descriptor flagged available, the driver's event word armed at the very next slot), the host's side of every new stream established with nothing delivered, 5 KB to the Docker daemon never acknowledged, 3.3 GB free in the guest, every task asleep, no broken-ring message, no allocation failure printed, and a forced interrupt changing nothing (the handler runs the receive worker only on a visible used entry) | the guest stopped reposting receive buffers. Patch 0015 made each one a 256 KiB linear skb — an order-6 page allocation the allocator may refuse under fragmentation — and the driver's refill breaks out on the first refusal with nothing to retry it. A 4 GiB guest trimmed to 64 MiB free with its free pages reported away is exactly where an order-6 block runs out; the M5's 16 GiB guests never got there. The M1's descriptor cap (10240) was a red herring: 57 descriptors held at the stall |
| 2026-09-04 | stall | tried first: receive buffers as 128 KiB skbs backed by pages (32 KiB chunks where they exist, single pages otherwise), one indirect chain each, the unfilled pages trimmed on receive, `CONFIG_MAX_SKB_FRAGS=45` | rejected on the M5: published port 83/83/61 Gbit/s against 92, its reverse 67/60/67 against 89, egress 66/59 against 90/85, and a container curl that returned nothing — every received packet, a 44-byte credit update included, posted four page chunks and freed three. Patch 0021 instead keeps the 256 KiB linear buffer and asks for it without reclaim (`__GFP_NORETRY`), posting a 32 KiB one — order 3, which the allocator does not give up on — when refused; mixed sizes in the ring cost throughput only while memory is fragmented, every refill asks for the large size again, and a refill that posts nothing is retried from a delayed work |
| 2026-09-04 | stall | the diagnostic kernel (patch 0020) on the M1, third try: `virtio_vsock: rx_fill posted nothing: num_free=134 rx_buf_nr=122 max=256 ret=0 alloc=0`, then again at 120, 118, 115, 111, 108 buffers as the ring drained — the 256 KiB allocation returning null with 3.3 GB free in the guest | the cause is proven, not inferred: an order-6 allocation refused under fragmentation, and a refill that gives up on the first refusal |
| 2026-09-04 | stall | the two-tier refill with the large buffer asked for without reclaim (`__GFP_NORETRY`), suite TCP cases on the M5: egress 90/80/90, egress-r 80/59/63, port 70/61/70, port-r 68/62/68 against the record's 90/85/92/89 | rejected: on a 16 GiB guest the memory policy leaves few free order-6 blocks, only compaction makes more, and without it the refill posts 32 KiB buffers most of the time. The first attempt goes back to plain `GFP_KERNEL` — the record's behavior — with the 32 KiB buffer only when that fails, which is the M1's case |
| 2026-09-04 | record | the M1 record on the final kernel: lighter's four stages ran clean in 14 minutes (storage, memory, network, own disk) where the morning's first stage stalled almost every time; OrbStack's share stage died after its first `rm-rf` — `vmgr.log`: "Daemon exited: kernel panic" at 11:14 — leaving its memory, network and idle rows empty and its own-disk stage unable to connect | OrbStack's guest kernel, not ours; both OrbStack stages rerun after the chain, and the outcome recorded either way |
| 2026-09-04 | network | the final kernel (patches 0020, 0021) on the suite's four TCP cases, M5, fseventsd quiet after Nick restarted it, the Mac in use: egress 91/65/81, egress-r 90/87/82, port 97/94/85, port-r 94/92/83 Gbit/s against the record's 90/85/92/89; the fallback never fired | the record stands for the shipped kernel: with the large buffer granted, the refill is the code the record ran |
| 2026-09-04 | memory | the record's sequence traced (storage once, then the memory case; `LIGHTER_MEM_TRACE=1` on the host, meminfo and cgroup usage every three seconds in the guest): the host climbs to 6.4 GB through the storage cases and holds it — while the guest, by the end of them, has 13 GB free and 1.5 GB of cache; the case reads "settled" five seconds after the last case, the first trim fires at that same second, and the drop lands two seconds later: 6.4 → 1.9 GB in one step, the trim itself freeing 1.4 GB and its hurried reporting window returning the rest; the install then peaks at 2.8 GB and the minute reading is 2057 (OrbStack 2180) | the record's 10255 settled is free guest memory the host had not been told about: the churn setting reports 2 MiB runs after two seconds, and what rm-rf frees is in file-sized pieces below that. Not lag in returning memory, and not the cache: the moment reporting hurries. Next: hurry at two seconds idle (no reuse to fault on), trims unchanged |
| 2026-09-04 | memory | found while re-baselining: the agent in the tree bounded the containers' cache at an eighth of RAM since 08:29 — the UDP commit carried it — while the worklog's decision at 08:41 was a quarter; the M5's later runs (the network record, the gates, the hurry experiments) ran with the eighth, the M1's guests take no bound below 8 GiB and are unaffected | the quarter restored; the hurry runs' pnpm first-repetition times (8.4 and 6.5 s) were the eighth's known cost, not the hurry's, and their memory readings (4840 / 4329 settled) sit on the eighth's lower baseline (6388) rather than the quarter's (10255) |
| 2026-09-04 | record | the M1 record complete on the final build, every runtime (OrbStack's stages rerun after its panic, cleanly): lighter first on every read and walk (ripgrep 190 ms vs 1.11 s, find 122 vs 552, cp 5.2 vs 15.2 s, rm 3.3 vs 4.0), on all four memory readings (1263 / 3875 / 1029 / 1014 MiB vs 3273 / 5041 / 2674 / 1496), on the published-port inbound path (44 vs 30 Gbit/s), UDP (5.0 vs 3.1) and DNS (154 vs 422 µs); behind OrbStack on the share installs (npm 11.9 vs 11.5 s, pnpm 6.9 vs 5.9, yarn 11.1 vs 10.4), on egress (52 vs 64 Gbit/s), the published port's outbound path (55 vs 67), connects (15.3k vs 16.5k), GET (150 vs 128 µs), and idle (23 ms/s and 325 wakeups/s vs 21 and 91) | three open items for the M1, in order: the idle wakeups (three and a half times OrbStack's, three times lighter's own on the M5 — 8 vCPUs on 4P+4E), the share installs (pnpm was level at 6.0 in the previous M1 record), and the egress and outbound paths |
| 2026-09-04 | memory | Nick's daily driver (24 GiB guest, 16 vCPUs, dozens of containers) restarted on the day's build: load 80, 36 tasks in D state in `folio_wait_bit_common`, one in `__mem_cgroup_handle_over_high`, every infra container unhealthy, Docker builds stalled on network fetches. `/sys/fs/cgroup/docker/memory.high` was 3 GiB (the eighth) with `memory.current` pinned at it and 782,575 `high` events | the benchmark's cache bound applied to a whole workload: `memory.high` throttles every allocation over the line and the cache thrashes. Lifted live (`echo max`), healthy within twenty seconds. The bound is opt-in from here (`lighter.cachebound=<MiB>`), never a default; the suite's memory readings will be re-recorded on the default with the balloon offer in its place |
| 2026-09-04 | memory | the guest's offer through the balloon (agent → host, vsock 2381, 16 bytes a second: spare beyond RAM/16 after two seconds of container idle, a release flag when work resumes), the quarter bound still on, traced through the record's sequence | it works and it is fast: the offer ramps the target 4 → 10 GB in 2 GiB steps and the footprint falls 10.3 → 3.1 GB in four seconds (1.8 GB/s), fragmented free memory the reporting path could not return without compaction; the minute reading 2333 (OrbStack 2180). But the suite read "settled" at second 273 and the first offer came at second 274: container idle counted in one-second ticks and two of them wanted, then the steps. The first cut flapped 14.5 GB ↔ 0 each second because an offer of zero meant both hold and release; the release is now its own word |
| 2026-09-04 | memory | next: quarter-second idle ticks (the offer at two seconds sharp), 4 GiB steps, and patch 0022 — the balloon driver hands the host 4096 page frames per round trip instead of 256 (64 MiB per request with 0014's units, not 4) | the whole return should then land inside the five seconds the suite allows |
| 2026-09-04 | stall | Nick's daily driver, second wedge of the day, in the host: bulk data over the Docker socket dead (64 MB from a container did not finish in two minutes, `docker cp` sat at 232 KB) while `docker ps` answered; the ring dump: transmit ring `next_avail=35 next_used=240`, 51 chains taken and none returned, the driver waiting at 240; connections into 2375 each stuck at exactly one 256 KiB window | `complete()` wakes the deliverer only when 32 chains are done or every held chain is; a stalled outbound stream (the deploy's push to AWS through the thrash) holds its chains for good, fewer than 32 others finish behind it, and no wake comes — the guest returns completions in order, so every socket's send memory stays charged. Fixed: the deliverer's wait is bounded to a millisecond while any chain is done (`has_done`), so a finished chain goes back whatever the others do |
| 2026-09-04 | power | the M1's 325 idle wakeups probed on the record build: 8 vCPUs 98, 4 vCPUs 88, 2 vCPUs 82, idle polling off 102, the sockmap off 100; idle straight after each network case: UDP 103, connect-rate 102, egress 100, HTTP 96 | none of the machine's knobs and no single network case reproduces it, on either machine; it appeared once on the M5 and once on the M1, both after a full record sequence. Unresolved; the record carries what it read |
| 2026-09-04 | stall | the daily driver's wedge reproduced on a fresh machine: 64 MB streamed from a container never finished, `docker cp` sat at 232 KB; rings clean, the host's pumps idle, the guest's bridge threads in `vsock_connectible_sendmsg` waiting for credit after exactly 262,144 bytes; with a credit update on every acknowledgement (`LIGHTER_CREDIT_EAGER=1`) the same stream took 1.2 s | this morning's coalesced credit (01:43): an update only after a quarter of our 4 MiB window. A guest socket sends no more than its own buffer's worth — 256 KiB by default on the Docker-socket bridge — ahead of the last credit it saw, whatever we advertise, so the update at a megabyte never came. The threshold is now a quarter of the smaller window (the agent's 8 MiB streams unchanged, the Docker bridge credited every 64 KiB): 256 MB in 2.6 s, `docker cp` 936 KB in 30 ms |
| 2026-09-04 | memory | patch 0022 (4096 frames a round) and quarter-second ticks, no cache bound, traced: the offer's targets seesawed 4 → 8 → 12 → 4 → 8 → 12 GB within two seconds — each target summed from the driver's reported count, which lags its allocations, so a later offer fell below the target being filled and the driver deflated — then 11 GB back in three seconds (3.7 GB/s, twice the 256-frame rate); settled 12303, 15 s 3366, 60 s 3365; installs npm 6.3–7.0, pnpm 3.8–4.3, yarn 5.2–6.1 (record 6.6–7.0 / 3.7–3.9 / 5.2–5.6) | the target never lowers on an offer now, and the step is 8 GiB |
| 2026-09-04 | memory | monotonic 8 GiB steps, no bound: the offer fired at second 200, the target went 8.9 → 14.5 GB inside that second, the footprint 16.0 → 15.2 → 9.2 → 3.6 GB by second 204 — 12.5 GB in three seconds; but the last container stopped at 196 and the suite read "settled" at 201: 14351. Installs npm 6.1–6.6, pnpm 3.7–4.1, yarn 5.2–5.4 with no cache bound at all; 15 s 3307, 60 s 3257 (OrbStack 2357 / 2180) | the return is fast enough; the fuse is what is late: two seconds of idle, and a stopped container's teardown shows CPU in the cgroup for two more. Next: a one-second fuse when no container runs at all, two seconds of CPU idle otherwise; the balloon asked back when free memory falls under half the reserve (an eighth of RAM), not on any activity — a daily driver's idle Postgres answering a query would otherwise deflate gigabytes and retake them every two seconds |
| 2026-09-04 | memory | a one-second fuse with no container running, release when free memory fell under half the reserve: settled 15375, 15 s 3563, 60 s 3555; ripgrep's first repetition cold (3.2 s), copy-tree 3.6–4.0 against 3.3, installs npm 7.0–7.1, pnpm 3.8–4.4, yarn 5.4–5.8; the trace: the offer at second 206 against a reading at 203 — a stopped container's cgroup stays populated past its exit — and the target seesawing 4 ↔ 12 GB every tick as inflation toward a target computed a tick earlier dipped free memory under the release line | the offer no longer waits for the containers at all (free memory beyond the reserve is free whatever they do; the cost is a fault on reuse), and the release needs active work short of memory, not a dip |
| 2026-09-04 | memory | the offer on free memory beyond the reserve (an eighth of RAM), whatever the containers do; the release only from active work short of memory — the record's sequence, the M5 in use: settled 5311, peak 5311, 15 s 1687, 60 s 1523 against OrbStack's 2475 / 5509 / 2357 / 2180; installs npm 6.5 / 6.5 / 6.5, pnpm 4.1 / 3.9 / 3.9, yarn 6.2 / 6.1 / 5.4 against the record's 7.0 / 6.9 / 6.6, 3.9 / 3.7 / 3.8, 5.6 / 5.3 / 5.2 | ahead of OrbStack on the peak and both after-readings under the sequence for the first time, and installs level. copy-tree 3.7–5.6 s and rm-rf 2.5–2.8 against 3.3 and 2.3 in every balloon variant: a case that starts allocating pays a fault on every page the balloon gave back — or the Mac was busy; the M1 is quiet and has a same-day record, so the A/B runs there |
| 2026-09-04 | memory | the same build on the quiet M1 (4 GiB guest) against its same-day record: pnpm 12.3–13.3 s against 6.3–7.1, yarn 15.3–17.1 against 9.9–12.7, every ripgrep repetition cold (6.8–7.4 s against 0.13–0.19), copy-tree 21 s against 5.2; memory 2049 / 3086 / 1357 / 1355 against 1263 / 3875 / 1029 / 1014 | the offer was computed from MemAvailable, which counts the page cache the kernel could reclaim: the target asked the balloon for the cache too, and the driver reclaimed it to fill itself — the same thing at a fifth of the size on the M5 (copy-tree +20–40%). The offer is free memory only from here; the cache stays the trims' to give up |
| 2026-09-04 | memory | the offer from free memory only, M5 in use: settled 16400, 15 s 3770, 60 s 3510; installs npm 6.8–7.0, pnpm 3.7–4.5, yarn 5.4–7.3; ripgrep warm again (0.16 / 0.40 after the cold first), copy-tree 3.8–4.4, rm-rf 2.7–2.8 | the cache is back where it belongs and the settled reading with it: what the host holds five seconds after the last case is page cache the trims give up at five seconds of container idle, the second the reading is taken. The M1's quiet A/B says what the free-only offer costs storage; the trim's timing against the reading is the choice left |
| 2026-09-04 | memory | the free-only offer on the quiet M1 against its record: installs npm 12.7 / 11.8 / 12.5 against 11.9 / 11.6 / 12.4, pnpm 7.2 / 6.4 / 6.3 against 7.1 / 6.9 / 6.3, yarn 14.2 / 11.7 / 10.0 against 12.7 / 11.1 / 9.9; ripgrep 0.16 / 0.14 warm, copy-tree 5.6 / 5.2 / 4.6 against 5.2 / 5.3 / 5.0, rm-rf 2.4 / 2.7 / 2.4 against 4.2 / 3.3 / 2.5; memory 4825 / 4825 / 1627 / 1623 against 1263 / 3875 / 1029 / 1014 | the storage cost is gone. The memory readings on the 4 GiB guest are 600 MB worse at a minute: the balloon leaves the reserve (an eighth of RAM, 512 MiB here) free, and with the trims' hurried reporting and compaction removed nothing returned it. Both stay: the balloon for the bulk, reporting with compaction after a trim for the reserve — and the trims at two and seven seconds when no container runs at all |
| 2026-09-04 | memory | trims at two and seven seconds when no container runs, the free-only offer, no hurried reporting or compaction — the quiet M1 against its record: npm 11.7 / 11.3 / 11.3 against 11.9 / 11.6 / 12.4, pnpm 6.3 / 6.2 / 5.9 against 7.1 / 6.9 / 6.3, yarn 13.1 / 10.7 / 10.7 against 12.7 / 11.1 / 9.9, copy-tree 5.0 flat, rm-rf 2.5 / 3.0 / 2.5 against 4.2 / 3.3 / 2.5; memory 3201 / 3202 / 1760 / 1748 against 1263 / 3875 / 1029 / 1014. The M5, in use: settled 5796, peak 6560, 15 s 4944, 60 s 3936 | installs and storage at parity or better on the quiet machine; the peak under the sequence better on both. The settled and after-readings 700 MB worse on the 4 GiB guest: the reserve plus no hurried reporting after the trim. Restored beside the balloon, under measurement |
| 2026-09-04 | memory | balloon plus hurried reporting and compaction after a trim — the quiet M1 against its record: npm 12.1 / 12.3 / 11.3 against 11.9 / 11.6 / 12.4, pnpm 6.2 / 5.8 / 6.0 against 7.1 / 6.9 / 6.3, yarn 13.5 / 11.9 / 10.4 against 12.7 / 11.1 / 9.9, storage level or better; memory 2257 / 3226 / 1658 / 1658 against 1263 / 3875 / 1029 / 1014. The M5 in use: 4201 / 5520 / 3432 / 3218 against the record's 10255 / 10541 / 3495 / 3299 and OrbStack's 2475 / 5509 / 2357 / 2180 | the peak is won on both under the sequence; the M1's after-readings stay 630 MB over its record with the reporting back — the end-state trace: the balloon at 3021 of 4096 MiB, the guest keeping the eighth-of-RAM reserve free and reporting unable to return it in runs. Next: a thirty-second of RAM when nothing runs at all, the eighth while containers do |
| 2026-09-04 | memory | Nick's daily driver at rest after a build: 17 GB footprint, the guest with 11 GB of cache of which the containers' cgroup held 1.9 GB and the engine's 25 MiB — `dockerd` and `containerd` in the root cgroup, where no trim reaches. The init moved the engine with `echo $$` inside a subshell, and in the busybox shell `$$` is the parent's pid | dockerd starts from a fresh `sh -c` that puts its own pid in the engine cgroup before exec; the engine trim (to 8 MiB at idle) covers the image layers from here |
| 2026-09-04 | memory | the thin idle reserve on the M5 (in use): settled 4139, peak 5615, 15 s 3399, 60 s 3404 — the after-readings unchanged; the end-state trace: the balloon at 14851 of 16384 MiB, so the guest keeps 1.5 GB, while the footprint reads 3.4 GB with `level_mib=4096` (macOS reporting memory pressure — the Mac was in use beside a 16 GiB guest) and the share holding 122,851 descriptors over 184,268 live inodes | the M5's residue is not the guest's: compressed pages under the host's own pressure, and the share's registry. The M1 is the clean read for the reserve question; the registry's size at idle is a rung of its own (park cold inodes when the guest is idle) |
| 2026-09-04 | memory | a thirty-second of RAM as the idle reserve on the M1's 4 GiB guest: pnpm 6.4–6.8 fine, then ripgrep reading nothing (the tree never materialized), find 1019 ms, copy-tree 18.7 s; the console: `virtio_balloon: Out of puff! Can't get 4 pages` every 200 ms | 128 MiB left the materializing container no room to start; and a target past what the guest could spare had the driver retrying its last pages for good, reclaiming a little each time. The idle reserve is a sixteenth, and a hold message pins the target at what the balloon actually holds rather than leaving the remainder to be chased |
| 2026-09-04 | memory | the daily driver restarted on the init fix: `dockerd` and `containerd` in `/engine`, 498 MiB charged there within a minute | the engine trim reaches the image layers from here; the driver's rest footprint after a build is the number to watch |
| 2026-09-04 | memory | the engine in its cgroup, the M5 in use: settled 3007, peak 5718, 15 s 2221, 60 s 2287 against OrbStack's 2475 / 5509 / 2357 / 2180 — the after-readings down from 3.4 GB; the M1 (still the thirty-second reserve): storage level with its record (ripgrep 0.15 / 0.12, copy-tree 5.0 / 5.1 / 4.6, rm-rf 2.5 / 2.5 / 2.7), its memory case spoiled by the next cycle's run starting on the same machine | the M5's residue was mostly the engine's image-layer cache. Iteration from here: `scripts/iter-agent.sh` — the agent built in thirty seconds, picked up from the bench share behind `lighter.devagent=`, one repetition of the storage cases and the memory case on the quiet M1: seven minutes a number instead of thirty |
| 2026-09-04 | memory | the first fast iteration on the M1 (one repetition each; the development agent was not picked up — the suite wipes its work directory, the agent has its own share from here): npm 11.7, pnpm 6.3, yarn 12.7 level; memory 1976 / 2525 / 1521 / 1520 against 1263 / 3875 / 1029 / 1014; copy-tree 10.4 s against 5.2; 95 "Out of puff" lines; the balloon target 3351 → 0 → 2929 → 0 → 2486 → 0 every second and a half from boot | an offer made while work runs seesaws a 4 GiB guest: the work's next allocation trips the release, the next tick offers everything again. The offer now waits for a second of the guest's own CPU idle — the guest's, not the containers' cgroup, which outlives a container's exit — and the release stays with active work short of memory |
| 2026-09-04 | memory | the offer after a second of the guest's own idle, the development agent picked up from its share (`INIT devagent=`): npm 12.3, pnpm 7.9, yarn 13.5, copy-tree 7.4 s, rm-rf 2.4; memory 2478 / 3912 / 1662 / 1655 against 1263 / 3875 / 1029 / 1014 | the seesaw is gone and the cost that remains is structural: a second's pause is the suite's gap between cases, the balloon takes everything free in it, and the next case deflates and faults on every page it touches. The trims wait five seconds, longer than any gap, which is why they never cost this. The offer waits three: past the gap, inside the five a footprint is read at |
| 2026-09-04 | memory | the offer after three seconds of guest idle, trims at three and eight with no container, the M1 (one repetition): npm 13.9, pnpm 6.2, yarn 11.5, copy-tree 5.9, rm-rf 2.6; memory 2248 / 3252 / 1390 / 1302 against 1263 / 3875 / 1029 / 1014 | the peak 600 MB better, the rest 300 MB worse — the idle reserve, a sixteenth of RAM, which is what a command starting into a full balloon gets before the release lands. The trade on a 4 GiB guest; the 16 GiB guest gains on every reading. Confirming with three repetitions before the memory rows are re-recorded |
| 2026-09-04 | memory | the final agent on the M1, three repetitions: npm 14.0 / 11.5 / 13.0 against 11.9 / 11.6 / 12.4, pnpm 6.5 / 5.8 / 6.0 against 7.1 / 6.9 / 6.3, yarn 12.1 / 10.7 / 11.2 against 12.7 / 11.1 / 9.9, copy-tree 5.2 / 5.4 / 5.5 against 5.2 / 5.3 / 5.0, rm-rf 2.9 / 3.1 / 2.9 against 4.2 / 3.3 / 2.5; memory 2257 / 3272 / 1508 / 1506 against 1263 / 3875 / 1029 / 1014, no puff lines | on a 4 GiB guest the balloon breaks even: the peak 600 MB better, the minute reading 500 MB worse (the reserve is a sixteenth of a small RAM), npm's median a tenth slower. Against OrbStack's M1 column (3273 / 5041 / 2674 / 1496) the record's readings win all four outright and the balloon's tie the last. The offer applies from eight gigabytes of guest RAM, where it wins every reading; below, reporting and the trims stay the policy |
| 2026-09-04 | memory | the M1 with the offer off (below 8 GiB), three repetitions: npm 11.8 / 11.7 / 12.5, pnpm 7.4 / 7.0 / 7.6, yarn 12.4 / 10.6 / 11.3, copy-tree 5.7 / 6.0 / 4.8, rm-rf 4.0 / 2.6 / 3.5 — level with the record; memory 1903 / 3608 / 1511 / 1373 against 1263 / 3875 / 1029 / 1014 | the rest readings are 350 MB above the record with the record's own mechanisms. Whether that is the day's other agent changes or the machine drifting between runs: the record commit's agent runs through the dev share on today's host build |
| 2026-09-04 | memory | the record commit's agent through the dev share on today's host build and images, M1, one repetition: installs level (npm 11.9, pnpm 6.1, yarn 11.8); memory 6672 / 6672 / 1678 / 1663 against the record's 1263 / 3875 / 1029 / 1014 with the same agent; the Mac 73% free, no other VM up | the M1's rest reading moved 650 MB with the agent held constant: today's host build, kernel (patch 0022) or rootfs (the engine cgroup, the dev hook). The record's kernel and rootfs rebuilt from its commit run next on today's host; the settled reading's 1263 against 6672 is the trim at five seconds racing a reading at five |
| 2026-09-04 | memory | the record's own kernel, rootfs and agent, rebuilt from its commit, on today's host build, M1: installs level (npm 11.4, pnpm 6.2, yarn 11.2); memory 6977 / 6977 / 1967 / 1947 against the morning's 1263 / 3875 / 1029 / 1014 from the same images; the end-state trace: footprint 1932, resident 2976, reusable 2155, internal 814 | the guest byte-identical, the difference on the Mac: 1.1 GB between the footprint and the resident pages that are not reusable — guest memory macOS compressed during the storage cases, still counted after the guest gave it back. The morning's row came after other runtimes had churned the host. An M1 memory reading carries the host's history at about ±700 MB; today's fine-tuning at that scale was inside it, the large effects (the seesaw, the cache reclaimed into the balloon, the puff loop) were not. Open: whether a released page's compressed copy can be made to go at once |
| 2026-09-04 | power | the record's sequence on the M1 with samplers alongside (powermetrics and guest interrupt counts every 30 s): the idle case at the end read 38 ms/s and 140 wakeups/s — against 98 when the case runs alone and the record's 325; the samplers themselves cost the guest CPU through the quiet minute, and the interrupt columns came out unparseable | the state exists at a smaller size than the record caught; not isolated. Parked behind the share installs and the compressed copies; next attempt with a sampler that reads `/proc/interrupts` raw and stays out of the quiet minute |
| 2026-09-04 | memory | the compressed-copy probe on the M1: a 4 GiB guest fills 2.5 GB of tmpfs, a 5 GB hog on the Mac, the guest frees the file, the balloon returns the memory; footprint 626 → 484 (filled) → 445 (under the hog) → 445 through the free and 453 after the hog left; the Mac's compressor flat at 620 MB throughout | under real pressure this Mac paged the guest's pages out rather than keeping them compressed, and the footprint excludes them; what the guest then freed never came back into the footprint. The state the afternoon's readings showed — footprint a gigabyte above resident-non-reusable — was not reproduced this way; it needs the storage cases' own pressure pattern. Parked with the wakeups |
| 2026-09-04 | storage | the share's statistics (`LIGHTER_FS_STATS`) through one pnpm and one npm install on each machine: 1.96 million requests on both, the same mix to within a percent; the server's time 28.0 s on the M1 against 23.5 on the M5 while the wall time doubles (pnpm 5.7 against 4.0, npm 11.9 against 6.7). By operation on the M1: write 365k, forget 354k, create 305k, lookup 285k, unlink 198k, setattr 124k, rename2 124k, readdir 73k, rmdir 32k (117 µs each — the emptiness listing) | the M1's cost is the guest's side of two million round trips on slower cores, not the server's. 354,000 forgets are one request each: the kernel's batch form exists and the server answers it, but virtio-fs dequeues forgets singly and never batches. Next: a guest patch gathering forgets for a millisecond into `FUSE_BATCH_FORGET` |
| 2026-09-04 | storage | batched forgets in the guest's virtio-fs driver (`FUSE_BATCH_FORGET`, 252 a batch or a millisecond, whichever first) — the install pair sent 354k single forgets, a fifth of its requests, each a descriptor chain and a doorbell for a message the server only counts. Paired on the M1, three reps each, same afternoon, baseline first: pnpm 6.75 → 6.63 s, npm 11.52 → 12.26 s; a second three-rep run of the patched kernel read 6.33 / 12.31. With statistics on: requests 1.97M → 1.61M, the server's time 31.7 → 31.9 s, parked reopens 4,959 → 5,307 (first read as 240k → 252k: the counter is cumulative and had been summed across the intervals it is printed in) | rejected: the forgets were free — the server's time did not move with a fifth of its requests gone — and npm was 6% slower with the patch, paired. The patch is parked in `.logs/patch-park/`, not the tree |
| 2026-09-04 | storage | the M1's descriptor budget on the M5: the install pair with `LIGHTER_FS_FD_BUDGET=6144` (what an 8 GB Mac's `kern.maxfilesperproc` of 10,240 yields), then the M5's own 120,960, then 6144 again, one rep each with statistics on, the M5 in use. 6144: pnpm 5.51 / npm 12.36 s, 1,966 reopens, server 36.5 s; 120,960: 4.61 / 7.13, no reopens, server 26.0 s; 6144 again: 4.06 / 7.56, server 27.2 s. The M1 at the same budget reopens 4,959 times an install pair and its server time is 31.7 s against the M5's 26–27 (the reopen counts first read here as 87k / 46k / 240k were sums of a cumulative counter across the intervals it is printed in — corrected) | the budget is not the M1's gap: the second small-budget run cost npm 0.4 s and pnpm nothing, and five thousand reopens at ~30 µs is a sixth of a second. The registry already operates on a parked file through its parent's descriptor and its name (`Place`, `Located::At`, `under_name`) — the redesign this row first proposed is the code as it stands. What the per-operation profile does say: the M1 matches the M5 on create/lookup/rename/mkdir (15.8 vs 15.9 µs, 14.1 vs 14.0) and pays double on write (5.5 vs 2.7), unlink (22.8 vs 11.2) and readdir (61 vs 50) — the operations that take the apply queue's locks, on a machine whose eight cores are all vCPUs. macOS scales the descriptor ceiling with RAM (8 GB: 30,720 / 10,240; 48 GB: 368,640 / 184,320), noted for whenever a share does meet it |
| 2026-09-04 | storage | where the M1's install time goes, one variable at a time on the 8-core M1 (4P+4E), install pair, single reps unless marked: defaults (8 vCPUs, 16 request workers, 5 apply threads, 50 µs idle poll) pnpm 6.99/6.60/6.51 npm 12.53/11.92/11.38 (×3, evening) and 6.6/6.7/7.1 · 11.5–12.4 in the afternoon; 4 vCPUs 5.98 · 11.40; 6 vCPUs 5.30/5.48/5.43 · 12.82/12.51/11.68 (×3); 4 request workers 5.76 · 12.21; 8 request workers 5.65 · 12.16; serial creates (`LIGHTER_FS_APPLY_WIDE=0`) 5.60 · 13.13; 3 apply threads 6.76 · 11.58; 8 apply threads 5.46 · 13.38; idle poll 200 µs 5.63 · 12.74; idle poll off 5.62 · 13.38; OrbStack's record 5.89 · 11.54 | pnpm is bound by host-side contention — every configuration that leaves the file server a core (fewer vCPUs, fewer request workers) or more apply threads for its clones takes it from 6.6 to 5.3–6.0, ahead of OrbStack at six vCPUs — and npm by the guest's own CPU and the apply lane's creates, which want FEWER apply threads (three beat five and eight) and all eight vCPUs. The two installs want opposite things from one pool |
| 2026-09-04 | storage | a bounded lane for creates beside the wide one for clones (`LIGHTER_FS_APPLY_CREATE_WIDTH`, unbounded by default; commit 47ba565), eight apply threads, M1: width 1 pnpm 5.32 npm 13.71; width 2 6.75 · 11.84; width 3 6.05 · 12.06. On the M5 in use: unbounded 3.98 · 6.81, width 2 4.22 · 7.98, unbounded again 4.08 · 8.50 | no width serves both installs on the M1, and the M5's npm swung 1.7 s between two identical runs while the machine was in use, so the lane is left unbounded and the knob kept for the next machine that asks |
| 2026-09-04 | storage | fseventsd during the install pair on the M5, sampled every second: under lighter mean 79% of a core, peak 349%; under OrbStack mean 148%, peak 430% | not ours to fix — the daemon's cost comes with the churn, and OrbStack pays double; our stream's `kFSEventStreamCreateFlagFileEvents` is not the difference. On the M1 it is a tenth of the machine for either runtime |
| 2026-09-04 | memory | the trim at five seconds of container idle as the cause of pnpm's two speeds on the M1 (the suite's gap between installs straddles five seconds): `lighter.trim=0` via the dev agent, three reps at defaults, pnpm 6.79/6.66/6.70 npm 12.38/11.50/12.60 against 6.99/6.60/6.51 · 12.53/11.92/11.38 with trims | rejected as the cause — and worth knowing: a trim between two installs does not cost the second one. The `lighter.trim=0` switch stays for A/Bs |
| 2026-09-04 | storage | the file server's threads at `USER_INTERACTIVE` (`LIGHTER_SERVER_QOS=1`) on the M1, defaults otherwise, three reps: pnpm 6.20/6.26/6.31 npm 13.18/12.58/12.70 against 6.99/6.60/6.51 · 12.53/11.92/11.38 | rejected: a third of a second on pnpm bought with a second on npm — the vCPU threads run at the default class and lifting the server above them takes the guest's own CPU from npm |
| 2026-09-04 | storage | directory affinity in the bounded lane (`LIGHTER_FS_APPLY_DIR_AFFINITY=1`, commit e76f002): at most one create/mkdir/symlink job per parent directory at a time, the workers spreading across directories. M1, three reps each: five apply threads pnpm 6.27/6.03/6.16 npm 12.42/12.56/11.37; eight threads 5.45/5.37/5.40 · 12.37/12.38/12.47 (eight without affinity, one rep: 5.46 · 13.38); eight threads with the create lane held to three 5.57/5.46/5.52 · 12.02/12.04/12.80; to four 5.50/6.20/5.65 · 12.16/12.37/11.67 | the shape the M1 wants: eight apply threads for pnpm's clones, the creates on three lanes keyed by directory — pnpm 6.6 → 5.5 s (OrbStack 5.89), npm level with the default (12.0 against 11.9; OrbStack 11.54). Affinity alone gave npm a second back at eight threads; the width gave the last half. Whether the M5 wants the same is the next run |
| 2026-09-04 | storage | where the host's CPU goes through the M1's npm install: `ps -M` of the VMM every second, the busiest twelve seconds (the timed rep) — 51 s of CPU on eight cores (53% of the machine): the eight vCPU threads 28.6 s user (the guest's own npm), the five apply workers 2.2 s of system time each and almost no user (18% busy: the lane waits on APFS, it does not compute), one thread at 6.0 s user + 1.0 s system — the filesystem poller spinning on the ring half of every second — and everything else under a second. `sample` leaves agree: vCPUs in `hv_trap`; apply in `__openat` 10.8k, `pwrite` 7.1k, `__unlinkat` 6.2k, `fsetattrlist` 2.2k, `mkdirat` 1.5k, `close` 1.1k; the poller in its own loop, `fstatat` and `mach_absolute_time`; the block poller 3.3k in `__fcntl` outside the timed window | npm on the M1 is bound by the guest's single node thread on a slower core (the M5's guest spends 1.6× less user CPU on the same install, OrbStack reads 11.5 on this M1 against our 11.9–12.0), with the synchronous request latency the only server-side term on its critical path. The apply lane is latency-bound, not CPU-bound, which is why eight workers help pnpm's clones and hurt npm's creates (APFS contention raises the latency more than the parallelism buys). The poller's half a core is the price of poll-after-submit; on an eight-core machine at 53% it is not what npm waits for |
| 2026-09-04 | storage | the M1's shape on the M5, three reps each, the machine lightly used: defaults pnpm 4.04/4.01/3.88 npm 6.82/6.61/6.81; eight threads + affinity + width 3 4.24/4.52/4.22 · 7.00/7.59/7.67; then one at a time — eight threads alone 5.30/4.04/4.27 · 7.30/8.69/6.90; affinity alone 4.02/3.87/4.74 · 7.11/6.63/6.64; width 3 alone 4.47/3.96/4.10 · 6.62/6.55/7.45 | the thread count is the only one of the three the M5 pays for; affinity and the width are level to a little better there and both positive on the M1. Adopted (commit 395654c): affinity on, width 3, and eight workers on a Mac of eight cores or fewer, five above — the one machine-dependent default, measured on two machines and not yet understood |
| 2026-09-04 | storage | the host poller's idle window on the M1 (`LIGHTER_HOST_POLL_US`; 200 µs default, half a core of spinning through npm), on the M1's shape, three reps: 50 µs pnpm 5.30/5.25/5.29 npm 12.34/11.45/12.89, system time 13 and 18 s per install against 11 and 15; off 5.55/5.38/5.47 · 12.81/13.47/11.83, system time 20 and 33 s | kept at 200: a quarter second on pnpm at 50 µs is bought with a fifth more kernel time, and parking on every idle doubles it — the poller's spin is paid for by the syscalls it saves |
| 2026-09-04 | record | both machines re-recorded on the evening's defaults (395654c), the M5 quiet from 19:14 (daily driver, Colima and OrbStack stopped), OrbStack's memory case re-run in the same state on each. M5 lighter: npm 6.52 pnpm 4.02 yarn 5.10 s (morning 6.89 / 3.84 / 5.31); memory 1610 / 2057 / 1269 / 1270 MiB against OrbStack's 1028 / 5498 / 2850 / 2114; network within 2–6% of the morning; idle 10 ms/s and 120 wakeups/s (6 and 102). M1 lighter: npm 11.17 pnpm 6.02 yarn 11.13 (morning 11.90 / 6.90 / ?; OrbStack 11.54 / 5.89); memory 2282 / 3611 / 1413 / 1317 against OrbStack's 1097 / 4112 / 2144 / 1027 | the README's tables regenerated from these. The M1's settled reading is the suite's five-second read landing on the agent's five-second trim (the trim itself was measured free for the next install, `lighter.trim=0` row above); the M5's network and power dips are small enough to be session noise and are the first thing to re-read tomorrow |
| 2026-09-04 | storage | the yarn install's profile on the M1 (7% behind OrbStack there: 11.13 against 10.36 s), statistics on: 1.0M requests — setattr 286k at 0.8 µs, lookup 183k at 5.1, write 165k at 1.7, create 143k at 15.0, forget 82k, unlink 72k at 14.2; the server's whole time 6.5 s across warm-up and rep, no push waits; the apply lane's creates 123 µs | not the server's: yarn is a single node thread whose critical path is its own CPU plus a round trip per request, and two setattrs per extracted file (chmod, then utimes) are 286k of those round trips. The lever is fewer requests from the guest — a coalesced setattr in the driver — or a cheaper round trip, and neither is an evening's change. Parked, as the M1's last storage gap |
| 2026-09-04 | power | the agent through the idle case (M5 quiet, three alternating pairs, dev agent on the share against the rootfs agent): one-second tick once idle for ten seconds, memory offers sent only when they change, trims at 3/8 s whether or not a container runs — 6/143, 5/138, 5/131 ms/s · wakeups/s against 9/118, 8/122, 8/114; on the M1 one pair 11/138 against 15/123. Inside the guest over ten seconds: timer interrupts 609 against 877, function-call IPIs 110 against 138, the vsock device 0 against 40, context switches 536 against 843. Memory, standalone case, at a minute after an npm install: 624 MiB on the M5 and 614 on the M1 (the in-sequence record 1270 and 1317) | adopted (f5665b4): a third less CPU idle and a quieter guest by every count the guest keeps. powermetrics' wakeup column moved the other way by 15%, and the guest's own counts say it is not more interrupts: a vCPU that idles longer exits idle from idle more often, which is what that column counts. The rootfs is rebuilt on it and both records re-run |
| 2026-09-04 | power | `vm.stat_interval=10` written by the agent at start (the per-CPU vmstat workers woke once a second each on the idle guest): two pairs on the M5, dev agent 8/138 (probed) and 6/134 against the rootfs agent's 5/111 and 5/96 ms/s · wakeups | rejected: nothing on the host's side of it — the vmstat workers are a few of the guest's ninety wakeups a second and the vCPU wakes for containerd, RCU and the timer regardless. The guest's idle wakers by schedule count over ten seconds: containerd 115, rcu_sched 27, dockerd 26, kworkers ~60 across events_long / events_freezable / mm_percpu_wq, the agent 9. The remaining idle CPU (5 ms/s against OrbStack's 2) is in the vCPU threads — 3.8 of 4.5 ms/s by `ps -M` — at roughly 40 µs of host CPU per guest wakeup; the lever is fewer guest wakeups (containerd's 11 a second first) or a cheaper WFI round trip |
| 2026-09-04 | power | the M1's idle reading at the end of the full suite (325 this morning, 369 and 329 tonight, against 91–123 standalone), bisected: the idle case after memory, after each network case, after all eight network cases, after watch-latency, after npm, after rm-rf + watch-latency + memory, after three storage repetitions (fseventsd sampled: asleep), and after a fresh cargo build — every one 91–123 wakeups/s and 8–15 ms/s. The full suite once more with the guest probed in its idle minute: 66 timer interrupts and 10 IPIs a second, the same as a clean idle, and that run read 107 | not reproduced in nine attempts, so not understood: three of four full-suite runs today read three times the standalone idle with nothing in the guest to show for it, and no shorter sequence does. The README carries the record as it read. Next time it happens the thing to catch is the host's threads (`ps -M` deltas through the minute, `.logs/chain182.sh` has the harness), not the guest |
| 2026-09-04 | memory | the in-sequence memory reading probed on the M5 (npm, pnpm, copy-tree, rm-rf, then the memory case; guest meminfo through the control socket beside the host footprint): six seconds after rm-rf the guest uses ~480 MiB (cached 243, anon 83, slab 87) and the host footprint reads 1047; a minute later the guest uses ~380 and the footprint 727. The case itself read settled 1084, 744 at 15 s and 741 at a minute; standalone the same case reads 682 / 643 / 624 | the gap between the guest's use and the host's footprint is free memory the guest has not yet reported: ~570 MiB at six seconds draining to ~350 over the minute, about 6 MiB/s — far under what reporting could move, so the pages are not in reportable runs (a 4 GiB guest after a package churn is fragmented; reporting is order 5 for 25 s and order 9 after). The settled column against OrbStack (1158 vs 1028 on the M5, 2353 vs 1097 on the M1) is this. The balloon, which takes host pages one at a time from any free list, is off below 8 GiB — it broke even on a 4 GiB guest under the old offer logic; being re-measured with the settled one (`lighter.balloonmin=`) |
| 2026-09-04 | memory | the balloon on the 4 GiB bench guest with the settled offer policy (`lighter.balloonmin=2` via the dev agent), M5: standalone memory case on 743 / 3392 / 549 / 490 against off 755 / 3494 / 659 / 618; in sequence (npm, pnpm, copy-tree, rm-rf, memory) on 782 / 788 / 692 / 693 against off 870 / 870 / 752 / 744 — and copy-tree 7.68 s against 6.53, rm-rf 3.20 against 2.49 | rejected again, for the reason it was drawn at eight gigabytes: the offer lands in the three-second gap between two commands and the next one faults every page back, a fifth to a third slower for ninety megabytes returned sooner. Below 8 GiB the reporting path returns memory at the pace fragmentation allows and costs the next command nothing; the settled column against OrbStack on small guests is the price of that, taken deliberately |
| 2026-09-04 | network | the stream A/B knob (`LIGHTER_STREAM_THREADS=1`, two threads a stream against the reactor) had rotted: DNS and the guest's UDP were started only on the reactor's branch, so the threads mode had no resolver and could not build the benchmark image. Repaired — the reactor always runs for DNS and UDP; the streams, inbound included, choose. Streams gate green in both modes. M5, three reps, threads against reactor: into a published port 84.0 against 92.0 Gbit/s, out 81.8 against 94.4, connects 3,170 against 16,828 a second, GET 90 against 60 µs; M1, one rep: GET 178 against 153, connects 1,360 against 14,900 | the reactor stays, and by a distance on connects: a thread a connection is a spawn a connect. The knob is measurable again, which is what it is for |
| 2026-09-04 | network | two host-side knobs on the M1's inbound round trip, three reps each: the pollers' idle window at 1000 µs — GET 156/157/146 µs, connects 17.1k/15.0k/15.4k, out of a published port 55.2/54.7/55.1 Gbit/s; credits on every ack — 184/175/171, 14.7k/14.6k/13.5k, 55.0/53.8/54.9; baseline 156/153/149, 14.8k/14.3k/15.6k, 55.1/55.0/54.4. OrbStack's record: 128 µs, 16.5k, 67.5 Gbit/s | neither moves it: the M1's inbound cost is structural — the thread handoffs a round trip crosses on the host (listener → reactor → vsock device on the way in; poller → deliverer → reactor → socket on the way out), each a scheduler hop that costs more on four performance cores shared with eight vCPUs than on the M5, where the same path beats OrbStack (58 against 73 µs). The next move is to count the hops with timestamps and take them out, a design rather than a knob |
| 2026-09-04 | network | the reactor spins before it parks (commit 35254d2): after any activity it polls kqueue and the vsock side's progressed flag for a window instead of blocking, and the vsock side writes the wake pipe only for a parked reactor. Three reps of the published-port cases. M1: GET 149 → 129 (50 µs) → 131 (200 µs); connects 15.6k → 15.3k → 14.3k; out of a published port 54.9 → 55.2 → 54.6 Gbit/s; into 43.2 → 40.0 → 43.4. M5: GET 60 → 63 → 59; connects 17.8k → 17.9k → 18.1k; out 89.5 → 94.5 → 93.8; into 98.1 → 94.8 → 93.4. OrbStack's records: M1 128 µs, 16.5k, 67.5 Gbit/s; M5 73, 16.2k, 93.1 | adopted at 50 µs: the M1's GET meets OrbStack's, the rest is level, and the window costs nothing idle since it follows activity. The out-of-a-published-port path on the M1 (55 against 67) is untouched by it — that one is throughput, not latency, and is the next network rung there |
| 2026-09-04 | network | the VMM sampled for six seconds through the M1's egress case (container → the Mac, 52.7 against OrbStack's 63.6 Gbit/s; this run 41.8/41.3/52.6): busy samples vcpu 4,921 (`hv_trap` — the guest's TCP and the sockmap join), the reactor 2,813 of which 2,699 in `writev` (the Mac's own TCP send), the vsock TX poller 2,423 — its loop 951, `__psynch_cvbroad` 466 (the progress condvar's broadcast, a syscall per packet whether or not anything waits), and ~600 in the clock (`mach_absolute_time`, `clock_gettime`, `mach_timebase_info`, `sub_timespec`: an `Instant::now` per spin iteration while every iteration finds work) | two cheap cuts on the poller's own thread, worth ~40% of it: read the clock only on an empty iteration, and broadcast only when a blocking waiter exists (the reactor takes the flag now). Then measure egress on the M1. After the release, not before |
| 2026-09-04 | release | 0.2.0: the full gate set green on the head (m1–m8, netload, streams — the version bump broke m7/m8 mid-run once, the internal crates pinned to 0.1.0; fixed and rerun), the daily driver up on the same build, the tarball signed and notarized (`dist/lighter-0.2.0-arm64.tar.gz`, sha256 9e011a95…2e93 — rebuilt and re-notarized from the lint-fixed head after the first tarball, from one commit earlier, was withdrawn), the formula and the tap pointed at it, `v0.2.0` tagged on the release commit, dev → main as one PR | shipped at Nick's call on the gates alone, no soak: 0.x. CI on dev had been red on five clippy warnings the local run tolerated — the CI commands are the gate, verbatim |
| 2026-09-05 | framework | S1: the guest booted under Virtualization.framework from a Rust host on `objc2-virtualization` 0.3.2 (kernel rebuilt with `CONFIG_PCI`, `PCI_HOST_GENERIC`, `VIRTIO_PCI`, `ARM_GIC_V3_ITS`; `console=hvc0`; our rootfs and a fresh data disk; the card a `VZFileHandleNetworkDeviceAttachment` datagram socketpair answered by `net.rs`'s responder; vsock; balloon; the Rosetta share) | init ready 2.07–2.09 s after `start` (dockerd 1 s of that); DHCP through the responder ok; every device on PCI (`platform/40000000.pci/pci0000:00`), none on MMIO; the balloon negotiates MUST_TELL_HOST and DEFLATE_ON_OOM only — no free page reporting, no page hinting; the framework's helper `com.apple.Virtualization.VirtualMachine` held 463 MiB RSS just after boot of a 4 GiB guest, our process 10 MiB | kept: the framework boots our guest as is |
| 2026-09-05 | rosetta | S1: `docker run --platform linux/amd64` with `/mnt/rosetta/rosetta` registered through `binfmt_misc` (`OCF`, qemu's entry removed) | `uname -m` = `x86_64`; python3 `hashlib.sha256` 4 KiB blocks: native arm64 663k/s, Rosetta 145k/s, qemu 56k/s; a 3M-iteration python loop: native 96 ms, Rosetta 152 ms, qemu 1443 ms — Rosetta 63% of native and 9.5× qemu on interpreter work | kept: Rosetta works under the framework |
| 2026-09-05 | framework | a hard `stop` (no guest shutdown) after a `docker load` | the image was gone on the next boot: btrfs `commit=30` and the stop landed inside the window | ruled out: every stop goes through the guest (sync, unmount) |
| 2026-09-05 | framework | S2: Apple's vsock (`VZVirtioSocketDevice`) measured raw from the guest with a 1 MiB-write tool, M5, 4 vCPUs: guest→host 7.4 Gbit/s one stream, 6.3 aggregate over four (1.5–1.7 each), 8.5 with 8 MiB socket buffers; host→guest 7.5 one stream, 12.9 over four; 64-byte ping-pong median 22.6 µs, p99 51, min 14.5; the framework's helper at 220% CPU during a send, our sink at 40% | an order of magnitude under our own device (90–98 Gbit/s) and it does not scale with streams; the helper process is the limit | ruled out as the streams' transport |
| 2026-09-05 | framework | S2: the card as a `VZFileHandleNetworkDeviceAttachment` datagram socketpair at MTU 65535, guest UDP flood to the gateway, frames counted on the host | 1400-byte datagrams 1.85 M/s = 20.7 Gbit/s offered, 20–21 received; 65000-byte datagrams 156 k/s = 81 Gbit/s offered, 82 received — the packet path carries ten times what vsock does; the device negotiates no checksum or segmentation offload | kept: the card is the bulk path |
| 2026-09-05 | framework | S2: TCP over the card terminated on the host in one smoltcp 0.14 thread (8 MiB socket buffers, Nagle off, the responder still answering ARP/DHCP/ICMP on the same loop), guest `--net=host` sender, nft exception for the gateway | guest→host 86.8 Gbit/s one stream, 86.0 aggregate over four; host→guest 44.4 one stream, but 2.5 aggregate over four (a burst-and-drop pattern: the source fills every socket then floods; congestion control off); 64-byte ping-pong over TCP median 33.9–36 µs, p99 52–111, min 21.9, the loop parking in `poll(2)` between frames | kept: streams ride TCP on the card; the host→guest fan-out and the park are Phase 1/3 work |
| 2026-09-05 | framework | S2 decision: vsock dropped altogether — every channel (streams, DNS, the Docker socket, control, memory offers, the file server) is TCP over the card to the gateway address, one transport and no `VZVirtioSocketDevice`, no listener delegates, none of Apple's vsock write-loss behaviour (macOS 26 guests lose bytes on non-blocking vsock writes, reported May 2026) | the round trip is 11 µs longer than vsock's today and closes with a spin before the park; the bandwidth is ten times; the code is one path | kept |
| 2026-09-05 | own disk | S4: fio 4k qd1 on the framework's block devices, M5, 1 GiB file on the btrfs data disk, host cache warm: virtio-blk (caching cached, sync none) randread 60–64k IOPS at 3–4 µs with patch 0008's 30 µs poll, 39k at 22 µs with the poll off (`poll_usecs=0`); randwrite 42k at 7 µs polled, 19k at 48 µs not; seq 1 MiB reads 17 GB/s; NVMe controller (macOS 14+) randread 35k at 25 µs qd1 and 32k at 248 µs qd8 (no scaling), randwrite 20k at 45 µs; `caching automatic` on a file the host had not cached read at 98 µs (the SSD); one queue per device whatever the vCPU count | virtio-blk with host caching on and the guest poll kept; NVMe out; against our own device's record (reads 124k–438k, writes 316k qd1) the framework's disk is two to seven times slower per request, so the own-disk cases are measured in the Phase 1 suite before anything is tuned | kept: virtio-blk, cached; sync mode fsync for durability (measured in the suite) |
| 2026-09-05 | framework | Phase 1, first boot of the moved machine: `lighter-vmm` on `objc2-virtualization` (`vz.rs`), the card a datagram socketpair answered by the responder and a smoltcp interface on the gateway (`link.rs`, the reactor folded in), the agent connecting to the gateway instead of CID 2, the docker socket and control channel as host-opened TCP connections into the guest, published ports through the same, `lighterfs` (kernel patch 0023, FUSE over TCP) for the shares | boot to `INIT ready` 2.0 s; `docker run` pulls through the streams, DNS and ICMP answer, Rosetta runs `x86_64`, a share reads and writes both ways on its first boot; two bugs on the way: `may_recv` is false during the handshake (read as EOF; fixed by checking the peer's FIN states) and a published port's request arrived prefixed with the port header (sockmap hands the join whole socket buffers, so the agent now copies what is queued behind the header by hand before joining) | kept |
| 2026-09-05 | network | the moved machine's first iperf3, M5, release build, 4 vCPUs: published port Mac→container 37.2 Gbit/s, container→Mac 35.0, four streams 38.5/40.3; egress container→Mac 34.2; the debug build read 0.7–0.9 (its profile was smoltcp's software checksum, unoptimised); the card's sends were non-blocking at first and dropped frames into TCP retransmits (fixed: blocking sends) | against 98.5/90.1 (own device) and 97.2/52.9 (OrbStack): Phase 3's first rung | open |
| 2026-09-05 | network | the link's socket rings, release build, M5, published port, one stream: receive ring 1 MiB → container→Mac 35.8 Gbit/s, 4 MiB → 53.7, 8 MiB → 54.7; Mac→container 36.7/39.8/39.2 whatever the send ring; the profile during a transfer: the link thread a third in `write` to the Mac socket, a quarter waiting in `kevent`, a fifth in `recvfrom` from the card, the guest 69% idle — window-bound, not CPU-bound; turning off inbound checksum verification (the link is a memory pipe) moved nothing (35.5/34.0) but stays | receive ring 4 MiB, send ring 1 MiB; the Mac→container path is Phase 3's | kept |
| 2026-09-05 | network | parallel image pulls on the moved machine failed with `TLS handshake timeout` while single pulls worked: the link's per-connection trace showed every stream open, rings empty, nothing pending; the stranded bytes were in the connection's parse buffer — the agent's nineteen-byte header and the first payload (a ClientHello) arrive in one segment whenever the sockmap join is quick, and the reactor took the header and never wrote the rest. The same coalescing had already bitten the other direction in the guest (the port header ahead of an HTTP request) | fixed: what came with the header goes to the Mac socket before the ring; three rounds of the fixture's three parallel pulls in 6.5–8.2 s each | kept |
| 2026-09-05 | framework | Phase 1 gates on the moved machine, M5, debug builds, the daily driver running beside them: m1 boot, m2 devices (the framework's virtio-blk honours discard: the image shrank from 65536 to 16 KiB), m3 network, m3 link (the old vsock gate: a host socket into a guest port), m3 docker (compose, published ports, a named volume), m3 streams (TLS, refused in 1 s, half-close, `host.docker.internal`, 256 MiB each way intact, a thousand concurrent connections, DNS, ICMP), m4 filesystem (the prober's checks, coherence both ways, durability under SIGKILL), m5 speed, m7 amd64 under Rosetta, m8 daily driver (start, six services, a host edit reaching a container, the clock after a sleep, stop) all pass | m5's numbers on the share against the same-session native baseline: a host change reaches the guest in 2 ms, ripgrep 1144% and the metadata walk 380% of native (the page cache and the notifications intact), `npm install` 47% of native against 98% on the old device — the transport's round trip, the first Phase 3 rung | kept |
| 2026-09-05 | memory | the moved machine gave nothing back in m6 (footprint 586 → 3809 MiB, 0 of 3223 returned in 60 s) although the agent offered and the host set the balloon to 7104 MiB: a forced 1 GiB target inflated 63,488 pages (248 MiB, a quarter) with patches 0014 (host-page units) and 0022 (4096 pfns a round), 253,952 (992 MiB, the whole target) with the stock driver | both patches were written against our own device's accounting and the framework's device reads them differently; retired, the stock virtio-balloon driver stays | kept |
| 2026-09-05 | memory | Virtualization.framework's balloon, measured three ways on the M5 (8 GiB guest, stock virtio-balloon driver, `balloon_inflate` confirming the guest handed over what was asked): (1) m6 — 3 GiB allocated and freed in a container, the agent offered, the host set the balloon to 7098 MiB, the guest inflated, the helper's footprint stayed at 3825 MiB for 60 s; (2) 3 GiB written to a tmpfs in the guest and freed, balloon to 7680 MiB, the helper's dirty memory 2238 → 3597 MB and no byte reclaimable (`footprint`: Reclaimable 0, the 8 GiB "Memory Tag 240" region 3.4 GB resident); (3) the same machine under host pressure — 20 GiB allocated on the Mac, free memory down to 66 MiB — the helper still at 3597 MB, so not even lazily | the framework does not return ballooned pages to macOS (Lima #2789 with Apple Feedback FB22614752 on macOS 26; Tart PR #1276 saw the same; "the balloon is a placebo"), and its device offers neither free page reporting nor page hinting; only a VMM on Hypervisor.framework releases guest memory. OrbStack's settled 1 GiB is its own VMM's. On this framework lighter's footprint is its peak, as Colima's and Docker Desktop's are | ruled out: memory cannot come back on Virtualization.framework; a decision for Nick (see the morning report) |
| 2026-09-05 | memory | patches 0014 and 0022 retired for the framework's device were not the cause (both inflate the full target; `balloon_inflate` counts compound pages, so 63,488 units were 992 MiB) | left retired: they were written for our device's 16 KiB accounting and the framework's device gains nothing from them | kept |
| 2026-09-05 | framework | Phase 1 suite on the moved machine, host share, M5, 16 GiB guest, 3 reps, the daily driver running beside it (`benchmarks/results/vz-share.csv`): npm 14.3 s (old device 6.29, OrbStack 8.49), pnpm 8.6 (3.69 / 4.72), yarn 14.1 (5.20 / 7.79), ripgrep 88 ms (79 / 1020), find 90 ms (87 / 595), copy-tree 17.0 s (3.29 / 8.71), rm-rf 7.5 s (2.39 / 2.97), edit latency 2 ms; network: egress 24–34 Gbit/s (98.5 / 97.2), egress-r 35 (90.1 / 52.9), port 34 (97.7 / 54.2), port-r 29 (89.9 / 93.1), UDP 22.7 (5.0 / 3.1), connects 18–20k/s (17.6k / 16.2k), GET 114–124 µs (68 / 73), DNS 59 µs (55 / 251); memory settled = peak = 9368 MiB (the framework keeps the page cache the installs filled); idle 4 ms/s CPU and 78 wakeups/s (5 / 98 old, 2 / 99 OrbStack) | reads, notifications, UDP, connects, DNS and idle hold or lead; the installs and the tree copy are the transport's round trips (Phase 3 rung 1); TCP throughput is rung 2; memory is the framework's | open |
| 2026-09-05 | framework | Phase 1 suite on the moved machine, own disk, M5, 16 GiB guest, 3 reps, noisy (`benchmarks/results/vz-guest.csv`): npm 4.40 s (old device 4.74, OrbStack 7.01), pnpm 1.16 (1.32 / 2.03), yarn 4.03 (4.07 / 5.08), ripgrep 79 ms (79 / 102), find 97 ms (97 / 127), copy-tree 0.83 s (0.94 / 1.11), rm-rf 0.39 s (0.38 / 0.50) | the framework's virtio-blk with the poll patch and the kernel's btrfs patches lead every own-disk row, ahead of the old device too | kept |
| 2026-09-05 | share | the guest transport spinning 50 µs for its own reply (patch 0023 v2, `lighterfs.poll_usecs`), share cases, M5, noisy, host dispatch unchanged (`vz-share-poll.csv`): npm 13.8 s (14.3 without), pnpm 9.5 (8.6) with 19 s of system time, yarn 15.9 (14.1), copy-tree 18.3 (17.0), rm-rf 8.0 (7.5), ripgrep/find/edit unchanged | level to worse: the host's side of a round trip (link thread → worker → link → frame) is longer than the window, so the spin expires and burns; the host is what to shorten first, then the window is worth remeasuring | open |
| 2026-09-05 | share | small, lone requests answered on the link thread (`LIGHTER_FS_INLINE=1`) on the spin kernel, share cases, M5, noisy (`vz-share-inline.csv`): npm 12.9 s (14.3 baseline, 13.8 spin alone), pnpm 8.9 (8.6 / 9.5), yarn 13.6 (14.1 / 15.9), copy-tree see csv, ripgrep/find/edit unchanged | a tenth on npm and yarn, nothing on pnpm; the round trip is still several times the old device's, so the next step is a measurement of where a request's time goes (the server's own histogram, `LIGHTER_FS_STATS`, against the wall), not another guess | open |
| 2026-09-05 | share | per-operation probe on the moved machine against the old device (the daily driver, same files, python in a container): cold read of 2000 small files 33 µs/file (old 38), warm read 33 (old 5.8), stat 0.8 (0.9), create+write+close 160 (23), unlink 115 (91), listdir 1.8 (2.8); the server's histogram for a warm pass is 2000 IOCTLs (python's `isatty` on every `open()`) at 0.4 µs each — one synchronous round trip per file, so the round trip through the framework is ~33 µs with the host answering inline and the guest spinning, where the old device's was 3–5 µs. The S2 TCP echo had read the same (34 µs median, 22 min): the framework's helper sits on both directions of every frame, and its two wakeups a round trip are the floor | on Virtualization.framework a synchronous share request costs ~30 µs; installs make hundreds of thousands, so they trail the old device by that per request and reach OrbStack only by making fewer requests (setattr coalescing, an in-kernel ENOTTY for terminal ioctls on regular files, negative dentries); own-disk work is unaffected and ahead | open |
| 2026-09-05 | framework | is OrbStack on Virtualization.framework? `otool -L`, `codesign -d --entitlements`, `nm -u`, `strings`, a `sample` of the running helper and the process table with OrbStack running | its helper links both frameworks and holds both entitlements, but runs a libkrun-descended Rust VMM on Hypervisor.framework: `hv_vm_create/map/unmap/protect`, `hv_vcpu_run` in the sampled stacks, its own `virtio/balloon/device.rs`, `virtio/net/device.rs` and `virtio/fs/rosetta.rs`, guest RAM a VM_ALLOCATE region inside the helper, no `com.apple.Virtualization.VirtualMachine` process; the framework classes it links (`VZLinuxRosettaDirectoryShare`, `VZVirtualMachine`, a `VzSpec`) are dormant, kept for the Rosetta installer. Its developer, in orbstack/orbstack#2230: "a custom virtualization stack written from scratch in Rust, not Apple's Virtualization framework", and on HN: dynamic memory is "our biggest perf/resource gain … nothing else supports this, including Containerization" | the two properties we wanted, memory return and Rosetta, coexist only on an own VMM |
| 2026-09-05 | rosetta | what Rosetta asks its file server, observed inside the framework machine: `strace -e ioctl` of `/mnt/rosetta/rosetta` running an x86-64 busybox, then a 20-line C probe issuing the same ioctls against the `rosetta` file | three ioctls of type `0x61` on the binary itself (opened as `/proc/self/exe`): `0x25` (READ, 69 bytes) and the 2022 `0x22` both return the constant "Our hard work / by these words guarded / please don't steal / © Apple Inc\0"; `0x23` (READ, 128 bytes) returns zeros; `0x24` (void) returns success; result value 1. Identical across runs and independent of the buffer's prior contents (0x00, 0xff). Apple's server answers only when the descriptor is the Rosetta binary (the probe's own `/proc/self/exe` got -1). The 69 bytes are in the user's installed `rosetta` binary at offset 60169, right after an `\x7fELF` string, no NUL before it | no challenge–response and nothing to disassemble; lighter-fs can answer this, reading the constant out of the user's own copy (recognised by SHA-256) so that no byte of Apple's ships |
| 2026-09-05 | rosetta | Rosetta on our own VMM (d3c25ed plus: `Server::serve_rosetta` answering the four ioctls for the binary's inode only, `rosetta::key()` finding the constant in `/Library/Apple/usr/libexec/oah/RosettaLinux/rosetta` among NUL-terminated strings of 68 bytes, the share tagged `rosetta`, `--tso` setting `HV_SYS_REG_ACTLR_EL1` on every vCPU) | `strace` inside the guest shows the same three ioctls answered with 1 and busybox runs; with `x86_64` registered through binfmt (`OCF`) `docker run --platform linux/amd64 python:3.12-alpine uname -m` is `x86_64`; python 3M-iteration loop 152 ms (framework 152, native here 80 against the framework's 96), `hashlib.sha256` 4 KiB 151k/s (framework 145k, native 658k) | kept: the same translator, the same numbers, on the machine that returns memory |
| 2026-09-05 | rosetta | TSO proved rather than assumed: a message-passing litmus (writer x=1;y=1, reader y then x, pinned to two vCPUs, 2M rounds a run) native in the guest with `--tso` and without | with TSO 0 reorderings in 4M rounds; without, 0 and 4 in 2M each — the bit the hypervisor exposes changes the memory model. VM-wide TSO's cost on native arm64: memcpy 31–38 GB/s against 45–48 (about a quarter), mutex ping-pong 2.31 vs 2.40 µs, contended atomic 97–100 vs 96–99 Mops/s, pointer chase 66–67 vs 65–76 ns, python loop 80 vs 78–83 ms, sha256 658k vs 663–665k | VM-wide TSO costs bulk copies a quarter; it has to be per thread |
| 2026-09-05 | rosetta | does Rosetta ask for TSO itself? `strace -e prctl` of Rosetta on the TSO-off machine | `prctl(0x4d4d444c /* PR_SET_MEM_MODEL */, 1 /* TSO */)` twice, answered EINVAL by our kernel: Rosetta follows the April 2024 prctl (Hector Martin's series, Apple's own patch) and expects the kernel to switch its threads' ordering | kernel patch 0023: `PR_SET_MEM_MODEL`/`PR_GET_MEM_MODEL` on Apple silicon flipping `ACTLR_EL1` bit 1 per thread (inherited on clone, reset on exec, written only when the incoming thread's setting differs from the CPU's last, writability proved at boot); the VMM leaves the bit alone, `--tso` stays for A/B |
| 2026-09-05 | framework | decision: back onto our own VMM (2ef301e reverts 742576a, c3d3c79, f0b6146; the framework lives on as branch `framework`) | the framework's two costs were structural (memory never returned, ~33 µs per synchronous request) and its one benefit, Rosetta, needs neither the framework nor anything of Apple's: a constant the user already has and one hypervisor bit | the plan's addendum: Rosetta on the own VMM proven, the machine put back, docs, the remaining OrbStack rows, 0.3.0 |
| 2026-09-05 | rosetta | per-thread TSO on the machine (kernel patch 0023, the VMM leaving the bit alone): boot log, `strace -e prctl` of Rosetta, the message-passing litmus compiled and run as x86-64 under Rosetta and natively, the native microbenchmarks again | "Apple TSO: available per thread (PR_SET_MEM_MODEL); boot value off" — the guest's write to `ACTLR_EL1` takes effect under Hypervisor.framework; Rosetta's two `prctl(0x4d4d444c, 1)` return 0; x86-64 litmus under Rosetta 0 reorderings in 4M rounds, native arm64 3 and 1 in 2M each; native memcpy 50–59 GB/s (31–38 with the machine-wide bit), ping-pong 2.25 µs, atomic 95 Mops/s, chase 67 ns; Rosetta loop 161 ms, sha256 147k/s. `node:22-alpine` (`process.arch` x64, a 10M loop) and `postgres:16-alpine` (200k rows inserted and summed) amd64 images run | kept: Rosetta's threads get x86 ordering, nothing else pays |
| 2026-09-05 | network | the two cuts named by the 2026-09-04 egress sample: the device poller reads the clock only on an empty iteration (it read it on every one, a quarter of its busy samples), and the vsock device broadcasts its progress condvar only while a thread is counted waiting on it (a `__psynch_cvbroad` syscall per packet before). A/B on the M5, noisy (daily driver up), 3 reps, `ab-base.csv` / `ab-cuts.csv` | medians, Gbit/s: out of a published port 72 → 79, into one 74 → 87, container → Mac 58 → 85, Mac → container 77 → 79; GET median 73 → 75 µs, p99 195 → 226 (noise-level both) | kept: every guest → Mac path gains 10–45%; the quiet-machine record is still to be taken |
| 2026-09-05 | boot | a start-up case in the suite (`boot`, last in the list): from a cold stop, the runtime started the way a person starts it (`lighter start` on a home of its own, `orb start`, `colima start`, opening Docker Desktop), until `docker version` answers and until the first container has run, after an untimed round that pays for the image pull; the report and README gain a "Starting up" table. First readings, M5, noisy, 2 reps (`boot-try.csv`, `boot-orb.csv`) | lighter 2.5–2.7 s to docker, 3.1–3.3 s to the first container; OrbStack 1.1–1.2 s and 1.5 s | a row we lose by two to one today; the machine itself reaches `INIT ready` in ~1.6 s and `dockerd` takes a second of that — Phase D's list gains it |
| 2026-09-05 | boot | where `lighter start`'s 2.5 s went, from the host log's clock, the guest's `/proc/uptime` stamps on the INIT lines (new), and dockerd's own log read out of the guest | CLI to machine started 73 ms; kernel to init 0.65 s, of which 0.55 s the raid6 library benchmarking nine algorithms at sixteen jiffies each (`CONFIG_RAID6_PQ_BENCHMARK`); init to `INIT ready` ~0.3 s; then dockerd: 1041 ms between "started new containerd process" and the graph driver — dockerd polls its managed containerd once a second and always misses the first poll — then 500 ms of its own set-up (114 ms failing IPsec module probes, 270 ms the bridge); init's own poll for dockerd's socket once a second; the CLI's poll for docker every 200 ms | four changes: the benchmark off in the kernel config; containerd started by init the moment the data disk is mounted, dockerd pointed at it (`--containerd`), the CRI plugin off; init polls dockerd every 50 ms and the CLI polls docker every 20 ms; `CONFIG_NF_CT_NETLINK`, `XFRM_USER`, `INET_ESP/AH` so dockerd's conntrack deletes and IPsec probes succeed instead of failing slowly |
| 2026-09-05 | boot | the same starts after the changes, M5, noisy, private home, image present | kernel to init 0.05 s; `INIT ready` at 0.28–0.31 s; dockerd's set-up 390 ms; `lighter start` 785–884 ms to docker answering (2.5 s before; OrbStack 1.1–1.2 s); the first `docker run --rm alpine true` +560–630 ms (OrbStack's whole boot-to-first-container is 1.5 s against our 1.4–1.5) | kept; the row is ours on the first reading and level on the second, and a container's own run — create 96 ms, start 220–280, remove 160–240 — is the next lever |
| 2026-09-05 | stop | `lighter stop` sent SIGTERM and the machine `_exit`ed: an image pulled just before was gone at the next start ("layer does not exist" — btrfs commits every 30 s and dockerd does not fsync layers). Now `stop` asks the guest's agent to power off (engine down, sync, umount, reboot) and signals only a guest that does not answer; the agent says on the console what the stop took | images survive a stop; the stop takes 5.3 s: dockerd logs "Daemon shutdown complete" at once and "Error shutting down http server: context canceled" five seconds later — its API server waits out a 5 s grace on one active connection | find and close the connection before the signal |
| 2026-09-05 | stop | the connection dockerd waited on: `ss -xp` in the guest showed four host-originated connections into `/run/docker.sock` besides the probe's own; the one that is never idle is `lighter run`'s port watcher following `/events`. Now `lighter stop` sends the machine process SIGUSR1 first — a self-pipe wakes a thread that shuts the event stream down and the watcher does not reopen it — waits 150 ms, then asks the guest to power off | `lighter stop` 491–502 ms (5.3 s before); the agent's console line: dockerd exited after 100 ms, the whole power-off 130–139 ms | kept |
| 2026-09-05 | container | why `docker run --rm alpine true` took 560 ms here and 250–290 under OrbStack, on the same Mac, same engine generation (Alpine 3.22's Docker 28.3 / containerd 2.1 / runc 1.3 changed nothing): the guest's `/proc/stat` over five runs showed 410 ms of CPU and two seconds of iowait — a container run is waiting on disk. `runc run` itself is 10–30 ms; fio in the guest: direct 4 KiB writes 4 µs, but write + fsync 8.8 ms an op (OrbStack 0.5). The block stats: ~80 flushes a run at 3.7 ms each. The host's `fsync` of the image from a test program: 0.04 ms. The device's own flush, traced (`LIGHTER_BLK_TRACE`): 3.7 ms mean — because Rust's `File::sync_data` on Apple platforms is `F_FULLFSYNC`, a drive-cache commit measured at 3.9–4.6 ms, not the `fsync` the code's comment believed it was | `libc::fsync` in `Disk::flush`: guest fsync 0.17 ms an op (5,743 a second; OrbStack 2,002), `docker run --rm alpine true` 173–188 ms, `lighter start` 659 ms to docker and +147 ms to the first container (OrbStack 1.1–1.2 s and +350) | kept: every flush the guest ever issued — image pulls, layer commits, databases, `fsync` in any container — was paying this |
| 2026-09-05 | container | what the guests' kernels cost for the paths a container start takes, the same script in both: netns + veth create 44 vs 32 ms, delete 49 vs 36, a cgroup mkdir/move/rmdir 3 vs 0 ms, an `iptables -C` 600 vs 180 µs, fork+exec 100 vs 145 µs (ours the faster); `rcupdate.rcu_expedited=1` on our cmdline moved a container run from ~480 to ~450 ms, within noise | the kernel paths were never the 2× gap; the cgroup rmdir and the netns teardown are a later rung | recorded |
| 2026-09-05 | power | the idle cost attributed on a quiet 4-vCPU bench guest, a minute after settling: host 5.0 ms/s, all of it in the four vCPU threads (60–90 ms a minute each), nothing in the device or stream threads; the guest's own accounting 4.8 ms/s of CPU over ~85 timer interrupts and ~23 reschedule IPIs a second — some 40 µs of host CPU per wakeup. The guest's poll-before-WFI off (`idle.poll_ns=0`): 4.3 ms/s, so the window is not it (it halves on every long sleep, as designed). Stopping dockerd and containerd inside the guest read 11.3 ms/s because the host's port watcher then reconnects twice a second — not a measurement | the cost is the wakeups themselves at ~40 µs each against OrbStack's ~20 (2 ms/s at 99 wakeups); the sources are the two Go daemons' timers (hrtimer_wakeup entries, tick_nohz_handler) and kernel workers (mm_percpu_wq, rcu, kvfree_rcu) | parked: the remaining lever is the per-wakeup cost of a vCPU leaving and re-entering WFI under the hypervisor, or fewer guest timers; both are their own rung |
| 2026-09-05 | kernel | `CONFIG_HZ=100` against 250, the M5, noisy, the same runs: GET on a kept connection 108–139 µs median (65 at 250), p99 242–542 (125–299), connects 15–18k/s (17–19k), `docker run --rm alpine true` 350–507 ms (173–188), `lighter start` 1.07–1.12 s (0.66); idle 3.2 ms/s (5.0) with 63 timer interrupts a second (85) | the jiffies-granular waits in the block flush machinery, the scheduler and the network stack are inside every one of those paths; the tick rate is a latency knob before it is a power knob | rejected; HZ=1000 next, for the same reason in reverse |
| 2026-09-05 | kernel | `CONFIG_HZ=1000` against 250, the M5 now quiet (Nick off it), the same runs: GET on a kept connection 66–75 µs median (65), p99 188–370 (125–299, still noise-shaped), connects 17.4–17.8k/s (17–19k), `docker run --rm alpine true` 86–115 ms (173–188; OrbStack 250–290), `lighter start` 444–455 ms to docker (660; OrbStack 1.1–1.2 s) and +97 ms to the first container (147; OrbStack +350); idle 4.2 ms/s (5.0) at 90 timer interrupts a second | the same jiffies-granular waits that made HZ=100 twice as slow make 1000 faster than 250; idle does not pay because the tick stops on idle cores | kept |
| 2026-09-05 | network | UDP from a container read 0 in the M5 record (5.0 Gbit/s the day before, and 5.0 in the M1's run of the pre-Alpine-3.22 build). Bisected on one kernel with two root filesystems: Alpine 3.21 carries UDP, 3.22 does not. In the 3.22 guest the divert chain counts the datagrams (`nft` trace: the tproxy rule accepts, the mark is set, nat prerouting passes, then no input hook, no forward hook), the agent's UDP socket sees no syscall at all, routing and every IPv4 sysctl are identical to 3.21's. The difference: `net.bridge.bridge-nf-call-iptables` is 1 in the 3.22 guest and 0 in 3.21's. Init turns it off once dockerd's socket exists, but Docker 28 creates its socket before it sets up the bridge and turns the knob on afterwards; turning it off by hand restores UDP (3.35 Gbit/s under a kernel build), and a `docker network create` turns it on again — which would have broken UDP on 0.2.0 too, for anyone whose compose file makes a network | with br_netfilter on, the IP prerouting hooks run in bridge context for a container's frames, where the socket TPROXY assigns does not survive into routing (init's own comment, from the day it was first found); a knob Docker rewrites cannot be the fix | `CONFIG_BRIDGE_NETFILTER` out of the kernel: published ports are DNAT on routed traffic and network isolation is on the forward hook, so Docker loses only a container reaching another's published port through the host's own address on the same bridge |
| 2026-09-05 | network | the kernel without `CONFIG_BRIDGE_NETFILTER`: Docker 28 would not start — "cannot restrict inter-container communication or run without the userland proxy: stat /proc/sys/net/bridge/bridge-nf-call-iptables" — because init ran it with `--userland-proxy=false`, chosen when Alpine's package had no docker-proxy. Alpine 3.22 ships it, so the proxy is left on: Docker then neither needs nor can enable br_netfilter | dockerd up in 50 ms; UDP from a container 4.89 Gbit/s, and 4.98 after a `docker network create`; a published port answers from the Mac (DNAT, as before); a container reaching another's published port through the guest's own address — hairpin, which the knob being off had quietly broken on 0.2.0 — now answers through docker-proxy; TCP egress unchanged | kept |
| 2026-09-05 | record | the M1's own results for OrbStack, Colima, Docker Desktop and native (three hours, 05:15–06:46) were overwritten: a `git add -A` on the M5 had swept the M5 record's CSVs into a commit, and the `git checkout -- benchmarks/results` run on the M1 after syncing the tree put those over the M1's fresh files. Rebuilt from the M1's harness logs, which print every repetition: 61 rows for OrbStack, 58 Colima, 55 Docker Desktop (share only — its own-disk stage produced nothing; that column keeps the previous day's record), 42 native; the GET p99 rows are the one thing the log does not carry and are re-measured separately | the M1 tables regenerate from them | a record's CSVs are never in the working tree of a machine being synced: `benchmarks/results/*` stays excluded from rsync and a remote `git checkout` of it is never run again |
| 2026-09-05 | kernel | HZ=1000 against 250 on both machines, back to back, plus the M1's full re-record on 1000: the M5 (18 cores, 9 vCPUs) gains everywhere at 1000 — on the quiet A/B/A npm 6.2–6.6 vs 7.7–7.9 s, GET 62–64 vs 71–78 µs, TCP into a published port 81–86 vs 68–80 Gbit/s — and the M1 (8 cores, 8 vCPUs) loses on most rows: own-disk npm 9.0–11.2 s (record 7.9–8.4), `rm -rf` on the share 4.7–9.9 s (2.7–4.0), TCP into a published port 16–33 Gbit/s (37–41), GET p99 255–1068 µs (199–311). Every tick is a VM exit (`HV_EXIT_REASON_VTIMER_ACTIVATED`), so the rate is a per-vCPU cost where the vCPUs fill the cores | the 1000 gain on a container run came from jiffies-granular waits: expedited RCU at 250 (now written by init to `/sys/kernel/rcu_expedited`) takes `docker run --rm alpine true` from 145–173 to 102–135 ms (1000: 83–90) and the cgroup cycle from 3 ms to 0, with own-disk installs level (pnpm 1.11–1.20 vs 1.17–1.23 s, npm 4.4 vs 4.6) and idle 4.2 ms/s; what remains is the veth/netns create and delete, 38 and 41 ms at 250 against 14 and 18 at 1000, not RCU | HZ=250 with expedited RCU; the veth path is the next rung on the container row |
| 2026-09-05 | record | the release records on the final build (HZ=250 + expedited RCU, no br_netfilter, Alpine 3.22, `fsync` flush): the M1's full lighter re-record (07:50) and the M5's (07:43–07:53, daily driver up, `--allow-noisy`). The M5's network stage was re-run once on the quiet machine (08:01) because the record's ran straight after the gate chain and the storage stage and read GET 71/93/99 µs, idle 13 ms/s at 352 wakeups; quiet: GET 63/63/62, p99 133–152, idle 6 ms/s at 126 wakeups, the same as the 07:10 A/B legs (62–67), so the quiet stage is the record and the noisy one is the note here. M1 native GET p99 (144/106/92) measured separately, the one cell the log reconstruction could not recover | M5 against OrbStack: own disk and share won on every row (npm 4.46 vs 7.01 s, share npm 6.41 vs 8.49, `cp -a` 902 ms vs 1.11 s); memory peak 4168 vs 5498 and 850 vs 2114 MiB a minute after, settled 1131 vs 1028; TCP 93/82/91/87 vs 97/53/54/93 Gbit/s, GET 63 vs 73 µs, p99 144 vs 119, DNS 63 vs 251; idle 6 vs 2 ms/s; start 0.4/0.5 vs 1.1/1.5 s. M1: own-disk npm 7.78 vs 9.67 (native 7.81), pnpm 1.80 vs 2.40, `cp -a` 3.73 vs 2.49 (lost), yarn level; share `rm -rf` 2.54 vs 3.92, `cp -a` 5.28 vs 16.13, npm 11.62 vs 11.25 (lost by 3%); settled 800 MiB (was 2353) against OrbStack's 3906, which had not returned its previous stage's memory when read; idle 10 vs 20 ms/s; TCP out 40 vs 65 (lost), in 42.5 vs 29; GET 133 vs 127; start 0.5/0.7 vs 1.2/1.5 s | committed as the 0.3.0 record; open rows: M5 settled memory and idle CPU, M5 GET p99, M1 egress and own-disk `cp -a` |
| 2026-09-05 | benchmarks | Docker Desktop's start-up row timed out on both machines: the harness quit the app, waited for `com.docker.backend` to go (90 s cap), slept 5 s and opened it again, and docker never answered. Probed by hand on 4.89: Quit takes the window and the VM down at once, but the backend, `com.docker.build` and `docker-agent` stay for 190 s, an `open -a Docker` while any of them is alive is swallowed, and when they exit the app is simply down | `boot_stop` now gives the orderly exit 20 s, ends the rest of the `/Docker.app/` tree, waits for none left, and opens from a Mac with no Docker process: M5 2185/2474, 2101/2387, 1830/2098 ms (docker / first container), M1 2657/2940, 2962/3323, 2505/2846 | kept; Docker Desktop is in the start-up table |
| 2026-09-05 | release | 0.3.0 head f6b7195: guest rebuilt for the agent's version, release binary signed, `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test --all` clean, gates m1–m8 green (m5 beside the daily driver with `LIGHTER_BENCH_ALLOW_NOISY=1`); the speed gate rewrote `benchmarks/results/lighter.csv` with its pass numbers, restored from the record commit before the version commit. PR dev → main #1 raised | the tarball is not built: `scripts/package-release.sh` needs the Developer ID certificate and the App Store Connect key from 1Password, `op` is signed out, no cached `.context` credentials, no identity in the login keychain — a fingerprint away, not a code problem | tarball, GitHub release, formula and tap, tag v0.3.0 remain, from this head |
| 2026-09-05 | release | 0.3.0 shipped from f6b7195: `scripts/package-release.sh 0.3.0` signed with the Developer ID and Apple accepted the notarization (submission 22c225bd); `dist/lighter-0.3.0-arm64.tar.gz` 80 MB, sha256 `efac26746b3466c2fbee11caad203ce0a2fbb7db433135c8ef1a73e300242f4e`; tarball smoke on the M5 under a private `LIGHTER_HOME` (doctor, start 0.39 s, arm64 and amd64 containers, stop 0.47 s, daily driver and docker context untouched); tag `v0.3.0` on f6b7195; GitHub release with the asset (checksum re-read from the download); formula in `packaging/lighter.rb` (b805a2d) and on the tap's PR branch, PR fieldwork-ai/homebrew-tap#1 retitled to 0.3.0 with 0.2.0 kept as its first commit; `brew install fieldwork-ai/tap/lighter` from that branch on the M1 (never had lighter): doctor, start, arm64 and amd64 (Rosetta), stop, then uninstalled | Homebrew 6 refuses `brew install ./formula.rb`, so the formula test is the tap clone checked out at the branch; the M1's tap clone had a conflicted index from the 0.2.0 night, reset to `origin/main` first; the M1's plain ssh shell has no docker on PATH (`/usr/local/bin`) | released; left for Nick: approve and merge the tap PR (branch policy) and lighter PR #1 dev → main; the tap's `main` serves 0.1.0 until then, whose tarball no longer exists |
| 2026-09-05 | lighter-fs | CI red on f7d88f7, a worklog-only commit: `a_parked_directory_is_not_revived_by_creating_inside_it` counted 17 revivals against its bound of 16. `REOPENS` is a process-wide counter and `LIGHTER_FS_FD_BUDGET` a process-wide variable, and cargo runs the test threads in parallel, so another reclaim test's revivals (or its budget) land in this one's window | the five tests that lower the budget or count revivals hold one `Mutex` for their duration (`with_budget`, `reclaim_alone`); five local runs green | kept; not a product change, 0.3.0's tarball is unaffected |
| 2026-09-05 | benchmarks | the memory table's first row was "settled, before an install": the footprint five seconds after the warm-up's three installs, before the guest trims anything (1131 MiB on the M5, against 850 a minute after the timed install). A fresh idle boot reads 650 MB on the M5 and OrbStack's 1150 under the same protocol, and neither number was in the table | the boot case now cold-starts once more after its timed reps, runs nothing, waits a minute and reads the footprint (`memory-idle`); the memory case keeps peak and the two after-install readings and no longer records a first row. Re-measured on both machines: idle M5 lighter 635, OrbStack 1197, Colima 11361 (its VM is sized 16 GiB there, 4 on the M1), Docker Desktop 2111 MiB; M1 lighter 362, OrbStack 733, Colima 1145, Docker Desktop 1753. Start-up with it: M5 lighter 403 ms, OrbStack 1544 (a second pass; the first had one rep at 2066 while the docker context was being changed under it, the others 1258/1151), Colima 11.8 s, Docker Desktop 2.09 s; M1 461 ms, 1211, 9753, 2729 | kept; the row is now what its label says |
| 2026-09-05 | benchmarks | the speed gate wrote `benchmarks/results/native.csv` and `lighter.csv`, the record's own files, and replaced the quiet-machine numbers with its pass numbers the morning of 0.3.0 | it measures under `--label gate-native` / `gate-lighter`, git ignores `gate-*.csv` | kept |
| 2026-09-05 | cli | the machine's flame did not show in Activity Monitor for a release install: the bundle carries the icon and the plist names it, but Launch Services knows only bundles it has been shown, and one under `~/.local` or a Homebrew cellar never is; `lsregister -f` on the bundle made the icon appear at once | `bundle::ensure` registers the bundle it returns, in the background, once per bundle path (marker `bundle.registered` in the home) so a start pays nothing for it afterwards | kept |
| 2026-09-05 | record | the M1's lighter re-record (3c, 07:06–07:50) shared the machine with the HZ A/B run there 07:14–07:28: its network stage (07:10–07:20) and the start of its own-disk stage ran beside it. Found when a probe of egress on the idle M1 read 53 Gbit/s three times against the record's 40 | both stages re-run alone (09:42–09:5x): egress 52.6/53.8/54.1 (record 40.4), egress-r 42.3/17.9/34.7 (that direction is jittery on the M1, a lead), port 43.1/44.3/33.9, port-r 54.6/54.4/55.1, GET 137/133/127, p99 kept by the CSV, DNS 153, idle 10 ms/s at 103 wakeups; own disk npm 8711/7947/8739 (record 7.78 s: the quiet number is worse, kept all the same), pnpm 1.78, yarn 7.92, `cp -a` 3.08 (record 3.73), `rm -rf` 606 | the quiet stages are the record; the M1 egress gap to OrbStack is 54 vs 65, not 40 vs 65 |
| 2026-09-05 | guest | qemu out of the rootfs: it ran x86-64 at a tenth of native (`docs/x86-64.md`), which reads as broken rather than slow, and shipped an emulator in every image for Macs that had not installed Rosetta. Without a handler an amd64 image fails with "exec format error", which says nothing | the binfmt entry without Rosetta is a hard link of the agent (`/sbin/lighter-noamd64`, `F` flag) that recognizes itself by `/proc/self/exe` (binfmt puts the program's path in argv[0], not the interpreter's) and prints `lighter rosetta --install`, exit 126; doctor reports a missing Rosetta as a fault with that fix; m7 checks the message on a Mac without Rosetta and Rosetta itself on one with it. Tested both ways: a guest booted without the share prints the hint, m7 green with Rosetta | kept; Rosetta is the only x86-64 path |
| 2026-09-05 | benchmarks | the x86-64 claim had no table behind it: two Rosetta microbenchmarks against qemu, nothing against the other runtimes. Added `--arch amd64` to the harness (the image built and run `--platform linux/amd64`, its own cache volumes, `<target>-amd64.csv`), the bench VM given the Rosetta share the CLI attaches (its first amd64 image build hit the new no-Rosetta hint), and two cases that run under either architecture: `cpu-sha256` (a gigabyte of zeros through `sha256sum`, no disk or network) and `container-start` (`docker run --rm alpine true` from the host). Smoke on the M5, lighter own disk: sha256 2.99 s arm64 / 4.10 s amd64, container start 137–175 / 145–162 ms, pnpm 1.1 / 2.7–2.9 s | the README gets an "x86-64 images" table per machine: lighter's arm64 number for scale, then each runtime's amd64 number; records running on both machines with each runtime's binfmt handler printed (OrbStack and Docker Desktop under Rosetta, Colima started `--vz-rosetta`) | kept |
| 2026-09-05 | container start | the remaining tick-bound cost at HZ=250: `docker run --rm alpine true` 110–150 ms against 83–90 at HZ=1000, the veth and netns waits. Read the 6.18 teardown: `netdev_run_todo` does `rcu_barrier()` after `rtnl_unlock`, `netdev_wait_allrefs_any` another plus `msleep(1)` while anything holds the device, and `rcu_barrier` is a normal grace period whatever `rcu_expedited` says; the grace-period kthread waits `jiffies_till_first_fqs` (1 jiffy, 4 ms) before its first scan. A/B/A in one boot on the M5, the parameter being writable at run time: run medians 151 → 131 (first_fqs=0) → 158 → 126 (first and next 0) | about 20 ms of a container's life is that first wait; `next_fqs=0` would spin the kthread through long grace periods, so the candidate is `first_fqs=0` alone, written by init beside `rcu_expedited`. Second pass queued: the netns steps with a millisecond clock (busybox `date` has no `%N`, the first pass read 0) and idle cost under the setting | pending the second pass |
| 2026-09-05 | container start | second pass on `jiffies_till_first_fqs`, with coreutils' `date` for the netns steps and powermetrics under each arm: run medians 150 → 140 → 149 ms; veth delete (six cycles each) 32–54 → 19–39 → 39–71 ms, medians about 45 → 31 → 56; netns create too noisy to read (27–83 ms either way, five `ip` execs inside); idle 6.28 vs 6.41 ms/s, 157 vs 164 wakeups, no cost | init writes `0` to `/sys/module/rcutree/parameters/jiffies_till_first_fqs` beside `rcu_expedited`; `jiffies_till_next_fqs` left at 1 (zero would spin the grace-period thread). Rootfs rebuilt and the `container-start` case re-run on it | kept; 10–20 ms off a container's life, the rest of the gap to HZ=1000 is `msleep(1)` in the refcount wait and the tick itself |
| 2026-09-05 | benchmarks | which x86-64 handler each runtime used for the table, read from the amd64 process's own `/proc/self/maps` (`/proc/self/exe` names the program under these binfmt flags): lighter `/mnt/rosetta/rosetta`, OrbStack `/mnt/rv/[rosetta]`, Colima `/mnt/lima-rosetta/rosetta` (started `--vz-rosetta`), Docker Desktop `/run/rosetta/rosetta` | all four under Rosetta on the M5; the table compares translators' hosts, not translators. OrbStack's `sha256sum` at 8.0 s against 4.2–4.4 for the other three is under Rosetta too; its user time equals its wall time where the others overlap the pipe's two processes | noted in the README intro |
| 2026-09-05 | M1 network + own disk | one suspect for both M1 gaps (egress 54 vs 65 Gbit/s, port-out 55 vs 68, `cp -a` 3.1 vs 2.5 s): eight vCPUs on eight cores, and every host poller spins 200 µs after a request, a core the guest wanted. A/B/A/C of `LIGHTER_HOST_POLL_US` on the M1: 200 → egress 53.9/52.9/36.6, port-out 55.7/55.9/55.2, copy 1.85/1.71/1.74 s; 50 → 47.5/47.5/51.2, 56.0/56.8/56.1, 2.18/1.95/1.78; 200 again → 52.7/53.0/53.6, 55.7/54.8/36.7, 1.83/2.59/1.75; 0 → 27.1/27.2/27.1, 53.4/52.0/53.7, 2.25/2.83/3.06 | the spin is worth its core on the M1: shorter windows lose egress and the copy, none halves egress. Not the lever. Side finding: `cp -a` alone after a fresh materialize reads 1.7–1.9 s where the record's sequence read 3.1–3.7, so the copy's cost depends on what the disk went through before it; a one-boot sequence (copy, two installs, `rm -rf`, copy again) is queued | poll window stays 200 µs; item 7 moves to disk state |
| 2026-09-05 | own disk | why `cp -a` on the M1's own disk reads 1.7–1.9 s alone and 3.1–3.7 in the record: one boot, `copy-tree` three times with churn between (two installs, an `rm -rf`, a ripgrep): 4501/2544/2081, 3837/3661/2011, 3626/2791/1727 ms. Every triplet is slow first and fast by the third; the standalone run that read 1.7 had no warm-up installs before its copies | the copy is slow into space the guest has never written and fast into space its previous repetition freed. The suspect is the host: the data image is sparse, and a write into a hole is an APFS allocation before it is a write. A first-touch against overwrite test with `dd` on the M5 is queued | pending |
| 2026-09-05 | own disk | first touch measured three ways. In the guest, on the data disk through btrfs (M5): a new 1 GiB file 493 ms, the same file overwritten in place 294, a second new file 606, a new file after freeing one 221, another new file 629. On the host alone, into the same kind of sparse file: 256 MiB of 1 MiB writes 18 ms into a hole against 16 overwriting, 16k random 4 KiB writes 131 against 96. And APFS's `F_PREALLOCATE` takes only `F_PEOFPOSMODE` (past the end of file); `F_VOLPOSMODE` at any offset inside a sparse file is EINVAL, so a hole cannot be reserved ahead | the guest pays 2–3x for space it has never written; the host's allocation is not where that goes (a third at most, on small writes). What remains is btrfs allocating a fresh block group, or the VMM's path into fresh regions; a raw `dd` to the device at untouched offsets, direct, is queued to split the two | pending |
| 2026-09-05 | own disk | raw `dd` to the data device, direct, at offsets never written: 1 GiB in 1 MiB writes 131 ms first touch against 108 again and 116 at another fresh offset; 256 MiB in 4 KiB writes 184 against 120 and 185. Through btrfs the same gigabyte had cost 493–629 against 221–294 | the VMM-and-host path is a fifth on large writes and half again on small ones; btrfs's fresh block group is the larger part for large writes. APFS cannot reserve a hole ahead (`F_PREALLOCATE` is end-of-file only), so the host's share could only be prepaid by zero-filling chunks ahead, which buys a quarter of the small-write penalty at the price of disk usage growing in steps | parked: the M1 `cp -a` row is a 0.6 s gap made of two small pieces; noted, not taken |
| 2026-09-05 | idle | who wakes an idle M5 guest, per thread over 60 s after a quiet minute (the first pass counted main threads only and missed Go's timer threads): containerd 23.3 context switches/s, dockerd 3.6, the agent 2.4, containerd-shim 1.7, rcu_sched 2.5, kworkers about 8 (`mm_percpu_wq` once a second per CPU, `events_power_efficient`); timer interrupts 87/s across nine vCPUs, IPIs 22/s, and 24/s on `virtio7`, which is the balloon; `vm.stat_interval=10` took 4 wakeups off. A knockout that killed dockerd read higher, not lower: the host's port watcher reconnects in a loop without it, a flawed design | two levers: the balloon's 24 interrupts a second at idle are the host's own doing, and containerd's 23 wakes a second are a Go runtime asleep at nothing (dockerd, the same runtime, wakes 3.6 times) | next: what the balloon is doing 24 times a second |
| 2026-09-05 | M1 network | `sample` of the VMM during egress on the M1 (3 s, 1831 samples a thread): the vsock poller's thread at the top of its spin loop in every sample, with 16% of its samples in `mach_absolute_time`/`clock_gettime` and 20% in `__psynch_cvbroad`; the reactor 75% inside `writev` (the Mac kernel copying into the socket); vCPUs 1.35 threads' worth in `hv_trap`. The broadcasts were for one permanent waiter: the memory policy's guest reader sits in a 100 ms `wait_progress` on its own port for the machine's life, and `waiters` was one count for everyone, so every batch of anyone's stream paid a condvar broadcast and woke that thread | waiters are counted per connection (`Conn::waiters`), senders out of outbox room separately (`room_waiters`); the tx drain broadcasts only when a packet landed on a connection with a waiter, delivery into the guest only when a sender wants room, resets and shutdowns as before. Build, clippy and the 27 vsock tests clean. A/B/A/B on the M1: egress 52.1/53.0/53.3 → 54.7/57.5/57.9 → 51.9/52.2/52.2 → 56.3/56.9/56.9 Gbit/s; port-out 55.2/53.5/55.0 → 56.3/55.7/55.5 → 53.4/53.9/38.7 → 55.3/55.1/55.3; GET 140/129/125 → 126/125/123 → 134/131/128 → 126/125/126 µs | kept: +8% egress and 6 µs off a GET on the M1 for a broadcast that woke nobody who cared |
| 2026-09-05 | M1 network | streams on two threads each (`LIGHTER_STREAM_THREADS=1`) against the reactor, A/B/A on the M1: egress 54.1/53.4/53.3 → 33.0/35.9/34.8 → 52.5/52.7/53.8 Gbit/s, port-out 55.6/55.2/55.2 → 38.5/39.6/38.6 → 55.1/55.3/54.9, GET 131/136/127 → 161/158/155 → 132/130/137 µs | the reactor's single thread with its `writev` batching and spin is worth a third of the throughput and 25 µs of latency on eight cores | reactor stays; the threads mode remains an A/B instrument |
| 2026-09-05 | idle | the balloon's 24 interrupts a second: a private VM idling with no container reads 0.85/s on that line over a minute (806 → 857), and the two attribution runs that read 24/s each had a privileged container running for the sample. So the traffic is free page reporting driven by the agent's cache trim while a container exists, the state a working machine is in, and the record's idle case, measured after the net teardown with no container, does not see it. A second stage that stopped containerd with SIGSTOP hung: the next snapshot was a `docker run`, which needs containerd | the idle figure has two regimes, with and without a container, and the README's is the emptier one; a container-present idle needs its snapshots taken from outside docker (the agent's control channel or the VMM's own counters) | pending: an idle-with-container measurement without docker in the probe |
| 2026-09-05 | M1 network | the inbound rows under the per-connection waiters, which the first A/B left out and a re-record read low: A/B/A/B on the M1, port-in 43.0/41.6/43.7 → 44.4/46.1/45.8 → 29.6/44.2/40.0 → 45.3/46.1/36.2 Gbit/s; Mac-to-container 27.7/25.2/41.9 → 47.3/33.7/47.2 → 26.3/28.1/40.3 → 47.4/46.4/31.5; connects 11.7–20.2k either way | the inbound path is better or level, and the Mac-to-container direction clearly better (its blocked sender used to be woken by everyone's batches and now by its own credit); the low port-in in the re-record was the M1's scatter, which on these rows is a third of the value | the M1 network stage is re-run once more, pre-declared as the record whatever it reads |
| 2026-09-05 | GET p99 | the reactor's spin window against the GET tail on the M5 (per-connection-waiters build), A/B/A/C/D with idle beside each: 50 µs → median 59, p99 250/167/165, idle 6 ms/s at 157 wakeups; 200 → 66, 215/209/309, 5 at 134; 50 → 59, 230/141/167, 6 at 152; 500 → 58, 194/126/108, 6 at 157; 0 → 57, 218/125/177, 6 at 154 | the tail is not the spin: not monotonic in the window, and the M5's median no longer needs a spin at all (the M1's did, 149 → 129 µs, which is why 50 stays). p99 on this case varies by half between repetitions | parked; the row wants a quieter instrument before a lever |
| 2026-09-05 | idle | ten seconds of `strace -f` on the guest's daemons at idle: containerd 42 `nanosleep(10 ms)` and 69 zero-timeout `epoll_pwait` in the ten seconds, 5 futex wakes a second; dockerd 101 `nanosleep(20 µs)` (Go's sysmon spinning up) and 6 epolls; the agent nothing. Against 23 and 3.6 context switches a second measured without strace, so a wake is two switches and containerd's Go runtime is at 5–10 wakes a second, sysmon-shaped | not ours to change short of a different containerd build; the guest's 87 timer interrupts a second are mostly the tick restarting around task wakes (kworkers once a second per CPU, RCU, the daemons), and `vm.stat_interval=10` bought four | parked: idle CPU 6 vs 2 ms/s stays a per-wakeup cost (47 µs against OrbStack's 20) rather than a count |
| 2026-09-05 | record | the network stage on the per-connection-waiters build, both machines, alone (the M1's twice, the later run pre-declared the record): M5 egress 99.0/107.2/100.6 Gbit/s (record 93.3, OrbStack 97.2), Mac-to-container 95.4/95.0/93.1 (82.1), port-in 85.9/93.6/94.6 (91.1), port-out 92.9/92.7/98.3 (87.3, OrbStack 93.1), GET 57/59/57 µs (63), p99 177/158/123, DNS 37 µs (63), idle 6 ms/s at 150 wakeups; M1 egress 56.7/57.5/57.1 (53.8), Mac-to-container 47.3/46.6/47.0 (42.5), port-in 45.5/45.3/46.0 (44.1), port-out 53.7/55.0/54.7, GET 130/128/126, p99 284/228/335, DNS 136/92/130, idle 11 ms/s at 135 | the M5 network table is now won on every throughput row but port-out, which is level; the M1's Mac-to-container and port-in rows widen their lead and egress closes to 57 vs 65 | committed |
| 2026-09-05 | idle footprint | the cheap lever for item 9 measured: a fresh VM at 650 MB after a minute idle; `drop_caches` from a privileged container frees 77 MB of the guest's page cache (Cached 136 → 58 MB) and the host footprint reads 715, 691, 689, 688 MB over the next two minutes, the container that did the dropping having cost more than it freed | freed page cache is scattered 4 KiB pages and never forms the 2 MiB blocks free page reporting hands back; the boot-time cache is not the host's to recover this way | parked; the page array for the configured RAM (190 MiB of a 12 GiB guest) is the real item, which is a smaller default or virtio-mem |
| 2026-09-05 | M1 | the queue pollers and the filesystem server's threads run at default QoS unless `LIGHTER_SERVER_QOS` is set, so on an M1 (four performance, four efficiency cores, eight vCPUs) they can land on an efficiency core. A/B/A/B of the raise on the M1: egress 58.3/58.4/58.2 → 56.0/54.6/36.3 → 58.0/57.8/57.4 → 55.6/55.4/54.7 Gbit/s; port-out 56.0 → 55 → 56 → 54; GET level; share `pnpm install` 6.08/6.04/7.19 → 5.66/5.56/6.10 → 6.12/6.10/6.14 → 5.26/5.25/5.33 s; own-disk `cp -a` level | the filesystem's threads want the raise on the M1 (10–13% on the share install, which would take the row from OrbStack's 5.91), the vsock poller does not (4% off egress). One switch for both was the wrong shape | split it: the fs server and its poller raised when the guest's vCPUs are at least the Mac's cores, the other pollers never; env still overrides |
| 2026-09-05 | idle | `sample` of the VMM for ten seconds after a quiet minute on the M5 (7395 samples a thread): every thread's top of stack is a wait but the vCPUs', which show 120 samples in `hv_trap` and nothing of lighter's own code anywhere near the top | the six milliseconds a second are the hypervisor's exit and entry and the guest's timer work, not anything in the VMM's loop; at 47 µs a wakeup against OrbStack's 20 the difference is below our layer or in the count, and the count is the guest's daemons and per-CPU kernel timers | parked with the numbers; a smaller default vCPU count would cut the per-CPU timers, which is a product question |
| 2026-09-05 | container start | a container's life by Docker's own event clock on the M5, ten warm runs of `alpine true`: create → start 38 ms, start → die 75 ms, die → destroy 8 ms, 127 in all. By variant, eight runs each: attached 48/72/7, detached 45/75/8, `--network none` 45/17/10 (detached 44/18/10), `--init` 48/90/10 | create → start barely moves without a network (45 → 44), and start → die loses 57 ms of its 75: dockerd tears the network sandbox down before it reports the death, so the veth delete, the netns and the rules are in that number. The process itself is the 17 | the network teardown at exit is the rung: about 57 of 127 ms; HZ=1000 against 250 on the same test next, to see how much of it is jiffies |
| 2026-09-05 | container start | the same phases on the saved HZ=1000 kernel against 250, both with expedited RCU and `first_fqs=0`, same rootfs, A/B/A: 250 → create→start 45.9, start→die 58.5, die→destroy 7.5, 112 in all; 1000 → 36.5, 29.9, 3.1, 70; 250 again → 45.9, 60.2, 10.3, 116 | 45 ms of a container's life is still counted in jiffies, 30 of it in the network teardown after exit. The teardown's own sleeps are literal: `msleep(1)` per turn of `netdev_wait_allrefs_any` and `schedule_timeout_uninterruptible(1)` in `dev_deactivate_many`, each a whole jiffy, 4 ms at 250 | next: a kernel patch that makes those two sleeps hrtimer sleeps, then the phases again |
| 2026-09-05 | container start | kernel patch 0024, the two literal jiffy sleeps in the teardown (`msleep(1)` per turn of `netdev_wait_allrefs_any`, `schedule_timeout_uninterruptible(1)` in `dev_deactivate_many`) made hrtimer sleeps; built and confirmed applied; the phases A/B/A/B on the M5: start → die 57.6 → 63.6 → 59.2 → 62.6 ms, totals 111 → 114 → 109 → 114 | nothing; those sleeps are not where the jiffies go. What is left tick-bound is the grace periods: a normal one ends when every busy CPU has passed a quiescent state, which without a full-dynticks kernel is its next tick, 4 ms at 250 and 1 at 1000, and a veth's exit waits on several | patch dropped; the lever is the tick rate itself, per machine: 1000 where the vCPUs leave cores free (the M5's 70 ms container against 112), 250 where they fill them (the M1) |
| 2026-09-05 | M1 | the raise split by thread (filesystem server and its poller raised when the vCPUs fill the cores, other pollers never), A/B/A/B on the M1 against the raise forced off: egress 57.3/39.2/58.1 → 57.8/57.3/58.0 → 57.4/57.9/57.0 → 55.8/56.6/57.0; port-out level; share `pnpm install` 5.80/5.91/5.67 → 5.61/5.57/5.29 → 5.95/6.08/6.18 → 6.06/5.54/5.28; share `npm ci` 12.79/11.45/11.15 → 12.47/12.61/12.47 → 11.59/11.48/11.41 → 13.50/13.26/12.49, its user time 12 → 14 s | pnpm gains 7%, npm loses 8%: the raised server threads take the cores npm's own CPU work wanted. Not a win to ship | reverted; `LIGHTER_SERVER_QOS=1` stays as the instrument it was |
| 2026-09-05 | kernel | two kernels from one build, differing in HZ alone: `Image` at 250, `Image-hz1000` at 1000 (`build-kernel.sh` takes `KERNEL_HZ`, `build.sh` builds both on separate source volumes, `LIGHTER_KERNEL_ONLY=` for one). The CLI boots the 1000 one where the configured vCPUs are at most half the Mac's cores (`config::kernel_hz`; `LIGHTER_KERNEL_HZ` overrides), the harness and the gates take the same choice (`LIGHTER_BENCH_KERNEL`, `LIGHTER_GATE_KERNEL` to name one), the tarball carries both, `doctor` says which and why. On the M5 through the CLI: a container's life 60 ms (create → start 29, start → die 28, die → destroy 3) against 112–127 on the 250 image | the rule is the same shape as the idle-poll window's and for the same reason: every tick is a VM exit per vCPU, a cost where the vCPUs fill the cores and free where they do not, and what it buys is every wait the kernel counts in jiffies. The M1 (eight vCPUs on eight cores) keeps 250, as its own A/B said | kept; M5 re-recorded on it next |
| 2026-09-05 | kernel | the tick-rate rule at its boundary: an M1 guest with four vCPUs on eight cores (the CLI's default there, and the point where the rule flips to 1000), A/B/A/B of the 250 and 1000 images: share `npm ci` 10.6/10.5/10.3 → 10.5/11.6/10.8 → 10.4/10.5/11.4 → 10.8/10.5/10.8 s; port-in 46.5/46.8/46.7 → 37.7/46.6/43.6 → 46.6/31.3/42.1 → 46.5/47.1/45.2 Gbit/s; GET 103–109 either way; container start 240/192/200 → 207/161/136 → 195/204/177 → 178/144/140 ms; `rm -rf` 546–598 either way | at half the cores 1000 buys the container start 40 ms and costs nothing measurable; the rule (`cpus * 2 <= cores`) holds at its edge on the machine that rejected 1000 with all eight | kept |
| 2026-09-05 | clock | the guest's wallclock was set in three places, every one to a whole second: `lighter.time=` on the command line read before the kernel started (0–1 s of truncation plus the boot's 0.4 s), `date -s` in init, and `settimeofday` with `tv_usec: 0` from a value the host read once and then retried for up to five seconds after a wake. Steady state measured at about a second slow | the agent asks instead (`lighter_vmm::clock`, vsock port 2382: a byte in, seconds and nanoseconds out): three rounds on one connection, the shortest trip kept, the clock set to that answer plus half its trip plus the monotonic time since, `clock_settime`, at start and on the host's wake nudge, which now carries no time. The command-line seed stays for the seconds before the agent, for certificates. Measured: trip 25–35 µs; the guest against the host through a whole `docker run` −1.7 to −6.7 ms where the run itself is 83–103 ms, and the same after the wake verb | kept |
| 2026-09-05 | kernel | the tick-rate rule against the share installs, same session, A/B/A/B of the two images. M5 (nine vCPUs on eighteen cores, the machine loaded by five Claude sessions but steadily): `npm ci` 6.59/6.42/6.63 → 6.41/6.45/6.23 → 7.03/6.90/6.97 → 6.75/6.92/6.23 s; `pnpm install` 4.22/4.00/3.86 → 4.15/4.93/4.61 → 4.23/4.15/3.98 → 4.92/8.32/5.90; `yarn install` 5.15/5.30/5.21 → 5.45/6.01/5.17 → 5.33/5.17/5.48 → 5.42/5.14/5.56; `rm -rf` 2.4–3.3 either way. M1 (four vCPUs on eight cores, alone): npm 11.09/10.71/10.88 → 11.41/11.67/11.96 → 10.90/11.01/10.81 → 11.43/10.96/10.86; pnpm 6.46/5.74/5.53 → 6.51/5.12/6.30 → 5.75/5.17/5.20 → 6.65/5.32/5.67; yarn 11.4/9.9/9.7 → 11.6/10.4/8.9 → 10.8/9.7/9.2 → 11.4/10.2/9.1; rm -rf 2.79–2.89 → 2.76–3.03 → 2.79–2.88 → 2.92–2.96 | 1000 costs pnpm on the share 10–20% on the M5 and reads level to slightly worse on every M1 row, against a container's life 112 → 60 ms and the first container after start 0.55 → 0.47 s. The share installs are the table; the rule that picked 1000 where the vCPUs are at most half the cores is dropped | 250 is the default on every machine; `Image-hz1000` ships and `LIGHTER_KERNEL_HZ=1000` boots it, for a container-heavy day; the M5 record taken on 1000 under load is discarded, the committed 250 record stands |
| 2026-09-05 | gates | the full chain on the day's tree (per-connection waiters, the grace-period scan, the clock, Rosetta only, two kernels with 250 the default), on the M5 with `LIGHTER_BENCH_ALLOW_NOISY=1`: m1–m7 green; m8 failed at "could not skew the guest clock", because it skewed through the control channel's `time <seconds>` verb, which now carries no time | the gate puts the clock an hour back from inside the guest instead (`date -u -s` in a privileged container), which is nearer what a sleep does, and the wake path (`lighter resync`, the agent asking the host) put it right within the gate's second: m8 green | all eight green on bac85ac's tree |
| 2026-09-05 | memory | virtio-mem, phase 1: a device (`virtio/mem.rs`, id 24, 128 MiB blocks so the guest runs big-block mode with one device block per arm64 memory block, `UNPLUGGED_INACCESSIBLE` offered, unplug released through the balloon's `MADV_FREE_REUSABLE` path), a hotplug window after RAM in the layout, `CONFIG_VIRTIO_MEM` + `MHP_DEFAULT_ONLINE_TYPE_ONLINE_MOVABLE` + `PSI` in the kernel, `LIGHTER_VIRTIO_MEM=<base MiB>` to split the configured memory into base plus range, `LIGHTER_MEM_PLUG_MIB` for the first offer, `lighter-bench --mem-plan` to move the offer by the clock | M5, 12 GiB configured, idle a minute after boot: plain 695 MiB; 1 GiB base with the 11 GiB range plugged 546; range empty 357. A 4 GiB plug completed 25 ms after the offer, the unplug 80 ms; guest MemTotal 986 MB → 5.18 GB → 986 MB → 3.08 GB across a plug/unplug/replug plan, block count 8 → 40 → 8 → 24. 6.18's virtio-mem does not pass `MHP_MEMMAP_ON_MEMORY` (only dax, ACPI and s390 do), so a plugged block's page array comes from the base and goes back on unplug; `memory_hotplug.memmap_on_memory=1` changes nothing here. With the whole range plugged the policy's balloon cap follows the base, not the total (`MemoryPolicy::start` takes `ram_bytes`), which is phase 2's business | the device works; 357 MiB with nothing plugged is the floor before any trimming; on to a policy |
| 2026-09-05 | memory | virtio-mem, phase 2: the policy. The base is a quarter of the configured memory (a gigabyte at least; `mem::split`), the range is plugged whole at boot, the agent's memory line gains two flags (`need`: busy with under a quarter available; `nothing_runs`), the host doubles the guest on `need` and takes back what a quiet guest with no container offers; the Docker socket proxy makes the guest whole before any `containers/create`, `/start`, `/restart`, `/build` or `/session` request reaches dockerd (≤ 25 ms, once); a 30 s hold after any growth; the agent at `oom_score_adj` -1000, dockerd and containerd at -900 | M5 (noisy), 12 GiB: idle a minute after boot 314–351 MiB against 695 plain; a container saw `MemTotal` 12477480 kB; after it left, back to the floor 33 s later. First cut shrank with containers running and a tmpfs writer in one took the 3 GiB base in 140 ms, virtio-mem got ENOMEM adding memory, and the OOM killer took dockerd, containerd and the agent by size before the writer; with the range kept whole while any container runs the same writer did 4.9 GB at 4.2 GB/s without incident. A trivial container left the guest quiet and the range went in and out within a second of every start until the hold | shipped as the default, `LIGHTER_VIRTIO_MEM=0` for the A/B; no container means the floor, any container means the whole size |
| 2026-09-05 | memory | where the floor goes, with the range out (M5, 12 GiB configured, 3 GiB base, 45 s after boot, read through the agent's `sh` verb so no container is started): host footprint 349 MiB. Guest: `Slab` 92 MB of which `slabinfo` accounts for 20 — the other 70 is large kmallocs, which are the vsock receive ring: 256 entries of 256 KiB buffers (patch 0015) posted for the machine's life; `Cached` 59 MB (the engine's text pages after the trim), `AnonPages` 8 MB (dockerd and containerd in zram: RSS 0.9 and 4.5 MB), swiotlb already 3 MB (the kernel scales it with RAM; `swiotlb=noforce` saved 10 MiB, noise). A/B of the ring depth (`LIGHTER_VSOCK_RX`): 256 → Slab 92 MB, footprint 351; 64 → 43 MB, 339; 32 → 35 MB, 315 — the host saving lags the guest's at 45 s because untouched buffers were never resident, and catches up as traffic recycles them. After one container ran and left the host held ~70 MiB more (dockerd back in from zram +37, slab +26, cache +18) and a second container added nothing; forced compaction returned nothing, `drop_caches` 31 | the vsock ring is the largest single item; its depth against network throughput is the next A/B (`.logs/vsockrx-ab.sh`) | measure, then set the depth |
| 2026-09-05 | vsock | receive ring depth against the network cases (M5, two reps each, Mbit/s; `LIGHTER_VSOCK_RX`): 256 — egress 100605/103972, egress-r 89826/96718, port 90178/90769, port-r 98419/91126, udp 5004/5056, http 59/60 µs; 128 — 104280/97627, 95124/96550, 90278/92557, 91678/89999, 5120/5018, 76/57; 64 — 95533/105800, 89401/88687, 86504/88638, 91598/86438, 4975/5029, 58/61 | 128 is 256 on every case; 64 dips a few percent where the Mac sends to the container | 128 is the default (32 MB of buffers the guest no longer holds) |
| 2026-09-05 | memory | the base an eighth of the configured memory instead of a quarter (M5, 12 GiB, 45 s after boot, range out, ring 128): 3 GiB base 335 MiB, 1.5 GiB base 277 — 58 MiB for 24 of page array, the rest the structures the kernel sizes by RAM (swiotlb 3 → 1 MB, reserved 105 → 73 MB) | under 300 | an eighth, a gigabyte at least |
| 2026-09-05 | vmm | `vmmap` on the idle VMM: one `MALLOC_LARGE (empty) 49.3M` region, dirty — the kernel image `fs::read` into a Vec, copied into the guest and dropped, kept by macOS's cache of freed large allocations for the life of the process. Mapped read-only instead (`kernel::Mapped`), the region is gone | 49 MB off every footprint reading since the first boot | shipped |
| 2026-09-05 | memory | first records on the virtio-mem tree (88fc708), both machines. M5: idle a minute after boot 237 MiB (was 635), boot-docker 353–374 ms (was 390–409), but 15 and 60 s after an npm install with nothing running 1341/1330 (was 874/850), and own-disk installs npm 10885/6682/5937 ms (was 4506/4458/4449), pnpm 1824/1270/1241 (was 1224/1117/1114), yarn level; share installs within 6%. M1 (4 GiB guest): idle 246 (was 362), boot 440–453 (was 454–463), after-install rows 1073/1076 (was 624/616), yarn 14.1/12.2/11.2 s (was 12.9/10.6/10.2). Two causes read off the design: every offer went to the range and none to the balloon, so the base's freed cache in file-sized pieces (what the balloon used to take a host page at a time) waited on reporting; and zram swap at the kernel's default swappiness let ordinary reclaim swap node's heap under an install's cache pressure | fix: a line the range does not take goes to the balloon as before (and a release always does); `vm.swappiness=0`, the trims' explicit swappiness untouched | re-measured on the M5 (`.logs/vmem-chain4.sh`), then both records again |
| 2026-09-05 | memory | own-disk installs against the base size (M5, two reps, ms, `LIGHTER_VIRTIO_MEM`): plain — pnpm 1231/1144, npm 5041/4982; 3072 (a quarter) — 1167/1115, 4793/5055; 1536 (an eighth) — 1290/1303, 7250/6780. The base is the kernel's only zone for unmovable allocations, and when it is short the kernel reclaims slab node-wide: `npm ci`'s inodes and dentries thrashed | an eighth costs npm 40% and pnpm a tenth on the guest's own disk; a quarter is plain memory | a quarter (58 MiB more at idle than an eighth: 335 against 277) |
| 2026-09-05 | memory | after the balloon was given the lines the range does not take, m6 read one block stuck plugged (128 MiB) after 60 s with nothing running and idle CPU 2.48% against a 1.0% budget: the balloon's pages do not migrate (guest patch 0014), one inflated into a movable block pins the block in, and virtio-mem retries the unplug at a cost | the balloon engages only once the range is fully out; an unplug outstanding for a minute is given up (the offer goes back to what is plugged) so the driver stops trying | shipped; m6 rerun (`.logs/vmem-chain6.sh`) |
| 2026-09-05 | memory | the tree with a quarter base, the balloon only once the range is out, a stuck unplug given up after a minute, the balloon back before a container's plug, `vm.swappiness=0` (M5, 12 GiB): m6 passes (range out at 272 MiB, container sees 8112 of 8192, idle CPU 0.57%); memory case peak 3962, 15 s after 538, 60 s after 472 (the committed record 4168/874/850; OrbStack 5266/1874/1867); boot 361–381 ms to docker (was 390–409); idle a minute after start 311 (was 635); own-disk pnpm 1191/1109, npm 5245/4971 (plain 1231/1144, 5041/4982) | everything back where it was or better, and the idle rows halved | commit; both records again |
| 2026-09-05 | memory | second records (43a465d). M5: idle 273 MiB, installs level with the record on share and own disk, boot 353–418 ms; but container start 854/171/715 ms and the memory case 3179 at 15 s, 1174 at 60 s. M1: idle 203, own-disk installs level with its record (npm 8372/7844/9228 against 8711/7947/8739), container start 264–380 ms, memory 1140/1095. Two mechanisms: a container started from the floor waited on the balloon deflating two and a half gigabytes of mostly untouched base before the plug could have its page arrays (the driver's own retry after a plug it cannot do is fifty seconds, so a poke every hundred milliseconds while waiting); and the 30 s hold after the container's plug outlasted a seven-second install and the 15 s reading, with the balloon barred from acting while the range was in | the balloon pulses at the floor: inflated once the range is out (its pressure is what makes the kernel give up slab and cache), held three seconds, let go (the pages stay released until touched), and not inflated again until the range has been in; the hold is 10 s | `.logs/vmem-chain7.sh` |
| 2026-09-05 | memory | the pulse tree (M5, 12 GiB): m6 passes (range out 4 s after the last container, 254 MiB; idle CPU 0.69%); memory case 3653 peak, 479 at 15 s, 531 at 60 s; container start 126/137/151 ms, and against plain memory five reps each — range 130/133/127/130/148, plain 141/151/156/146/140; boot 361–374 ms; idle a minute after start 261 | a container's start costs nothing for the range; the after-install rows are half the pre-virtio-mem record and a quarter of OrbStack's | full gates, commit, third records |
